---
phase: 07-mining-pilot-handoff-and-recovery
plan: 06
type: tdd
wave: 3
depends_on: [07-04]
files_modified: [site/domain/ports/export-delivery.ts, site/domain/crm-handoff.ts, site/tests/crm-handoff.test.mjs]
autonomous: true
requirements: [REQ-crm-and-workspace-exports, REQ-initial-operating-target]
must_haves:
  truths: ["CSV materialization is current-state fenced, deterministic, and CSV-only", "Every artifact has an immutable manifest/checksum and safe delivery-expiry behavior", "No suppressed or otherwise ineligible contactable row is emitted"]
  artifacts: [{path: site/domain/crm-handoff.ts, provides: frozen eligibility snapshot, CSV serializer, manifest, and materialization service}, {path: site/domain/ports/export-delivery.ts, provides: narrow immutable artifact delivery port}]
  key_links: [{from: site/domain/crm-handoff.ts, to: Phase 5-6 projections, via: repository transaction-time eligibility/suppression fence}, {from: site/domain/crm-handoff.ts, to: site/domain/ports/export-delivery.ts, via: exact bytes/checksum immutable delivery}]
---
<objective>Implement deterministic CSV CRM Handoff. Purpose: create an auditable export artifact without a CRM connection or bypass of fresh verification, Package, identity, or suppression authority. Output: narrow delivery port and tested export service.</objective>
<context>@.planning/phases/07-mining-pilot-handoff-and-recovery/07-CONTEXT.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-RESEARCH.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-PATTERNS.md
@.planning/phases/06-governed-outreach-and-suppression/06-03-PLAN.md
@.planning/phases/06-governed-outreach-and-suppression/06-04-PLAN.md</context>
<read_first>Read the locked CSV, duplicate/suppression/reversal decisions and the Phase 6 package/suppression contracts. Implement a server-owned artifact only; do not add a CRM SDK, provider, opportunity, pipeline, or send path.</read_first>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Implement transactionally fenced snapshot and canonical CSV artifact</name><files>site/domain/crm-handoff.ts, site/tests/crm-handoff.test.mjs</files><behavior>- Snapshot server-rechecks current Export-ready/approved-package/fresh verified contact/no-suppression predicates and supplies explicit included/excluded reason ledger.
- Serializer uses fixed schema/header/order, UTF-8 RFC 4180, formula neutralization, stable identity dedupe, exact-byte SHA-256, and immutable versioned manifest.</behavior><action>Implement closed typed field mapping with stable Prospect ID, generic hierarchy IDs, account/target/role, verification evidence, score/evidence refs, offer/package, activity, source workspace/run IDs, and manifest ref. Canonically sort stable Prospect then Contact/contact-point ID; persist only safe identity refs/reasons for duplicate/suppression manifest. Never expose a raw contactable suppressed row or accept browser ordering/columns/eligibility.</action><verify><automated>cd site && node --test tests/crm-handoff.test.mjs</automated></verify><done>Every CSV row has current authority and every persisted byte has a reproducible manifest/checksum.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement immutable retry and authorized delivery boundary</name><files>site/domain/ports/export-delivery.ts, site/domain/crm-handoff.ts, site/tests/crm-handoff.test.mjs</files><behavior>- Same frozen definition/digest returns the original immutable artifact; changed snapshot creates a new version; semantic idempotency conflict rejects.
- Expired/failed delivery blocks access and records audit without regenerating bytes or changing current/historical eligibility.</behavior><action>Define an injected object/delivery interface accepting immutable bytes/metadata only, then wire repository idempotency/audit/delivery-state handling. Reauthorize owner and current artifact access at download; if current contact eligibility changed, preserve historical artifact but require a newly evaluated request. Do not implement CRM sync, browser CSV construction, automatic retry, external contact delivery, or provider credentials.</action><verify><automated>cd site && node --test tests/crm-handoff.test.mjs</automated></verify><done>Retry/reversal/expiry behavior is deterministic, auditable, and cannot silently create a different handoff.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| T-07-16 upstream eligibility bypass | mitigate | final transactional fence |\n| T-07-17 spreadsheet injection | mitigate | pre-quote neutralization vector suite |\n| T-07-18 artifact mutation/download replay | mitigate | immutable manifest/checksum plus reauthorization |</threat_model>
<verification>Run `cd site && node --test tests/crm-handoff.test.mjs`.</verification>
<acceptance_criteria>CSV is the sole handoff mechanism and preserves all row, snapshot, suppression, determinism, and history decisions.</acceptance_criteria>
<success_criteria>Eligible contact rows are materialized deterministically without creating CRM or outreach authority.</success_criteria>
<output>Create 07-06-SUMMARY.md only when genuinely complete.</output>
