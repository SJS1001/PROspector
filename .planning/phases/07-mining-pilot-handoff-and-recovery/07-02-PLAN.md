---
phase: 07-mining-pilot-handoff-and-recovery
plan: 02
type: tdd
wave: 1
depends_on: [06-10]
files_modified: [site/tests/crm-handoff.test.mjs]
autonomous: true
requirements: [REQ-crm-and-workspace-exports, REQ-initial-operating-target]
must_haves:
  truths: ["CSV has one deterministic row per current eligible contact-point plus stable Prospect identity", "Contact-row count remains distinct from the weekly Prospect metric", "Suppression and every stale or revoked upstream predicate fail closed at export time"]
  artifacts: [{path: site/tests/crm-handoff.test.mjs, provides: deterministic CSV, manifest, exclusion, and retry contract}]
  key_links: [{from: site/tests/crm-handoff.test.mjs, to: current Phase 4-6 projections, via: transaction-time snapshot fence}, {from: site/tests/crm-handoff.test.mjs, to: persisted artifact, via: SHA-256 exact-byte assertion}]
---
<objective>Specify deterministic CSV CRM Handoff before export code. Purpose: prevent a CSV artifact from bypassing current verification, package, identity, or Company-wide suppression authority. Output: synthetic RED contract suite.</objective>
<context>@.planning/phases/07-mining-pilot-handoff-and-recovery/07-CONTEXT.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-RESEARCH.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-VALIDATION.md
@.planning/phases/05-controlled-enrichment-and-verified-contacts/05-VALIDATION.md
@.planning/phases/06-governed-outreach-and-suppression/06-04-PLAN.md</context>
<read_first>Read the locked CSV CRM handoff and Duplicate, suppression, and reversals decisions; consume Phase 5 freshness/identity and Phase 6 package/suppression projections as inputs only.</read_first>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Write eligibility snapshot, duplicate, and suppression RED tests</name><files>site/tests/crm-handoff.test.mjs</files><behavior>- Include only a current Export-ready Prospect with a current approved package and a fresh mailbox_verified/source_verified, non-suppressed Enriched Contact/contact point.
- Deduplicate only stable Prospect ID plus current eligible contact-point identity; alias/merge, exact/domain/contact/organization/company suppression, stale verification, package revocation, drift, deletion, and cross-workspace input yield no contactable row.</behavior><action>Use injected fake Phase 4-6 projections and transactional-interleaving fixtures. Assert each exclusion reason is separately present, suppression may produce only a labelled non-contactable manifest, no display-name/email/CRM-ID dedupe exists, and current recheck prevents a stale prior snapshot/UI from authorizing a row. Per the locked CSV and duplicate/suppression decisions, assert no CRM client, send, provider, or schedule port is reachable.</action><verify><automated>cd site && node --test tests/crm-handoff.test.mjs</automated></verify><done>RED tests distinguish eligible contact rows from Export-ready Prospect count and prove all invalid current inputs fail closed.</done></task>
<task type="auto" tdd="true"><name>Task 2: Write exact-byte CSV and immutable retry RED tests</name><files>site/tests/crm-handoff.test.mjs</files><behavior>- UTF-8 fixed-header RFC 4180 bytes use canonical Prospect-ID then Contact/contact-point-ID ordering and neutralize cells beginning =, +, -, or @ before quoting.
- Same frozen snapshot and export-definition digest return the original artifact/checksum; changed snapshot creates an immutable new version, while expiry/failure blocks delivery without changing eligibility or bytes.</behavior><action>Add table cases for quotes, commas, CR/LF, Unicode, null/empty values, shuffled input, multi-contact Prospects, formula cells, idempotency-key semantic conflict, and download authorization. Assert manifest schema version, selection timestamp, row/unique-Prospect counts, exact SHA-256, source workspace ID, policy/configuration/package digests, identity reasons, and bounded audit reference.</action><verify><automated>cd site && node --test tests/crm-handoff.test.mjs</automated></verify><done>RED contract proves byte-identical retries and immutable history without CRM integration.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| T-07-04 stale/suppressed contact disclosure | mitigate | transaction-time projections and alias tests |\n| T-07-05 CSV injection or byte drift | mitigate | formula/escaping/checksum vectors |\n| T-07-06 duplicate/retry ambiguity | mitigate | stable identity and semantic idempotency vectors |</threat_model>
<verification>Run the focused contract suite; it must use synthetic data and fake ports only.</verification>
<acceptance_criteria>CSV-only handoff has deterministic bytes, an immutable manifest, explicit exclusions, and no CRM/sending authority.</acceptance_criteria>
<success_criteria>Every current eligibility, dedupe, suppression, retry, and delivery-expiry decision is executable as a test.</success_criteria>
<output>Create 07-02-SUMMARY.md only when genuinely complete.</output>
