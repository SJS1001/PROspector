---
phase: 07-mining-pilot-handoff-and-recovery
plan: 03
type: tdd
wave: 1
depends_on: [06-10]
files_modified: [site/tests/workspace-recovery.test.mjs]
autonomous: true
requirements: [REQ-crm-and-workspace-exports]
must_haves:
  truths: ["Archive excludes passphrases, credentials, tokens, and secrets while preserving records, objects, history, manifests, and tombstones", "Dry run proves an authorized compatible clean target with zero target writes", "Restore preserves IDs/history/hashes/tombstones and leaves every effect disabled"]
  artifacts: [{path: site/tests/workspace-recovery.test.mjs, provides: archive integrity, dry-run zero-write, restore replay, and effect-fence contracts}]
  key_links: [{from: site/tests/workspace-recovery.test.mjs, to: archive envelope, via: authenticated decrypt/checksum vectors}, {from: site/tests/workspace-recovery.test.mjs, to: restore target ports, via: write and external-effect counters}]
---
<objective>Specify encrypted portable-workspace recovery before selecting an implementation. Purpose: define the failure-closed archive, dry-run, restore, and replay invariants without fabricating crypto or hosted proof. Output: synthetic RED recovery suite.</objective>
<context>@.planning/phases/07-mining-pilot-handoff-and-recovery/07-CONTEXT.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-PATTERNS.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-VALIDATION.md
@docs/adr/0004-private-sites-pilot-and-portability.md</context>
<read_first>Read the locked Archive, dry run, restore, and replay decisions and the 07-VALIDATION zero-target-write requirements. Do not choose a cryptographic package in this plan.</read_first>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Write archive envelope, inclusion, and authorization RED tests</name><files>site/tests/workspace-recovery.test.mjs</files><behavior>- Recent owner reauthentication and server-derived workspace are required; passphrases exist only in operation memory and never in D1/R2 metadata, audit, browser persistence, logs, or fixture output.
- Canonical ordered archive manifests include records, immutable decisions/history/audit, content-addressed objects, dependencies, export metadata/artifacts, and suppression/deletion tombstones; credentials/tokens/secrets are excluded.</behavior><action>Write fake archive/object/delivery/reauth ports and test vectors for a versioned authenticated envelope/KDF interface. Require minimized audits, expiry authorization, canonical hashes/counts, same-key/same-digest behavior, and indistinguishable untrusted failure for wrong passphrase/tampering. Do not install crypto, create an archive, or access a host.</action><verify><automated>cd site && node --test tests/workspace-recovery.test.mjs</automated></verify><done>RED tests define archive content, safe audit boundary, and authenticated-envelope behavior without a stored secret.</done></task>
<task type="auto" tdd="true"><name>Task 2: Write dry-run, restore, replay, and effect-fence RED tests</name><files>site/tests/workspace-recovery.test.mjs</files><behavior>- Unauthorized, expired, tampered, missing-object, non-clean, version-skewed, or invariant-failed recovery aborts before target release with zero target D1/object writes and zero scheduler/runner/send/provider/delivery calls.
- Only explicit matching owner restore after current successful dry run/nonce applies idempotently; replay retains stable IDs, append-only history, hashes, configuration/package/export digests, and tombstones while all effects start paused/disabled.</behavior><action>Create deterministic target-state and fake-port counters covering clean-target marker, compatibility range, referential/unique/current-projection checks, lock/nonce races, partial-write rollback, second generic hierarchy, and minimized recovery outcomes. Assert dry run itself is read-only and never accepts client target scope as authority.</action><verify><automated>cd site && node --test tests/workspace-recovery.test.mjs</automated></verify><done>Every recovery denial proves unchanged target state; positive replay proves all operational effects remain disabled.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| T-07-07 secret/passphrase retention | mitigate | exclusion scans and fake-port audits |\n| T-07-08 tampered/unsupported archive | mitigate | authenticated-envelope and compatibility vectors |\n| T-07-09 partial/effectful restore | mitigate | zero-write counters, nonce lock, disabled-effect assertions |</threat_model>
<verification>Run `cd site && node --test tests/workspace-recovery.test.mjs`; RED is expected before the gated recovery implementation.</verification>
<acceptance_criteria>Recovery behavior is fully specified without treating tests or fixtures as a portable archive capability proof.</acceptance_criteria>
<success_criteria>Archive inclusion/exclusion, dry-run-first restore, idempotency, and no-effect replay all have executable assertions.</success_criteria>
<output>Create 07-03-SUMMARY.md only when genuinely complete.</output>
