---
phase: 07-mining-pilot-handoff-and-recovery
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified: [site/drizzle/0010_handoff_recovery.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0010_snapshot.json, site/domain/handoff-recovery-repository.ts]
autonomous: true
requirements: [REQ-crm-and-workspace-exports, REQ-initial-operating-target]
must_haves:
  truths: ["D1 represents immutable upstream schedule-state observations, weekly, export, archive, dry-run, restore, and bounded audit evidence without recreating upstream authority", "Every mutation derives owner/workspace on the server and binds idempotency to canonical semantics", "Restore initializes schedules disabled even when source history records a prior Phase 4 activation"]
  artifacts: [{path: site/drizzle/0010_handoff_recovery.sql, provides: additive Phase 7 evidence schema}, {path: site/domain/handoff-recovery-repository.ts, provides: admitted transaction and projection seam}]
  key_links: [{from: site/domain/handoff-recovery-repository.ts, to: site/drizzle/0010_handoff_recovery.sql, via: scoped foreign keys, unique constraints, and D1 batches}]
---
<objective>Build the durable, generic Phase 7 evidence foundation. Purpose: make handoff/recovery history immutable and arbitration server-side before services or UI are wired, while consuming rather than duplicating schedule authority. Output: additive migration and admitted repository seam.</objective>
<context>@.planning/phases/07-mining-pilot-handoff-and-recovery/07-PATTERNS.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-RESEARCH.md
@.planning/phases/06-governed-outreach-and-suppression/06-02-PLAN.md
@.planning/phases/06-governed-outreach-and-suppression/06-04-PLAN.md</context>
<read_first>Read the Phase 6 migration/repository and suppression-tombstone patterns plus the Phase 7 upstream-input table. The schema must reference upstream projections/history instead of duplicating qualification, verification, package, or suppression rules.</read_first>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add additive handoff and recovery evidence schema</name><files>site/drizzle/0010_handoff_recovery.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0010_snapshot.json</files><behavior>- Immutable upstream schedule-state observations bind the exact Phase 4 Profile configuration, weekday 06:00 America/Toronto definition, source revision/digest, and safe audit reference without creating Phase 7 schedule authority.
- Immutable snapshot/manifest/artifact, suppression-exclusion, archive, delivery-expiry, dry-run, restore nonce/lock/result, and bounded audit evidence retain workspace/subject/digest/version lineage.
- Constraints prevent cross-workspace references, mutable historical bytes/manifests, duplicate active restore applies, and target release without an accepted dry run; restored targets initialize schedules disabled and require fresh upstream Phase 4 readiness activation.</behavior><action>Following the existing D1 chain, create additive generic tables/enums/FKs/partial unique indexes for upstream schedule-state evidence and artifact/recovery metadata. Do not represent a Phase 7 owner activation command, hosted scheduler provisioning state, or scheduler callback. Preserve append-only Phase 6 tombstones and source history. Store object keys/content hashes/safe references only; never store passphrases, credentials, OAuth/bearer tokens, raw archive bytes, raw contacts, or a Mining-only column/constraint. Update Drizzle metadata consistently.</action><verify><automated>cd site && node --test tests/weekly-outcome.test.mjs tests/crm-handoff.test.mjs tests/workspace-recovery.test.mjs</automated></verify><done>Additive storage preserves upstream schedule evidence and all Phase 7 evidence while restore state remains disabled below UI.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement admitted repository transactions and current-input joins</name><files>site/domain/handoff-recovery-repository.ts</files><behavior>- Owner/workspace, expected revision, idempotency key, and canonical operation digest are server-derived for report, export, archive, dry-run, and restore commands.
- Repository exposes immutable Phase 3-6 histories/current projections, including Phase 4 schedule state, and transactional suppression fence without accepting client workspace, target, artifact, package, eligibility, or schedule claims.</behavior><action>Implement typed D1 reads/batches for stable IDs, first-transition history, upstream schedule/readiness status, current contact/package/suppression projection, artifact/manifest persistence, and restore locks. Provide bounded audit writer and zero-write dry-run target seam. Keep authoritative rule decisions in upstream modules; this repository only joins and persists evidence.</action><verify><automated>cd site && node --test tests/weekly-outcome.test.mjs tests/crm-handoff.test.mjs tests/workspace-recovery.test.mjs</automated></verify><done>All later services share one workspace-scoped persistence seam with semantic replay protection.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| T-07-10 cross-workspace or forged authority | mitigate | admitted repository joins and FK tests |\n| T-07-11 mutable manifest/audit | mitigate | append-only schema and canonical-operation uniqueness |\n| T-07-12 secret persistence | mitigate | explicit schema exclusion and audit minimization |\n| T-07-31 duplicate or restore-reactivated schedule authority | mitigate | upstream-only schedule projection and restore-disabled constraint |</threat_model>
<verification>Run all three Phase 7 focused contract suites; inspect that migration is additive.</verification>
<acceptance_criteria>A generic, durable persistence seam exists for Phase 7 evidence but grants no external or restore authority.</acceptance_criteria>
<success_criteria>Current upstream input, immutable artifact/recovery records, and suppression lineage are represented safely.</success_criteria>
<output>Create 07-04-SUMMARY.md only when genuinely complete.</output>
