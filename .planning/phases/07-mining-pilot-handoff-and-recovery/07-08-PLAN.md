---
phase: 07-mining-pilot-handoff-and-recovery
plan: 08
type: tdd
wave: 4
depends_on: [07-04, 07-07]
files_modified: [site/domain/ports/workspace-archive.ts, site/domain/workspace-archive.ts, site/domain/restore-verifier.ts, site/domain/restore-apply.ts, site/tests/workspace-recovery.test.mjs]
autonomous: true
requirements: [REQ-crm-and-workspace-exports]
must_haves:
  truths: ["Recently reauthenticated owner can create a versioned authenticated encrypted archive without passphrase retention", "Dry run has zero target writes and explicit restore is idempotent only for verified archive/clean target/nonce", "Restored state preserves canonical data and all effects remain disabled"]
  artifacts: [{path: site/domain/ports/workspace-archive.ts, provides: capability-approved narrow archive/object/delivery/target interfaces}, {path: site/domain/workspace-archive.ts, provides: canonical manifest and encrypted envelope service}, {path: site/domain/restore-verifier.ts, provides: read-only recovery verifier}, {path: site/domain/restore-apply.ts, provides: fenced idempotent restore service}]
  key_links: [{from: site/domain/workspace-archive.ts, to: approved archive port, via: versioned authenticated envelope and content hashes}, {from: site/domain/restore-verifier.ts, to: site/domain/restore-apply.ts, via: matching verified archive/target/nonce only}, {from: site/domain/restore-apply.ts, to: effect fence, via: default-disabled queues/schedules/outbox/provider state}]
---
<objective>Implement the capability-approved encrypted archive and two-stage recovery service. Purpose: make portable workspace recovery auditable, deterministic, and fail closed before any target release. Output: deep archive/restore modules backed by the accepted Plan 07-07 capability evidence.</objective>
<context>@.planning/phases/07-mining-pilot-handoff-and-recovery/07-CONTEXT.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-PATTERNS.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-VALIDATION.md
@.planning/phases/07-mining-pilot-handoff-and-recovery/07-07-PLAN.md</context>
<read_first>Read the accepted 07-07 summary before choosing imports. Use exactly its approved maintained crypto/KDF/envelope and hosted interface; if no summary exists because it is blocked, do not start this plan.</read_first>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Implement canonical archive and authenticated envelope boundary</name><files>site/domain/ports/workspace-archive.ts, site/domain/workspace-archive.ts, site/tests/workspace-recovery.test.mjs</files><behavior>- Canonical ordered records, content-addressed objects, immutable history/audit, manifests, configuration dependencies, export artifacts, and suppression/deletion tombstones are hashed and encrypted using only the accepted versioned envelope/KDF.
- Reauth/owner/workspace/delivery expiry are server-derived; passphrase, token, credentials, raw archive, and raw contacts are excluded from persistence/log/audit/browser boundaries.</behavior><action>Implement the narrow approved port and server service against repository enumeration, operation-memory passphrase input, test randomness seam, exact envelope metadata, content hash checks, immutable archive/delivery records, and bounded audit. Apply only the capability approved by 07-07; do not hand-roll crypto, downgrade compatibility, install unapproved packages, expose object keys as authority, or invoke an external provider beyond the injected test port.</action><verify><automated>cd site && node --test tests/workspace-recovery.test.mjs</automated></verify><done>Archive service produces/validates a versioned authenticated envelope and safe manifest evidence without retained passphrase or credentials.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement zero-write dry run and fenced idempotent restore</name><files>site/domain/restore-verifier.ts, site/domain/restore-apply.ts, site/tests/workspace-recovery.test.mjs</files><behavior>- Dry run authorizes/decrypts/verifies expiry, checksum/signature, compatibility, completeness, referential/current-projection invariants, and clean target without target writes/effects.
- Explicit restore requires current matching dry run/archive/target/nonce/owner, preserves IDs/history/hashes/tombstones, and begins all schedules/runners/outbox/provider/delivery state disabled.</behavior><action>Implement staged verifier result with indistinguishable untrusted credential/tamper failure, restore lock/nonce/idempotency, atomic apply/rollback behavior, reprojection/invariant validation, and minimized audit outcomes. Reject target non-clean, missing object, skew, authorization loss, stale nonce, and every partial failure before release. Never replay a runner, provider call, send, schedule, or delivery and never accept target identity from browser input.</action><verify><automated>cd site && node --test tests/workspace-recovery.test.mjs</automated></verify><done>Recovery applies only a verified archive to a clean compatible target and leaves all operational effects pause-fenced.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| T-07-22 archive cryptographic downgrade | mitigate | approved version/KDF only and reject tests |\n| T-07-23 restore write before verification | mitigate | staged zero-write verifier and counters |\n| T-07-24 replay reactivates effects | mitigate | explicit effect-fence initialization and fake-port counters |</threat_model>
<verification>Run `cd site && node --test tests/workspace-recovery.test.mjs`; do not treat this local proof as a hosted restore drill.</verification>
<acceptance_criteria>All archive/recovery decisions are implemented exactly against accepted capability evidence and remain fail closed outside the later drill.</acceptance_criteria>
<success_criteria>Portable archive/recovery code passes synthetic integrity, replay, and no-effect tests without hosted mutations.</success_criteria>
<output>Create 07-08-SUMMARY.md only when genuinely complete.</output>
