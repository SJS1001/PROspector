---
phase: 06-governed-outreach-and-suppression
plan: 12
type: tdd
wave: 9
depends_on: [06-10]
files_modified: [site/adapters/gmail.ts, site/worker/governed-outreach-worker.ts, site/domain/originated-mail-sync.ts, site/tests/gmail-boundary.test.mjs, site/tests/gmail-controlled-composition.test.mjs, site/tests/outreach-dispatch.test.mjs, site/tests/suppression.test.mjs]
autonomous: true
requirements: [REQ-immutable-outreach-approval, REQ-hard-company-suppression]
must_haves:
  truths: ["The owner-authorized Gmail adapter resolves protected material only in server-side composition and never persists or exposes it", "Only the fenced outbox worker can invoke MailPort for the exact authorized message", "Current suppression, approval, sender, connection, and lease failures produce zero Gmail request, no fallback, and no resend", "The server-side worker can recover only stored originated mail and routes authenticated minimized events idempotently into transactional stop rules without retry"]
  artifacts: [{path: site/adapters/gmail.ts, provides: bounded Gmail MailPort implementation and originated-only reconciliation/sync adapter}, {path: site/worker/governed-outreach-worker.ts, provides: server-side leased-outbox composition and scheduled work entrypoint}, {path: site/tests/gmail-controlled-composition.test.mjs, provides: fetch-stubbed protected-secret and fenced-worker contract coverage}]
  key_links: [{from: governed-outreach-worker.ts, to: outbox.ts, via: leased current-state dispatch service is the sole MailPort caller}, {from: outbox.ts, to: gmail.ts, via: injected MailPort after exact lease/suppression/approval recheck}, {from: governed-outreach-worker.ts, to: gmail.ts, via: sync entrypoint supplies only repository-loaded PROspector-originated identifiers}, {from: governed-outreach-worker.ts, to: originated-mail-sync.ts, via: authenticated minimized adapter events are persisted idempotently before transactional stop-rule cancellation}]
---
<objective>Implement the owner-authorized bounded Gmail composition and worker path. Purpose: turn the reject-only launch seam into the exact reviewed Gmail MailPort implementation without letting credentials, routes, browsers, or adapters bypass immutable approval, suppression, or the fenced outbox. Output: server-only protected-secret composition, a leased worker entrypoint, and fake-transport contract proof; no real Gmail request occurs during this plan.</objective>
<context>@.planning/phases/06-governed-outreach-and-suppression/06-CONTEXT.md
@.planning/phases/06-governed-outreach-and-suppression/06-PATTERNS.md
@.planning/phases/06-governed-outreach-and-suppression/06-VALIDATION.md
@.planning/phases/06-governed-outreach-and-suppression/06-10-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Replace the reject-only adapter with the exact bounded Gmail MailPort composition</name><files>site/adapters/gmail.ts, site/tests/gmail-boundary.test.mjs, site/tests/gmail-controlled-composition.test.mjs</files><behavior>- The adapter accepts only a committed minimized MailPort envelope and only after the outbox has verified the current lease generation, exact message approval/digest, sender/verified alias, connected pinned Google subject, granted scopes, working unsubscribe, compliance acknowledgement, and suppression/availability state.
- It resolves the approved protected-secret reference/version only in server-side hosted composition and sends it directly to the Gmail HTTPS request; no raw OAuth code, PKCE verifier, refresh/access token, bearer header, secret, or full provider payload reaches D1, browser state, logs, audit detail, exports, fixtures, or Git.
- Wrong redirect/state/PKCE, account swap, subject/alias/scope mismatch, missing/revoked/degraded connection, secret-reference failure, health uncertainty, or any untrusted/replayed sync result makes zero Gmail requests. There is no SDK/package install, fallback provider, automatic reconnect/account selection, or adapter-directed resend.</behavior><action>Use the owner-authorized 06-10 scope as a hard allowlist. Implement the Gmail REST MailPort only in site/adapters/gmail.ts with injected server-only fetch and protected-secret resolver seams that tests replace; retain stored connection metadata/reference/version only. Validate every dispatch and callback/reconciliation/sync input against active connection metadata, the exact stored PROspector-originated Message-ID/thread/marker set, and strict bounded result schemas. Return only normalized delivery/reconciliation/originated-event evidence to the domain; never import a mailbox or confer approval authority. Keep all test transports local/fake and do not perform OAuth authorization or a live request in this implementation plan.</action><verify><automated>cd site && node --test tests/gmail-boundary.test.mjs tests/gmail-controlled-composition.test.mjs</automated></verify><done>The Gmail adapter is a server-only, fail-closed MailPort implementation with no secret persistence/exposure and no direct provider authority.</done></task>
<task type="auto" tdd="true"><name>Task 2: Compose dispatch and originated-mail recovery only behind the governed worker</name><files>site/worker/governed-outreach-worker.ts, site/domain/originated-mail-sync.ts, site/tests/outreach-dispatch.test.mjs, site/tests/gmail-controlled-composition.test.mjs, site/tests/suppression.test.mjs</files><behavior>- The dispatch entrypoint receives only a scheduled/recovery candidate ID, asks the outbox service to acquire/recheck the short fenced lease, and injects MailPort only at its single post-recheck dispatch point; a route, browser, adapter, or arbitrary worker payload cannot call Gmail directly.
- Current suppression, pause/archive/high-risk drift, approval revocation/expiry, package/profile/prospect change, contact freshness, sender/connection/alias/scope mismatch, missing unsubscribe/basis/acknowledgement, or a stale fence causes cancellation/fail-closed state with zero Gmail fetches.
- The originated-mail sync/recovery entrypoint loads the exact stored PROspector-originated message/thread/marker identifiers for one active pinned connection, invokes the origin-restricted Gmail adapter sync once, validates and minimizes each returned event, and passes only accepted events into originated-mail-sync.ts.
- Same origin/event identity plus the same digest returns the original persisted result and stop effects; changed-payload reuse conflicts. Foreign connection/subject/origin events, unknown message/thread IDs, raw or oversized fields, and duplicate/replayed results create no event, audit, cancellation, or retry.
- Accepted reply/bounce events persist idempotently and invoke transactional outreach stop rules so matching Pending or unleased follow-ups cancel before success; ambiguous replies pause matching work. Known pre-transmission failure follows the controlled outbox state machine, while ambiguous acceptance records DeliveryUnknown and targeted originated-only recovery. No automatic sync retry, resend, alternate provider, or second invocation is possible.</behavior><action>Create two explicit server-side worker entrypoints. The dispatch entrypoint constructs Gmail MailPort from protected host services and delegates one candidate exclusively to the existing outbox lease/dispatch service. The sync/recovery entrypoint must first load active connection metadata plus the exact stored PROspector-originated Message-ID/thread/marker set from the trusted repository, call the Gmail adapter's origin-restricted sync exactly once with that allowlist, treat every adapter result as untrusted, enforce the pinned Google subject/connection and strict bounded event schema, discard all fields outside the minimized delivery/reply/bounce contract, and pass each accepted event into originated-mail-sync.ts. That domain service must use origin/event identity plus canonical digest for idempotent persistence and invoke outreach-stop-rules in the same transaction to cancel matching Pending/unleased follow-ups or pause ambiguous replies. Do not duplicate approval, suppression, send-state, or stop authority in the worker; add no browser/route send or mailbox-sync endpoint. With deterministic fake clock/fetch/MailPort, prove one dispatch call maximum, one sync call maximum per recovery attempt, zero calls/effects for blocked or foreign inputs, same-event replay returns the original state, changed replay conflicts, cancellation wins the pre-call race, and adapter/sync uncertainty never triggers an automatic retry, resend, fallback, or mailbox scan. Preserve the exact separately authorized greenfield target without altering it; never access or depend on the retired private Sites project, and add no secrets, deployments, or hosted changes.</action><verify><automated>cd site && node --test tests/outreach-dispatch.test.mjs tests/gmail-controlled-composition.test.mjs tests/suppression.test.mjs && npm run lint && npm run build</automated></verify><done>The governed worker is the sole dispatch and originated-mail recovery composition boundary: dispatch remains fenced, sync remains stored-origin-only, accepted events persist and cancel transactionally exactly once, and foreign, duplicate, failed, or uncertain events cause no retry or forbidden effect.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| credential or token disclosure | mitigate | server-only protected-reference resolver; source/fixture/audit exclusion tests |
| adapter/browser bypasses approval or suppression | mitigate | worker delegates solely to fenced outbox; zero-fetch negative tests |
| Gmail ambiguity causes duplicate delivery | mitigate | DeliveryUnknown, stored-origin reconciliation, no resend/fallback |
| account swap or broad mailbox ingestion | mitigate | pinned subject/alias/scopes and exact originated-ID allowlist |</threat_model>
<verification>Run the focused fake-transport suites plus lint/build. These tests prove composition only; they do not authorize a protected-secret binding, OAuth exchange, hosted deployment, or Gmail request.</verification>
<success_criteria>The exact Gmail adapter path is implemented and externally unreachable except through the fenced outbox worker, while all credentials and live effects remain absent pending 06-13.</success_criteria>
<output>Create `06-12-SUMMARY.md` only when local implementation and verification complete; it must state that no live Gmail request, credential, deployment, or hosted authority was exercised.</output>
