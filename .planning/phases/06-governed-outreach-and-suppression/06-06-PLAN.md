---
phase: 06-governed-outreach-and-suppression
plan: 06
type: tdd
wave: 5
depends_on: [06-03, 06-04, 06-05]
files_modified: [site/domain/outbox.ts, site/domain/originated-mail-sync.ts, site/domain/outreach-stop-rules.ts, site/tests/outreach-dispatch.test.mjs, site/tests/gmail-boundary.test.mjs, site/tests/suppression.test.mjs]
autonomous: true
requirements: [REQ-immutable-outreach-approval, REQ-hard-company-suppression]
must_haves:
  truths: ["Only atomic exact message approval creates one outbox item", "A fenced latest-state check precedes one MailPort call", "Ambiguous acceptance never auto-resends", "Authenticated originated replies and bounces transactionally stop matching pending or unleased follow-ups"]
  artifacts: [{path: site/domain/outbox.ts, provides: enqueue, lease, dispatch, reconciliation state machine}, {path: site/domain/originated-mail-sync.ts, provides: authenticated minimized originated-event persistence and stop-rule wiring}]
  key_links: [{from: outbox.ts, to: suppression.ts, via: enqueue and pre-call current rechecks}, {from: outbox.ts, to: ports/mail.ts, via: post-commit fenced dispatch only}, {from: originated-mail-sync.ts, to: outreach-stop-rules.ts, via: idempotent persisted reply/bounce event invokes transactional cancellation}]
---
<objective>Implement the transactional outbox and fenced Gmail dispatch state machine. Purpose: ensure suppression and current authority win every race before external contact. Output: fake-port-tested queue/lease/reconciliation service.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Atomically enqueue one current approved message</name><files>site/domain/outbox.ts, site/tests/outreach-dispatch.test.mjs</files><behavior>- One transaction validates unused/unexpired exact message approval, digest, admitted owner, all suppression subjects, Package/Profile/Prospect state, drift, sender connection/identity, unsubscribe, basis and acknowledgement before consuming approval and creating one Pending item.
- Same idempotency key/digest returns original; mismatched reuse conflicts and denied tuples create no item/call.</behavior><action>Use repository-backed canonical operation digest and unique send key. Per locked Gmail decision, create deterministic RFC Message-ID and bounded marker solely for reconciliation. Keep approval consumption/outbox/audit atomic and prohibit direct route/browser/adapter enqueue.</action><verify><automated>cd site && node --test tests/outreach-dispatch.test.mjs</automated></verify><done>Exactly one authorized message becomes queued and every stale/missing predicate fails closed.</done></task>
<task type="auto" tdd="true"><name>Task 2: Claim, recheck, dispatch, and reconcile with fences</name><files>site/domain/outbox.ts, site/tests/outreach-dispatch.test.mjs</files><behavior>- Lease has short exclusive monotonic generation; a stale holder cannot invoke/finalize and cancellation races win at the bounded pre-call point.
- Pre-transmission failures may use the same controlled authority; ambiguous acceptance yields durable DeliveryUnknown, marker-only reconciliation, owner action, and no automatic resend.</behavior><action>Implement Pending→Leased→Dispatching→Sent/Cancelled/FailedBeforeDispatch/DeliveryUnknown transitions with exact lease generation guards. Immediately before one injected MailPort call re-read every enqueue predicate and stop state. Record minimized provider evidence only; prohibit alternate provider, browser retry, automatic resend, and any call after post-approval suppression/pause/archive/drift/revocation.</action><verify><automated>cd site && node --test tests/outreach-dispatch.test.mjs tests/gmail-boundary.test.mjs</automated></verify><done>Dispatch is fenced, current, reconcilable, and honest about unknown delivery.</done></task>
<task type="auto" tdd="true"><name>Task 3: Persist authenticated originated-mail events and invoke stop rules</name><files>site/domain/originated-mail-sync.ts, site/domain/outreach-stop-rules.ts, site/tests/gmail-boundary.test.mjs, site/tests/suppression.test.mjs, site/tests/outreach-dispatch.test.mjs</files><behavior>- Only an active, pinned Gmail connection and stored PROspector-originated message/thread identifiers can contribute a reply or bounce event; arbitrary mailbox identifiers, swapped subjects, inactive connections, unrecognized origins, and oversized/raw fields are denied without persistence or stop action.
- An accepted reply/bounce is minimized, persisted exactly once by its connection/origin/event identity, then invokes outreach-stop-rules in the same transaction to cancel matching Pending or unleased follow-ups; an ambiguous reply pauses matching work for owner review.
- Deterministic races prove a reply/bounce that reaches the bounded pre-call decision point before provider invocation cancels the lease candidate with zero MailPort calls; duplicate sync cannot duplicate cancellation/audit effects.</behavior><action>Implement the adapter-to-domain sync seam using the MailPort origin-bound contract from 06-05: authenticate against current connection metadata, pinned Google subject, and the stored PROspector-originated ID set before accepting a strict allowlist of delivery classification, bounded bounce metadata, reply time/sender, bounded subject, and minimized escaped excerpt. Persist an idempotent digested event and bounded audit record, then call the 06-04 transactional stop service against the current subject/message/thread projection. Never fetch or import a mailbox, accept provider text as approval authority, expose raw content, create a resend/fallback, or cancel by scanning untrusted data.</action><verify><automated>cd site && node --test tests/gmail-boundary.test.mjs tests/suppression.test.mjs tests/outreach-dispatch.test.mjs</automated></verify><done>Originated-only reply/bounce synchronization is authenticated, minimized, idempotent, and transactionally wired to cancellation before an eligible pre-call lease can dispatch.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| duplicate/racy dispatch | mitigate | atomic consumption, unique key, lease fence |
| post-approval state change | mitigate | latest-state pre-call recheck |
| ambiguous external acceptance | mitigate | DeliveryUnknown and manual reconciliation only |</threat_model>
<verification>Run dispatch plus Gmail boundary suites.</verification><success_criteria>A worker is the sole caller and it cannot bypass current suppression or immutable approval.</success_criteria><output>Create `06-06-SUMMARY.md` only when complete.</output>
