---
phase: 06-governed-outreach-and-suppression
plan: 10
type: execute
wave: 8
depends_on: [06-09, 06-11]
files_modified: []
autonomous: false
requirements: [REQ-immutable-outreach-approval, REQ-hard-company-suppression]
user_setup:
  - service: Google Cloud/Gmail controlled test account
    why: Owner must authorize exact OAuth scopes, test mailbox/alias, and protected-secret handling before any binding or request.
  - service: existing private Sites project
    why: Hosted proof must use the existing private project and exact reviewed source; no replacement project is permitted.
must_haves:
  truths: ["No Gmail composition or hosted activation occurs without exact owner authorization", "Independent pre-composition review accepts no unresolved blocker/high finding"]
  artifacts: []
  key_links: [{from: accepted owner authorization, to: 06-12 Gmail composition, via: exact bounded worker-only MailPort scope}]
---
<objective>Hold non-substitutable pre-composition review and owner-authorization gates. Purpose: keep local fake-port proof from being confused with credentials, Gmail composition, mail delivery, phone effect, or deployment authorization. Output: accepted sanitized authorization/review references or an honest blocked plan.</objective>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human"><name>Task 1: Accept independent pre-composition security/privacy review</name><files>None — human checkpoint; no repository files change.</files><what-built>Local immutable artifacts, suppression, reject-only Gmail boundary, outbox, originated-event stop wiring, manual-call, transport, UI, integration, lint, and build proof use no real provider.</what-built><action>Obtain independent review of the exact local candidate revision for owner admission/CSRF, immutable digest/approval separation, all suppression scopes/tombstone retention, unsubscribe enumeration, Gmail boundary and secret minimization design, sender/alias/scope/subject controls, lease and sync races, unknown delivery/no resend, UI safe rendering, and manual-call outcome ordering. Any blocker/high finding halts this plan; local proof cannot substitute for review or authorize a provider request.</action><how-to-verify>Record only a sanitized revision and review reference; confirm zero unresolved blocker/high finding and no secret, real contact, Gmail request, dialer/call, external mail, public Site, or Sites mutation occurred.</how-to-verify><verify><human-check>Independent review accepted with no blocker/high finding.</human-check></verify><done>Pre-composition review is accepted or the plan remains blocked without summary.</done><resume-signal>Provide sanitized review reference, findings, or `blocked`.</resume-signal></task>
<task type="checkpoint:human-verify" gate="blocking-human"><name>Task 2: Obtain exact owner authorization for bounded Gmail composition</name><files>None — owner authorization checkpoint; no repository files change.</files><what-built>The adapter remains reject-only and all tests use a fake MailPort; no OAuth binding, protected-secret resolution, Gmail request, or delivery exists.</what-built><action>Obtain separate owner authorization that names the existing private Sites project, controlled non-production Google account and verified alias, exact minimum Gmail scopes, protected-secret handling location/reference policy, synthetic or owner-controlled recipient, single-message/outbox bound, permitted reconciliation scope, expiry, and the prohibition on fallback, mailbox import, automatic resend, real-contact use, public exposure, and unapproved deployments. The authorization must explicitly permit only the planned 06-12 composition path, which calls MailPort solely from the fenced outbox worker after every current suppression/approval/lease predicate; it is not permission to run a Gmail request yet.</action><how-to-verify>Record a sanitized authorization reference that binds all stated scope fields and the Phase 4/5 predecessor acceptance reference. Confirm it contains no secret value and has not approved account swapping, broad mailbox access, arbitrary recipients, a provider fallback, a resend, or a different Sites project.</how-to-verify><verify><human-check>Owner authorizes the exact bounded composition scope, or Gmail remains disabled.</human-check></verify><done>Only a current, exact, sanitized owner authorization can unblock 06-12; otherwise this plan remains blocked without summary.</done><resume-signal>Provide sanitized authorization reference, scope mismatch, or `blocked`.</resume-signal></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| local proof mistaken for provider authority | mitigate | blocking review and exact owner-authorization gates |
| hosted/project workaround | mitigate | existing private Site only checkpoint |
| secret/private-data disclosure | mitigate | sanitized references; no raw values |</threat_model>
<verification>Human checkpoints only; fixtures, tests, prose, or status rows do not complete them.</verification><success_criteria>Gmail composition and hosted authority remain fail-closed without exact owner evidence.</success_criteria><output>Do not create a summary when either checkpoint is blocked. If both complete, create `06-10-SUMMARY.md` with sanitized evidence references only.</output>
