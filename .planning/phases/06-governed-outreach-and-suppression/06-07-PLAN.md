---
phase: 06-governed-outreach-and-suppression
plan: 07
type: tdd
wave: 5
depends_on: [06-03, 06-04]
files_modified: [site/domain/manual-call.ts, site/tests/manual-call.test.mjs]
autonomous: true
requirements: [REQ-hard-company-suppression, REQ-immutable-outreach-approval]
must_haves:
  truths: ["Only a currently fresh verified business number may receive a server-authorized manual call link", "do_not_call writes suppression before activity success"]
  artifacts: [{path: site/domain/manual-call.ts, provides: call eligibility, derived script, and outcome authority}]
  key_links: [{from: manual-call.ts, to: suppression.ts, via: activation and do_not_call transaction}, {from: manual-call.ts, to: outreach-package.ts, via: exact derived-script digest}]
---
<objective>Implement verified-number manual-call control. Purpose: support a human calling workflow without a dialer, unsafe static link, arbitrary script, or implied completion. Output: server-authorized projection and outcome command.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Project and activate an authorized verified phone action</name><files>site/domain/manual-call.ts, site/tests/manual-call.test.mjs</files><behavior>- Only current Phase 5 fresh source/provider-verified E.164 business phone plus active approved Package/current suppression/availability can render and activate a tel target.
- Script exactly derives from approved Package digest and claim/evidence dependencies; stale/blocked current recheck removes action.</behavior><action>Implement server-side eligibility projection and activation command with admitted owner, expected revision/idempotency, authoritative subject resolver, current freshness, availability, package dependency and advisory compliance checks. Keep the returned href constrained to an authorized current activation; do not create a dialer, recording, SMS, automatic outcome, or editable arbitrary script.</action><verify><automated>cd site && node --test tests/manual-call.test.mjs</automated></verify><done>Stale UI cannot keep an active phone effect and every visible script has immutable provenance.</done></task>
<task type="auto" tdd="true"><name>Task 2: Record bounded manual outcomes with suppression precedence</name><files>site/domain/manual-call.ts, site/tests/manual-call.test.mjs</files><behavior>- Only connected, voicemail, no_answer, wrong_number, do_not_call, and follow_up with bounded reasoned notes are accepted.
- do_not_call atomically suppresses before activity completion; reply/bounce/suppression/pause/drift blocks follow_up.</behavior><action>Implement admitted outcome command and immutable minimal activity/audit writer. Validate exact package/script reference and all current stop predicates. For do_not_call call the suppression transaction first in the same atomic operation; do not claim a call occurred merely by link activation or create a message/follow-up without the normal new-version approval path.</action><verify><automated>cd site && node --test tests/manual-call.test.mjs tests/suppression.test.mjs</automated></verify><done>Manual records are bounded, auditable, and cannot outrun a prohibition.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| static/stale phone dialing | mitigate | server activation recheck |
| invented call script/outcome | mitigate | package digest binding and allowlist |
| do-not-call race | mitigate | suppression-before-activity transaction |</threat_model>
<verification>Run manual-call and suppression suites.</verification><success_criteria>Manual calling remains human-operated but technically fail-closed.</success_criteria><output>Create `06-07-SUMMARY.md` only when complete.</output>
