---
phase: 06-governed-outreach-and-suppression
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [site/drizzle/0009_governed_outreach.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0009_snapshot.json, site/domain/outreach-repository.ts, site/tests/outreach-artifacts.test.mjs]
autonomous: true
requirements: [REQ-immutable-outreach-approval, REQ-hard-company-suppression]
must_haves:
  truths: ["D1 enforces immutable artifacts, one-time approval consumption, leases, and append-only suppressions"]
  artifacts: [{path: site/drizzle/0009_governed_outreach.sql, provides: additive Phase 6 schema}, {path: site/domain/outreach-repository.ts, provides: workspace-scoped transaction seam}]
  key_links: [{from: outreach-repository.ts, to: 0009_governed_outreach.sql, via: FK and unique constraint-backed transactions}]
---
<objective>Build the durable Phase 6 authority foundation. Purpose: force immutable approval, suppression, outbox, and audit invariants below browser and adapter code. Output: additive migration and repository.</objective>
<context>@.planning/phases/06-governed-outreach-and-suppression/06-PATTERNS.md
@.planning/phases/05-controlled-enrichment-and-verified-contacts/05-02-PLAN.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add the additive governed-outreach schema</name><files>site/drizzle/0009_governed_outreach.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0009_snapshot.json, site/tests/outreach-artifacts.test.mjs</files><behavior>- Immutable package/message/approval/dependency/connection metadata/outbox/tombstone/activity records retain authority lineage.
- Unique constraints prevent duplicate approval consumption/send keys and stale lease finalization; tombstones are never deleted by lifecycle operations.</behavior><action>Following the accepted D1 migration chain, add append-only version, approval, dependency, normalized-subject, alias snapshot, outbox/fence, unsubscribe-token-digest, and minimal audit tables with constrained enums/FKs/partial unique indexes. Per locked suppression decision, store no OAuth code/token/PKCE/bearer value, full message/reply body, or secret; preserve existing data and Phase 5 suppression references.</action><verify><automated>cd site && node --test tests/outreach-artifacts.test.mjs</automated></verify><done>Migration is additive and durable constraints make invalid state unrepresentable.</done></task>
<task type="auto" tdd="true"><name>Task 2: Create the admitted repository transaction seam</name><files>site/domain/outreach-repository.ts, site/tests/outreach-artifacts.test.mjs</files><behavior>- Every mutation derives workspace/owner from server context and implements expected-revision plus same-key/same-digest idempotency.
- Repository transactions atomically write authority row, dependencies, audit, and projection; no client-provided workspace or provider response is accepted.</behavior><action>Implement typed workspace-scoped reads and D1 batches needed by later artifact, suppression, outbox, and stop services. Expose current predecessor/Phase 5 eligibility joins, authoritative subject resolution, and fenced update primitives; retain only bounded/digested audit fields.</action><verify><automated>cd site && node --test tests/outreach-artifacts.test.mjs</automated></verify><done>Later plans have one persistence seam whose constraints arbitrate concurrent authority commands.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| duplicate send authority | mitigate | unique approval/send-key constraints |
| secret disclosure | mitigate | secret-reference-only schema and test scan |
| cross-workspace mutation | mitigate | admitted scoped repository joins |</threat_model>
<verification>Run the focused artifact suite and inspect the migration as additive.</verification><success_criteria>Storage represents all locked Phase 6 evidence without provider authority.</success_criteria><output>Create `06-02-SUMMARY.md` only when complete.</output>
