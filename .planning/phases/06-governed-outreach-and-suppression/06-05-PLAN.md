---
phase: 06-governed-outreach-and-suppression
plan: 05
type: tdd
wave: 4
depends_on: [06-02]
files_modified: [site/domain/ports/mail.ts, site/adapters/gmail.ts, site/tests/gmail-boundary.test.mjs]
autonomous: true
requirements: [REQ-immutable-outreach-approval]
must_haves:
  truths: ["Domain depends only on a narrow MailPort", "Gmail credential and identity uncertainty blocks sending with no provider call"]
  artifacts: [{path: site/domain/ports/mail.ts, provides: provider-neutral mail port}, {path: site/adapters/gmail.ts, provides: reject-only Gmail launch adapter contract}]
  key_links: [{from: gmail.ts, to: mail.ts, via: adapter implementation only}]
---
<objective>Define the MailPort and Gmail authorization boundary without enabling Gmail. Purpose: isolate OAuth/API particulars, identities, and secret references from domain and UI authority. Output: injected fake-port seam and reject-only Gmail adapter.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Define narrow mail dispatch, reconciliation, and originated-sync contracts</name><files>site/domain/ports/mail.ts, site/tests/gmail-boundary.test.mjs</files><behavior>- Port accepts a committed minimized dispatch envelope and returns classified bounded evidence, never approval authority.
- Reconciliation/sync are constrained to stored PROspector-originated IDs and minimized delivery/bounce/reply fields.</behavior><action>Export TypeScript interfaces/types for dispatch, marker-only reconciliation, and origin-bound sync. Include no provider SDK, browser credential/state, raw message/reply content, or secret-bearing type. Test fake call counters and reject untrusted provider-result fields.</action><verify><automated>cd site && node --test tests/gmail-boundary.test.mjs</automated></verify><done>Domain services can be tested against a fake provider-neutral port only.</done></task>
<task type="auto" tdd="true"><name>Task 2: Build a fail-closed Gmail metadata/OAuth adapter boundary</name><files>site/adapters/gmail.ts, site/tests/gmail-boundary.test.mjs</files><behavior>- Missing/revoked/degraded connection, wrong redirect/PKCE/state replay, account swap, subject mismatch, partial scope, unverified From/alias, missing protected-secret reference, or health uncertainty yields zero calls.
- Connection storage/projection contains only bounded metadata and protected reference/version; Gmail is not activated.</behavior><action>Implement the launch-specific adapter as unconfigured/reject-only composition, validated connection metadata projection, sender/alias/scope/subject checks, and explicit deterministic marker construction. Do not install an SDK or make OAuth/network/Gmail requests; no fallback provider, reconnect mailbox selection, raw code/token, or credential persistence/logging is allowed.</action><verify><automated>cd site && node --test tests/gmail-boundary.test.mjs</automated></verify><done>All OAuth/Gmail negative cases fail closed while a future authorized adapter has a constrained seam.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| OAuth replay/account substitution | mitigate | opaque state/PKCE validation contract and subject pinning |
| secret exposure | mitigate | protected-reference-only metadata and source scans |
| provider authority leak | mitigate | reject-only adapter and narrow port |</threat_model>
<verification>Run `cd site && node --test tests/gmail-boundary.test.mjs` and scan imports for Gmail SDK usage outside adapter.</verification><success_criteria>No production path can connect, sync, or send Gmail without later owner/hosted authority.</success_criteria><output>Create `06-05-SUMMARY.md` only when complete.</output>
