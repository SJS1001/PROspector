---
phase: 06-governed-outreach-and-suppression
plan: 03
type: tdd
wave: 3
depends_on: [06-02]
files_modified: [site/domain/outreach-package.ts, site/domain/message-version.ts, site/tests/outreach-artifacts.test.mjs]
autonomous: true
requirements: [REQ-immutable-outreach-approval]
must_haves:
  truths: ["Package approval is immutable CRM-only authority", "Each message approval binds the exact deliverable"]
  artifacts: [{path: site/domain/outreach-package.ts, provides: package versions and approvals}, {path: site/domain/message-version.ts, provides: message versions and approvals}]
  key_links: [{from: message-version.ts, to: outreach-package.ts, via: exact package/configuration dependency}]
---
<objective>Implement canonical Outreach Package and Message Version authority. Purpose: preserve the locked separation between CRM eligibility and controlled delivery. Output: pure/deep immutable artifact services.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Create and approve immutable Outreach Packages</name><files>site/domain/outreach-package.ts, site/tests/outreach-artifacts.test.mjs</files><behavior>- Package binds exact approved Prospect/Contact/Profile configuration, evidence/source hashes, angle, claim guardrails, selected fresh points, derived script, and complete message set to a SHA-256 digest.
- Approval expires at configured pilot policy and invalidates for every locked dependency/revocation/freshness change while exposing CRM eligibility only.</behavior><action>Implement strict allowlisted canonicalization, dependency edges, expected revision/idempotency, admitted owner approval, expiry, invalidation and bounded audit through the repository. Per locked decision, reject weak/stale contacts and do not enqueue/send/approve a message.</action><verify><automated>cd site && node --test tests/outreach-artifacts.test.mjs</automated></verify><done>Only an exact currently eligible package can receive audited CRM-only approval.</done></task>
<task type="auto" tdd="true"><name>Task 2: Create and approve immutable Gmail Message Versions</name><files>site/domain/message-version.ts, site/tests/outreach-artifacts.test.mjs</files><behavior>- Canonical message includes sender/From/Reply-To, normalized recipient sets, bodies, links, attachments, thread IDs, schedule/timezone, package/configuration IDs, and acknowledgement/basis.
- Any canonical edit/reschedule gets a new digest and cannot reuse approval.</behavior><action>Implement strict normalization and immutable version creation plus independently expiring owner approval. Bind every approval to the reviewed digest/owner/package/configuration and enforce compliance acknowledgement; do not import Gmail SDK types, provider output, tokens, or HTML execution.</action><verify><automated>cd site && node --test tests/outreach-artifacts.test.mjs</automated></verify><done>Message approval is exact, independent, and insufficient to invoke an adapter by itself.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| edit after approval | mitigate | version-only canonical digest |
| package-as-send authority | mitigate | distinct approval service/state |
| unsafe draft rendering input | mitigate | typed data only; rendering later escapes it |</threat_model>
<verification>Run `cd site && node --test tests/outreach-artifacts.test.mjs`.</verification><success_criteria>Immutable artifacts cover every locked field and authority scope.</success_criteria><output>Create `06-03-SUMMARY.md` only when complete.</output>
