---
phase: 06-governed-outreach-and-suppression
plan: 04
type: tdd
wave: 4
depends_on: [06-02]
files_modified: [site/domain/suppression.ts, site/domain/outreach-stop-rules.ts, site/tests/suppression.test.mjs]
autonomous: true
requirements: [REQ-hard-company-suppression, REQ-immutable-outreach-approval]
must_haves:
  truths: ["All Company-wide suppression subjects prohibit outreach transactionally", "Stop events cancel applicable current follow-ups"]
  artifacts: [{path: site/domain/suppression.ts, provides: tombstone writer and subject resolver}, {path: site/domain/outreach-stop-rules.ts, provides: reply/bounce/pause/drift stops}]
  key_links: [{from: suppression.ts, to: outreach-repository.ts, via: current subject-resolution transaction}, {from: outreach-stop-rules.ts, to: pending outbox rows, via: cancellation projection}]
---
<objective>Implement immutable Company-wide suppression and stop logic. Purpose: make a prohibition win at every package, send, and call boundary. Output: normalized resolver, append-only tombstones, and cancellation service.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Resolve and write all normalized suppression scopes</name><files>site/domain/suppression.ts, site/tests/suppression.test.mjs</files><behavior>- Exact email, owner-confirmed domain equivalence, E.164 phone, Contact, Organization, and all-Company scopes block matching channels.
- Merge/split/delete/import/export/restore retain historical aliases and union suppression reach; provider folklore never changes email equivalence.</behavior><action>Implement a single normalization/resolution library and append-only tombstone command with admitted owner, expected revision, idempotency, actor/source/reason/effective time/aliases/audit lineage. Per locked decision, resolve all current subjects transactionally and reject any temporary exception or suppression deletion.</action><verify><automated>cd site && node --test tests/suppression.test.mjs</automated></verify><done>Every required subject scope persists and blocks matching action-time projections.</done></task>
<task type="auto" tdd="true"><name>Task 2: Apply ordered unsubscribe, opt-out, reply, bounce, and lifecycle stops</name><files>site/domain/outreach-stop-rules.ts, site/tests/suppression.test.mjs</files><behavior>- Public opaque token handling is generic/rate-limited/replay-safe and writes the tombstone before reporting success.
- Explicit opt-out and do_not_call suppress before activity success; reply/bounce/suppression/pause/archive/high-risk drift cancel applicable pending or unleased work and ambiguous reply pauses review.</behavior><action>Implement token-digest lookup with no read disclosure, bounded inbound metadata ingestion restricted to originated IDs, and transactional stop/cancel hooks. Do not import a mailbox, expose contact data, auto-resend, or permit a follow-up without a new message version/approval.</action><verify><automated>cd site && node --test tests/suppression.test.mjs</automated></verify><done>Every stop source becomes durable before success and defeats applicable follow-up work.</done></task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |
|---|---|---|
| identity/alias suppression bypass | mitigate | shared resolver and lineage tests |
| unsubscribe enumeration | mitigate | token digest, generic response, rate limit |
| follow-up after opt-out | mitigate | transactional stop/cancel hook |</threat_model>
<verification>Run `cd site && node --test tests/suppression.test.mjs`.</verification><success_criteria>Current cached eligibility cannot override a durable Company-wide prohibition.</success_criteria><output>Create `06-04-SUMMARY.md` only when complete.</output>
