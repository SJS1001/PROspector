---
phase: 03-product-readiness-and-market-discovery
plan: 03
type: tdd
wave: 3
depends_on: [03-02]
files_modified:
  - site/domain/product-readiness.ts
  - site/tests/product-readiness-repository.test.mjs
autonomous: true
requirements: [REQ-product-readiness]
must_haves:
  truths:
    - "Only complete exact Confirmed Knowledge can make a Product Ready."
    - "Ready atomically creates one immutable configuration, initial run, schedule, and audit record."
  artifacts:
    - path: site/domain/product-readiness.ts
      provides: pure readiness evaluator and revision-guarded Ready command
  key_links:
    - from: site/domain/product-readiness.ts
      to: typed_configurations
      via: canonical immutable manifest digest
      pattern: "manifestDigest"
---

<objective>
Implement the Product Ready authority command against the Wave 0 contract.

Purpose: Turn complete confirmed Product policy into one replayable configuration and blocked discovery intent without authorizing operational work.
Output: Pure evaluator plus atomic, owner-scoped D1 command.
</objective>

<context>
@.planning/phases/03-product-readiness-and-market-discovery/03-CONTEXT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-PATTERNS.md
@site/domain/interview.ts
@site/tests/product-readiness-repository.test.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement pure confirmed-knowledge Product readiness evaluation</name>
  <files>site/domain/product-readiness.ts, site/tests/product-readiness-repository.test.mjs</files>
  <behavior>
    - A Draft Product reports every missing one of nine required categories.
    - Proposed, stale, fixture, or client-provided values do not satisfy a category.
    - A complete Product is valid with zero Plays, Profiles, and Offers.
  </behavior>
  <action>Make the existing RED evaluator cases pass by exporting a pure `evaluateProductReadiness` that accepts only server-loaded Product and Confirmed Knowledge authority and returns every missing/confirmed category, exact version IDs/digests, and no side effects. Implement D-01 and D-03 by consuming the existing Consensus Interview's confirmed Product knowledge categories—capability, limitation, delivery, proof, ownership, claim guardrail, source policy, discovery policy, and default-runner policy—rather than seed values or invented Product facts. Do not accept current UI state, fixture text, generic latest-value lookup, Proposed Knowledge, descendant placeholders, or downstream state as evidence.</action>
  <verify><automated>cd site && node --test tests/product-readiness-repository.test.mjs --test-name-pattern="checklist|Proposed|zero Play" && npm test</automated></verify>
  <done>The evaluator deterministically returns the complete authoritative checklist and supports zero-descendant readiness.</done>
</task>
<task type="auto">
  <name>Task 2: Implement atomic revision-guarded Ready activation and winner replay</name>
  <files>site/domain/product-readiness.ts, site/tests/product-readiness-repository.test.mjs</files>
  <behavior>
    - Matching expected revision and exact sorted prerequisite set create exactly one configuration/run/schedule/audit result.
    - Retry returns the same authoritative result; competing commands converge on one winner.
    - Paused, archived, stale, missing-gate, failed configuration, and authority-unknown starts fail closed.
  </behavior>
  <action>Implement D-02/D-04 following the Phase 2 admitted-principal, canonical JSON/SHA-256, D1 command-guard, batch, and idempotent winner-read pattern. Resolve workspace from the admitted principal, load the Product under that root, compare exact expected revision and sorted submitted Confirmed Version ID/digest set, canonicalize the immutable Product Discovery Configuration, then in one guarded D1 batch transition Draft to Ready, append audit, insert/reuse configuration, initial run with the required deterministic key, manual-control availability, and monthly Product schedule. Bind the operation digest to Product/revision/prerequisites/configuration/trigger/schedule/key. Add the authoritative Phase 2 replacement-activation consumer: accept only the confirmed, admitted owner replacement-activation event after lifecycle and configuration validity rechecks; append immutable predecessor/successor configuration lineage and create/reuse one `material-change:product:{product_id}:{replacement_configuration_id}` run/trigger with the replacement snapshot. Use a unique operation digest and winner read so retries, reordered delivery, and concurrent activation cannot create more than one material-change trigger. Reject proposed/unconfirmed replacement or paused/archived/missing/failed configuration states. Mark exhausted initial work Needs attention without rollback or duplicate creation, and leave all downstream operational rows unchanged.</action>
  <verify><automated>cd site && node --test tests/product-readiness-repository.test.mjs && npm test && npm run lint</automated></verify>
  <done>All readiness repository contracts pass with one immutable result per logical operation and zero Phase 4-7 effects.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client → Ready command | Revision/prerequisites/key are hostile locators, never authority. |
| Product Ready → discovery intent | Readiness must not grant external execution or downstream lifecycle authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05 | Tampering | Ready command | mitigate | Exact confirmed set, canonical digest, guarded batch, unique winner, audit. |
| T-03-06 | Elevation | Ready result | mitigate | Explicitly exclude profile/prospecting/contact/spend/export/outbound writes and test zero delta. |
| T-03-07 | Denial | initial run | mitigate | Needs-attention state is visible and cannot silently retry/duplicate. |
</threat_model>
<verification>Complete RED→GREEN→REFACTOR with the full product-readiness repository suite.</verification>
<success_criteria>The owner can obtain exactly one immutable, replayable Ready configuration only from complete confirmed Product policy and no downstream effect is possible.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-03-SUMMARY.md` when done.</output>
