---
phase: 03-product-readiness-and-market-discovery
plan: 07
type: execute
wave: 6
depends_on: [03-06]
files_modified:
  - site/tests/product-readiness-repository.test.mjs
  - site/tests/market-discovery-repository.test.mjs
  - site/tests/discovery-handler-ui.test.mjs
  - site/package.json
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths: ["The complete local Phase 3 authority boundary is regression-tested before any release work."]
  artifacts:
    - path: site/package.json
      provides: focused Phase 3 test command using existing tooling
  key_links:
    - from: site/package.json
      to: site/tests
      via: test script
      pattern: "test:phase3"
---
<objective>Finalize local Phase 3 regression coverage and a no-effect release preflight command.

Purpose: Prove durable readiness/discovery behavior locally before the deliberately separate hosted gate.
Output: Green focused suite and existing-tooling test script.</objective>
<context>@.planning/phases/03-product-readiness-and-market-discovery/03-VALIDATION.md
@.planning/phases/03-product-readiness-and-market-discovery/03-01-PLAN.md</context>
<tasks>
<task type="auto"><name>Task 1: Close every Wave 0 contract gap</name><files>site/tests/product-readiness-repository.test.mjs, site/tests/market-discovery-repository.test.mjs, site/tests/discovery-handler-ui.test.mjs</files><action>Run the complete RED suites against the implemented boundary and strengthen only gaps in atomicity, Phase 2 confirmed replacement-activation/material-change trigger lineage, revision/idempotency, bounded cap, fixed synthetic-proof provenance/gate/audit, cooldown/material reopen, neutral denial, authority-unknown UI, third primary Market Discovery navigation, retained Knowledge/Pilot access, explicit last-selected Product picker, Draft-only Explore, and zero-delta coverage. Keep tests deterministic, full-chain D1, fixed-clock, and under the local feedback target. Do not weaken assertions or replace forbidden-table snapshots with selective checks.</action><verify><automated>cd site && node --test tests/product-readiness-repository.test.mjs tests/market-discovery-repository.test.mjs tests/discovery-handler-ui.test.mjs && npm test</automated></verify><done>All Phase 3 local authority, race, UI, hosted-proof-fixture, and downstream-zero-effect contracts pass.</done></task>
<task type="auto"><name>Task 2: Add existing-tooling Phase 3 preflight command</name><files>site/package.json</files><action>Add only a script composed from already-installed project tools that runs the three focused Phase 3 suites and build/type checks. Do not add, install, upgrade, or dynamically download packages. The command must fail on any local authority or zero-effect regression and must make no hosted call.</action><verify><automated>cd site && npm run test:phase3 && npm test && npm run lint</automated></verify><done>A one-command, local-only preflight is green and has no dependency or network side effect.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| local test claim → release decision | Passing tests must include downstream non-effect evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-03-17 | Repudiation | release evidence | mitigate | Deterministic focused command retains full authority and zero-delta checks. |
| T-03-SC | Tampering | package scripts | mitigate | Existing tooling only; no install, package change beyond script declaration. |
</threat_model>
<verification>Run `cd site && npm run test:phase3` and `cd site && npm run build`.</verification>
<success_criteria>Local success proves readiness/proposal authority and explicitly proves the absence of downstream effects.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-07-SUMMARY.md` when done.</output>
