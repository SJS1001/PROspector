---
phase: 03-product-readiness-and-market-discovery
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - site/app/discovery/discovery-workspace.tsx
  - site/app/discovery/product-readiness.tsx
  - site/app/discovery/proposal-cards.tsx
  - site/app/prospector-app.tsx
  - site/app/globals.css
  - site/tests/discovery-handler-ui.test.mjs
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths:
    - "The owner sees every readiness gap and immutable discovery state from the server projection."
    - "At most three evidence-backed suggestions can be reviewed with fail-closed action controls."
  artifacts:
    - path: site/app/discovery/discovery-workspace.tsx
      provides: authoritative discovery projection client
    - path: site/app/discovery/proposal-cards.tsx
      provides: bounded Draft-only review surfaces
  key_links:
    - from: site/app/discovery/discovery-workspace.tsx
      to: /api/discovery
      via: no-store same-origin projection replacement
      pattern: "api/discovery"
---
<objective>
Build the fail-closed Product Discovery workspace from the accepted UI contract.

Purpose: Make immutable readiness and proposal authority legible without letting stale UI infer operational permission.
Output: Authoritative read model, readiness view, proposal cards, shell integration, and accessibility styling.
</objective>
<context>@.planning/phases/03-product-readiness-and-market-discovery/03-UI-SPEC.md
@.planning/phases/03-product-readiness-and-market-discovery/03-PATTERNS.md
@site/app/prospector-app.tsx</context>
<tasks>
<task type="auto">
  <name>Task 1: Implement authoritative readiness/discovery workspace</name>
  <files>site/app/discovery/discovery-workspace.tsx, site/app/discovery/product-readiness.tsx, site/app/prospector-app.tsx, site/tests/discovery-handler-ui.test.mjs</files>
  <action>Implement D-01 through D-05 and UI-SPEC readiness/run states. Fetch `/api/discovery` using same-origin credentials/no-store and replace local projection only with authoritative response. Add `Market Discovery` as the third primary navigation destination; retain Knowledge and Pilot access, and do not derive the selected Product from a Play or Profile route. Render an explicit server-projected Product picker, persist the last selected Product locally per admitted owner/workspace, revalidate it against the next server projection, and fall back to an explicit picker state when it is unavailable. Show all nine server checklist rows with confirmed IDs/digests, Product revision, explicit zero-descendant explanation, immutable configuration/run/schedule metadata, manual control only for effectively available Ready Product, and Needs attention without readiness rollback. Surface the owner-authorized private synthetic-proof action only when the server reports the current gate/evidence reference; label it non-network synthetic proof and never as discovery execution. Use one idempotency key per logical mutation; on unknown/stale/malformed/read-error state retain key/snapshot, hide all mutation controls, and expose only explicit read refresh. Preserve native-disabled Phase 4-7 UI controls and the early neutral unauthorized return.</action>
  <verify><automated>cd site && node --test tests/discovery-handler-ui.test.mjs --test-name-pattern="readiness|unknown|manual|unauthorized|navigation|picker|synthetic" && npm test</automated></verify>
  <done>The rendered workspace never computes readiness locally and no authority-unknown state exposes a ready/discovery/lifecycle action.</done>
</task>
<task type="auto">
  <name>Task 2: Implement bounded proposal review cards and responsive accessible styling</name>
  <files>site/app/discovery/proposal-cards.tsx, site/app/globals.css, site/tests/discovery-handler-ui.test.mjs</files>
  <action>Implement D-07/D-09/D-10/D-11 and every UI-SPEC proposal contract: render at most three cards and exact `{n} of 3` label; evidence, inference, Product fit, audience/likely buyer/examples as suggestion context, risks, provenance, fingerprint/collision/reopen history, frozen IDs/digests, and the verbatim suggested-market/not-Customer-Profile boundary. Explore is the only primary CTA and confirms Draft Market Play interview only; Defer requires reason/date; Dismiss requires reason/explicit confirmation. Freeze competing actions while pending; render immutable decision/cooldown history after success. Render source text as text, native disabled controls plus reasons, alert/live/focus behavior, details history, 44px targets, wrapping IDs, responsive stacking, and reduced motion. In malformed/partial/unknown states render no decision controls.</action>
  <verify><automated>cd site && node --test tests/discovery-handler-ui.test.mjs --test-name-pattern="proposal|three|cooldown|Draft|render" && npm test && npm run lint</automated></verify>
  <done>Proposal UI is bounded, auditable, accessible, and cannot imply an accepted profile, prospect, or operational activation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API projection → UI | Network/projection failures must not leave stale action authority. |
| untrusted evidence → DOM | Evidence/excerpts may contain hostile markup or instructions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-03-14 | Elevation | stale UI action | mitigate | Server-derived projections, stable keys, unknown-state action removal, explicit GET only. |
| T-03-15 | Tampering | source rendering | mitigate | Escaped JSX text and tests forbid raw HTML. |
| T-03-16 | Information disclosure | unauthorized UI | mitigate | Neutral access state reveals no Product/run/proposal data. |
</threat_model>
<verification>Run the rendered/UI suite, then run `cd site && npm run build`.</verification>
<success_criteria>The owner can safely inspect authoritative readiness and bounded proposals; incomplete or unknown authority is visibly and functionally fail-closed.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-06-SUMMARY.md` when done.</output>
