---
phase: 03-product-readiness-and-market-discovery
plan: 04
type: tdd
wave: 3
depends_on: [03-02]
files_modified:
  - site/domain/discovery-submission.ts
  - site/domain/market-discovery.ts
  - site/tests/market-discovery-repository.test.mjs
autonomous: true
requirements: [REQ-market-discovery]
must_haves:
  truths:
    - "Every trigger replays its pinned configuration and exposes no more than three evidence-backed proposals."
    - "Explore, Defer, and Dismiss are immutable revision-guarded owner decisions with no downstream activation."
  artifacts:
    - path: site/domain/discovery-submission.ts
      provides: reject-only production-safe untrusted finding boundary
    - path: site/domain/market-discovery.ts
      provides: immutable run/proposal/cooldown/decision authority
  key_links:
    - from: site/domain/discovery-submission.ts
      to: site/domain/market-discovery.ts
      via: normalized bounded finding only
      pattern: "normalized"
---

<objective>
Implement bounded, replayable Product Market Discovery and Draft-only proposal decisions.

Purpose: Persist and review sourced market hypotheses without treating runner output as authority or activating prospecting.
Output: Untrusted local submission seam and Product-scoped market-discovery domain service.
</objective>

<context>
@.planning/phases/03-product-readiness-and-market-discovery/03-CONTEXT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-RESEARCH.md
@docs/adr/0003-untrusted-runners-and-human-gates.md
@site/tests/market-discovery-repository.test.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement bounded untrusted finding intake and replayable run persistence</name>
  <files>site/domain/discovery-submission.ts, site/domain/market-discovery.ts, site/tests/market-discovery-repository.test.mjs</files>
  <behavior>
    - Initial, monthly, manual, and material-change runs pin their exact configuration/policies/window/watermark.
    - Invalid, oversized, partial, or duplicate submissions never create actionable proposal authority.
    - Only successful valid completion advances the schedule watermark; all outcomes remain replayable.
  </behavior>
  <action>Make the relevant RED cases pass for D-05/D-06. Keep `discovery-submission.ts` narrow: bounded schema/provenance/identity/idempotency/monotonic-state validation that hands normalized data to the domain; treat all source content as escaped data. Implement a repository lookup that derives the workspace owner from the admitted principal, loads the immutable private-proof authorization plus consumption winner under that workspace, and verifies its exact Product ID, expected Product revision, reviewed source revision, migration digest, fixed fixture digest/provenance, expiry, capability, and opaque non-secret evidence reference before consuming it. Implement the narrowly scoped `submit_private_synthetic_proof` seam only when that lookup succeeds: accept the fixed repository-defined, non-network fixture with immutable `synthetic_private_proof` provenance, source revision, fixture digest, configuration/run identity, and canonical operation digest; append audit and immutable consumption, return that same-digest winner on replay, and reject a second distinct operation as already consumed. Reject absent, expired, cross-owner/cross-Product, stale revision, source/migration/fixture-digest mismatch, arbitrary source URLs/content, and all client-supplied owner/evidence authority. It must not parse instructions, execute tools, select a provider, accept credentials, retrieve URLs, call a model, invoke a runner/scheduler, or follow current configuration pointers. In `market-discovery.ts`, persist Product-scoped initial/monthly/manual/material-change trigger identity, exact frozen configuration/policy versions, deterministic attempt/result lineage, and `(last successful watermark - 24h, started_at]` window. Keep production transport reject-only/absent; this proof seam is not a transport substitute and all fixture submissions prove the complete forbidden-effect zero delta.</action>
  <verify><automated>cd site && node --test tests/market-discovery-repository.test.mjs --test-name-pattern="trigger|window|watermark|submission|synthetic|replay|expired|cross.Product|migration|fixture|consum" && npm test</automated></verify>
  <done>All run records replay the frozen snapshot; no live runner/provider/retrieval capability or unvalidated submission becomes authority.</done>
</task>
<task type="auto">
  <name>Task 2: Implement capped proposal lineage and immutable owner review</name>
  <files>site/domain/market-discovery.ts, site/tests/market-discovery-repository.test.mjs</files>
  <behavior>
    - Server ordering surfaces at most three valid proposals per trigger and collision evidence joins history.
    - Explore creates or opens only a Draft Market Play interview.
    - Defer/Dismiss enforce 90/180-day cooldowns; only material evidence can reopen.
  </behavior>
  <action>Implement D-07 through D-11 with server-side validation/ranking and a deterministic Product + normalized market category + audience + problem family fingerprint. Persist immutable proposal/evidence versions with Product fit, problem match, audience, likely buyer, examples, cited provenance, risk, and collision relationship. Cap each run at three surfaced proposals; attach duplicate evidence/version history to the fingerprint rather than create a second accepted market. Implement revision- and digest-guarded owner decision operations with canonical operation digests and winner reads: Explore opens/creates only the scoped Draft Market Play Consensus Interview; Defer requires reason/date and 90-day cooldown; Dismiss requires reason and 180-day cooldown. Reopen only for explicit materially changed evidenced match/audience/fit/risk lineage, never elapsed time, copied text, or retry. Assert every path has zero downstream operational delta.</action>
  <verify><automated>cd site && node --test tests/market-discovery-repository.test.mjs && npm test && npm run lint</automated></verify>
  <done>All discovery domain RED contracts pass; proposals remain Product-level hypotheses and never create a Ready Profile, prospecting, identity, contact, paid, export, or outbound state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| untrusted finding → application | Sources and runner-like input can be malicious, malformed, or replayed. |
| proposal → Market Play interview | A suggestion may create Draft-only review context, never operational authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-08 | Tampering | submission intake | mitigate | Bounded schema/provenance/identity/idempotency and reject-only production transport. |
| T-03-09 | Spoofing | proposal decision | mitigate | Admitted owner plus exact revision/digest/key command guard. |
| T-03-10 | Elevation | Explore path | mitigate | Draft interview only with complete forbidden-table zero-delta tests. |
</threat_model>
<verification>Complete RED→GREEN→REFACTOR with the complete market-discovery repository suite.</verification>
<success_criteria>Discovery is bounded/replayable and owner review has durable cooldown history without activating any future operational workflow.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-04-SUMMARY.md` when done.</output>
