---
phase: 03-product-readiness-and-market-discovery
plan: 09
type: execute
wave: 8
depends_on: [03-08]
files_modified: []
autonomous: false
requirements: [REQ-product-readiness, REQ-market-discovery]
user_setup:
  - service: hosted private pilot
    why: Separate owner authorization and accepted prior hosted capability evidence are required before any Phase 3 release activity.
must_haves:
  truths: ["Hosted Phase 3 work cannot proceed while predecessor authority or private-host capability evidence is unknown."]
  artifacts: []
  key_links: []
---
<objective>Obtain the explicit human authorization checkpoint for a narrow Phase 3 hosted proof.

Purpose: Keep the unproven hosted scheduler/runner boundary manual and non-substitutable.
Output: Owner decision and evidence references, or an explicit BLOCKED record; no deployment, migration, scheduler, runner, retrieval, provider, or product mutation.</objective>
<context>@.planning/phases/03-product-readiness-and-market-discovery/03-VALIDATION.md
@docs/WAVE-0-CAPABILITY-REPORT.md
@.planning/STATE.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human"><name>Task 1: Confirm the runtime authorization through the existing Consensus Interview</name><action>Using the existing owner-scoped Consensus Interview—not seed data, a direct D1 edit, or an invented Product value—record the explicit owner confirmation required by D-13 for exactly `private-hosted-synthetic-proposal-proof`. The confirmation must be for one admitted owner/workspace and Product, with expected Product revision, exact reviewed source revision, migration digest, fixed provenance-marked fixture digest, expiry, and an opaque non-secret evidence reference. This is not authorization for scheduled dispatch, Runner callback, retrieval, provider credentials/calls, spend, profile/prospecting, contacts, exports, or outbound effects.</action><what-built>Local Phase 3 authority and the offline preflight identify the one narrow synthetic-proof capability while all transport capabilities remain blocked.</what-built><how-to-verify>1. Review the accepted Phase 2 predecessor authority, exact reviewed Phase 3 revision/migration, and green local `test:phase3` evidence. 2. In the existing Consensus Interview, confirm the owner decision containing the exact Product/revision/source/migration/fixture-digest/expiry fields and retain only its opaque evidence reference. 3. Verify the confirmation explicitly excludes scheduler, runner, retrieval, provider, and all downstream effects.</how-to-verify><verify><human-check>Confirmed Consensus Interview decision contains all required scope fields and exclusions without exposing secret material.</human-check></verify><resume-signal>Record the non-secret confirmed-decision/evidence reference, or type “blocked”.</resume-signal></task>
<task type="checkpoint:human-action" gate="blocking-human"><name>Task 2: Activate the immutable server-derived authorization record and supply preflight references</name><action>Through the owner-only `activate_private_synthetic_proof_authorization` controlled server action, materialize the authorization from the already-confirmed Consensus Interview decision. The server, not the request, derives the admitted owner/workspace/Product and exact revision/migration/fixture digest; it stores only the immutable non-secret authorization/evidence reference, expiry, and capability metadata. Then provide only the non-secret references consumed by the release-preflight checker. Do not directly edit D1, deploy, migrate, enter secrets, or configure a scheduler/Runner/provider.</action><what-built>No automatic release or external capability activation has been performed; the immutable D1 authorization is created only from the confirmed owner decision.</what-built><how-to-verify>1. Invoke the controlled action as the admitted owner and confirm the returned record is bound to the intended Product, expected revision, exact digests, expiry, and opaque evidence reference. 2. Run the offline preflight with the supplied manifest. 3. Confirm it recognizes at most the private synthetic-proof scope, rejects expired/cross-Product/digest-mismatched/reused evidence, and continues to mark scheduler/runner/retrieval/provider BLOCKED. 4. Confirm no credentials or private evidence content were added to the repository or chat.</how-to-verify><verify><human-check>Preflight output names the narrow scope and blocked capabilities; immutable record and references contain no secrets.</human-check></verify><resume-signal>Provide the non-secret authorization record/evidence reference identifiers, or type “blocked”.</resume-signal></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| owner approval → hosted operations | Approval scope must not silently broaden to runner/scheduler/provider effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-03-20 | Elevation | hosted authorization | mitigate | Blocking human decision records narrow scope and explicit prohibited effects. |
| T-03-21 | Information disclosure | evidence handoff | mitigate | Non-secret references only; no credentials in code or planning records. |
</threat_model>
<verification>Human approval and non-secret evidence references are required; otherwise end BLOCKED with no state change.</verification>
<success_criteria>The release path is either explicitly narrow-authorized with evidence or remains safely blocked.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-09-SUMMARY.md` when done.</output>
