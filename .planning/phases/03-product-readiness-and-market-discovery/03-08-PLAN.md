---
phase: 03-product-readiness-and-market-discovery
plan: 08
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - site/scripts/phase3-release-preflight.mjs
  - site/tests/phase3-release-preflight.test.mjs
  - site/package.json
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths: ["No hosted release can claim scheduler/runner capability from local persistence or source presence."]
  artifacts:
    - path: site/scripts/phase3-release-preflight.mjs
      provides: offline release-evidence completeness checker
  key_links:
    - from: site/scripts/phase3-release-preflight.mjs
      to: docs/WAVE-0-CAPABILITY-REPORT.md
      via: required blocked-gate evidence identifiers
      pattern: "scheduler"
---
<objective>Build an offline, fail-closed Phase 3 release-evidence preflight.

Purpose: Prevent a deployment/migration from being mistaken for authorization to schedule, retrieve, run providers, or prospect.
Output: Local evidence checker and unit coverage; no deploy or external calls.</objective>
<context>@docs/WAVE-0-CAPABILITY-REPORT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-VALIDATION.md
@docs/adr/0003-untrusted-runners-and-human-gates.md</context>
<tasks>
<task type="auto"><name>Task 1: Implement offline blocked-gate and private-proof evidence checker</name><files>site/scripts/phase3-release-preflight.mjs, site/tests/phase3-release-preflight.test.mjs</files><action>Create a deterministic local script that reads explicitly supplied local evidence manifests/references and fails unless Phase 2 dependency status, exact reviewed source identity, local `test:phase3`, migration identity, and owner-scoped authorization are present. The only Phase 3 accepted capability it may recognize is `private-hosted-synthetic-proposal-proof`: inspect only non-secret authorization metadata binding immutable admitted owner/workspace, Product ID, expected revision, exact source revision, migration digest, fixture digest/provenance, expiry, single-use consumption/replay winner, and no-effect audit/log evidence. It must reject absent, expired, cross-Product, digest-mismatched, consumed-by-different-operation, stale, mismatched, or unknown evidence. Report scheduler/runner/retrieval/provider transport as BLOCKED by default and require separate future proof references for each. It must never authenticate, deploy, invoke a provider, inspect secrets, or mutate state. Treat missing, stale, mismatched, or unknown evidence as failure.</action><verify><automated>cd site && node --test tests/phase3-release-preflight.test.mjs && npm test</automated></verify><done>The preflight has a tested fail-closed default, recognizes only the owner-scoped synthetic hosted proof, and cannot turn code, a schedule row, or omitted proof into scheduler/runner authorization.</done></task>
<task type="auto"><name>Task 2: Integrate the checker into local release preparation</name><files>site/package.json</files><action>Wire the checker into the existing local Phase 3 preflight script without adding packages or network behavior. Its output must distinguish local authority success, the narrowly accepted private synthetic proof (when owner evidence is supplied), and blocked scheduler/runner/retrieval capability. It must exit nonzero while the relevant human evidence is absent and never claim production discovery execution.</action><verify><automated>cd site && npm run test:phase3 && ! npm run preflight:phase3-release && npm test && npm run lint</automated></verify><done>The release-preflight gate is fail-closed without owner evidence and never broadens the synthetic proof into transport authority.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| local evidence → hosted activation | Local artifacts cannot prove hosted scheduler/runner behavior. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-03-18 | Elevation | release gate | mitigate | Offline checker defaults BLOCKED and requires explicit separate evidence references. |
| T-03-19 | Information disclosure | preflight | mitigate | No secret reads, external APIs, or provider calls. |
</threat_model>
<verification>Run the checker’s unit suite; verify blocked mode exits nonzero and performs no network operation.</verification>
<success_criteria>Hosted capability is explicitly distinguishable from local Phase 3 correctness and remains fail-closed.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-08-SUMMARY.md` when done.</output>
