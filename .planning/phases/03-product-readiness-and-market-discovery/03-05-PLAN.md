---
phase: 03-product-readiness-and-market-discovery
plan: 05
type: execute
wave: 4
depends_on: [03-03, 03-04]
files_modified:
  - site/domain/discovery-handler.ts
  - site/app/api/discovery/route.ts
  - site/tests/discovery-handler-ui.test.mjs
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths: ["Only the admitted owner can read or mutate an authoritative Product discovery projection."]
  artifacts:
    - path: site/domain/discovery-handler.ts
      provides: closed, CSRF-protected owner API boundary
    - path: site/app/api/discovery/route.ts
      provides: provider wiring only
  key_links:
    - from: site/app/api/discovery/route.ts
      to: site/domain/discovery-handler.ts
      via: injected trusted bindings
      pattern: "handleDiscovery"
---
<objective>
Expose Product readiness and discovery through the existing private owner boundary.

Purpose: Ensure routes never turn client payloads or runner identity into authority.
Output: Owner-only GET projection and guarded closed POST action dispatch.
</objective>
<context>@.planning/phases/03-product-readiness-and-market-discovery/03-CONTEXT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-PATTERNS.md
@site/domain/interview-handler.ts
@site/app/api/interview/route.ts</context>
<tasks>
<task type="auto">
  <name>Task 1: Implement admitted-owner discovery handler</name>
  <files>site/domain/discovery-handler.ts, site/tests/discovery-handler-ui.test.mjs</files>
  <action>Implement D-12 by copying the established order: authenticate/admit before parse, owner-derived workspace lookup, GET projection, replacement CSRF on state response, then POST same-origin/Fetch Metadata `discovery-mutation` intent/8 KiB bounded JSON/consumed CSRF. Construct typed commands field-by-field for only `read_product_readiness`, `make_product_ready`, `start_manual_discovery`, `activate_private_synthetic_proof_authorization`, `submit_private_synthetic_proof`, `decide_proposal`, and explicit current-state read. `activate_private_synthetic_proof_authorization` is the sole controlled write path: only the admitted workspace owner can invoke it after the existing Consensus Interview has an explicit confirmed owner decision; the server derives owner/workspace/Product and exact source/migration/fixture digests, writes the immutable non-secret authorization/evidence reference and expiry, and never accepts raw authorization text, owner IDs, secrets, fixture body, or a client-selected digest. `submit_private_synthetic_proof` must perform the server authorization lookup and consume/replay the stored winner as specified by the domain; it cannot admit a Runner or arbitrary submission ingestion. Map access denial to neutral no-store 404, CSRF to 403, stale/idempotency conflicts to 409, oversize to 413, unsupported action to 400, opaque errors to no-store/nosniff 500. Test no identity, outsider, cross-owner product locator, request-security failures, stale/race/replay behavior, authorization absent/expired/cross-Product/digest mismatch/second-use denial, controlled activation only from the confirmed Consensus Interview, fixed-fixture provenance/audit, and no downstream writes.</action>
  <verify><automated>cd site && node --test tests/discovery-handler-ui.test.mjs --test-name-pattern="handler|owner|origin|CSRF|neutral|synthetic" && npm test</automated></verify>
  <done>All HTTP authority behavior is owner-scoped, closed, bounded, replay-safe, and neutral on denial.</done>
</task>
<task type="auto">
  <name>Task 2: Add provider-wiring-only discovery route</name>
  <files>site/app/api/discovery/route.ts</files>
  <action>Create dynamic GET/POST route wiring analogous to interview route: inject existing DB, owner pepper, owner email, and `getChatGPTUser` into the handler. Per D-12/D-13, place no Product ID scope, readiness/lifecycle/domain logic, runner callback, scheduler dispatch, provider credentials, fallback, or external call in the route.</action>
  <verify><automated>cd site && node --test tests/discovery-handler-ui.test.mjs --test-name-pattern="route|binding|handler" && npm test && npm run lint</automated></verify>
  <done>The route is a thin trusted-binding adapter and all mutation behavior remains in tested domain/handler code.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser → handler | Request metadata/body/locator values are untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-03-11 | Spoofing | API identity | mitigate | Existing owner admission before parse and neutral 404. |
| T-03-12 | CSRF/Tampering | POST actions | mitigate | Same-origin, Fetch Metadata, intent, one-time CSRF, bounded closed dispatch. |
| T-03-13 | Elevation | route binding | mitigate | Route has no business authority or runner transport. |
</threat_model>
<verification>Run the handler contract suite and verify no route handler contains domain transitions.</verification>
<success_criteria>Only the admitted owner receives authoritative Phase 3 state or can issue a guarded command.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-05-SUMMARY.md` when done.</output>
