---
phase: 03-product-readiness-and-market-discovery
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - site/db/schema.ts
  - site/drizzle/0005_product_discovery.sql
  - site/tests/helpers/d1.mjs
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths:
    - "Discovery authority has durable relational, immutable, race-safe storage."
  artifacts:
    - path: site/drizzle/0005_product_discovery.sql
      provides: additive Phase 3 schema and partial uniqueness guards
  key_links:
    - from: site/tests/helpers/d1.mjs
      to: site/drizzle/0005_product_discovery.sql
      via: exact full migration sequence
      pattern: "0005_product_discovery"
---

<objective>
Add the additive Phase 3 persistence contracts and test fixture support.

Purpose: Let domain code commit immutable Product readiness, replayable discovery, and proposal decisions without a parallel source of truth.
Output: D1/Drizzle schema, inspected additive migration, and forbidden-downstream fixture manifest.
</objective>

<context>
@.planning/phases/03-product-readiness-and-market-discovery/03-CONTEXT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-PATTERNS.md
@docs/adr/0001-generic-company-product-play-model.md
@docs/adr/0002-confirmed-knowledge-and-effective-configuration.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define relational immutable discovery authority</name>
  <files>site/db/schema.ts, site/drizzle/0005_product_discovery.sql</files>
  <action>Extend the Phase 2 aggregate additively (do not replace Product, configuration, Market Play, or knowledge authority) with Product Discovery Configuration prerequisites, Product schedules, Product discovery runs/events/submissions, immutable proposal versions/evidence, owner decisions, and fingerprint/collision/split/merge/reopen lineage. Per D-02/D-05/D-08/D-09, persist canonical manifest JSON and digest plus frozen version ID/digest references, run trigger/window/watermark/configuration identity, exact proposal JSON/digest, and decision operation digest. Add `private_synthetic_proof_authorizations` and append-only `private_synthetic_proof_consumptions`: the authorization is server-created from an admitted owner and confirmed Consensus Interview decision, and binds workspace ID, immutable owner subject ID, Product ID, Product expected revision, exact reviewed source revision, migration digest, fixed fixture digest/provenance, opaque non-secret authorization/evidence reference, expiry, and the sole capability `private-hosted-synthetic-proposal-proof`; the consumption binds authorization ID and canonical operation digest to the immutable winner result. Use FKs and partial/unique indexes so an authorization cannot cross workspace/Product scope, cannot be activated from client-provided identity/evidence, is consumed once, and only the same operation digest can replay its existing winner. Never store fixture body, credentials, authorization text, or other secrets. Preserve historical rows, never mutate a configuration/proposal/decision/authorization/consumption in place, never fixture-backfill Product Ready, and do not use foreign-keys-off migration behavior.</action>
  <verify><automated>cd site && npm run db:generate && node --test tests/product-readiness-repository.test.mjs --test-name-pattern="migration|schema" && node --test tests/request-security.test.mjs tests/pilot-access.test.mjs && npm run build && npm run lint</automated></verify>
  <done>The inspected migration is additive and D1-valid; database constraints, not UI flags, enforce the one-winner invariants.</done>
</task>
<task type="auto">
  <name>Task 2: Extend full-chain fixture and forbidden downstream assertion support</name>
  <files>site/tests/helpers/d1.mjs</files>
  <action>Append the resolved Phase 3 migration to the exact full-chain list and update fixture assertions only after verifying the actual generated filename. Keep Phase 3 readiness/configuration/run/proposal rows outside the forbidden set, but include every Phase 4-7 operational table available in the schema: profile readiness/prospecting schedules and runs, Accounts, Targets, Signals, Candidates, Prospects, Contacts, provider grants/calls, packages/messages, exports, and external-effect records. Preserve `snapshotForbiddenOperationalRows`, `assertForbiddenOperationalRowsUnchanged`, and `runRace` as the shared positive-plus-zero-delta mechanism.</action>
  <verify><automated>cd site && node --test tests/product-readiness-repository.test.mjs --test-name-pattern="migration|forbidden" && node --test tests/request-security.test.mjs tests/pilot-access.test.mjs && npm run build && npm run lint</automated></verify>
  <done>All Phase 3 repository tests run the full migration chain and can prove no forbidden downstream operational row changes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| domain → D1 | Durable constraints must reject concurrent or cross-scope authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03 | Tampering | schema/migration | mitigate | FKs, append-only records, partial uniqueness, and full-chain migration tests. |
| T-03-04 | Repudiation | readiness/discovery rows | mitigate | Immutable manifest/result/decision digests and audit linkage. |
| T-03-SC | Tampering | package manager | accept | Use existing dependencies only; no install task exists. |
</threat_model>

<verification>While later Phase 3 suites intentionally remain RED, run the named migration/forbidden tests plus the passing request-security/pilot-access baseline, build, and lint; run full `npm test` once this wave's relevant Phase 3 suites are green. Inspect generated SQL for additive D1-compatible constraints.</verification>
<success_criteria>Storage can represent exactly one immutable readiness result, bounded replayable discovery, and immutable owner review without any Phase 4-7 write path.</success_criteria>
<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-02-SUMMARY.md` when done.</output>
