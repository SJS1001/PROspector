---
phase: 03-product-readiness-and-market-discovery
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - site/tests/product-readiness-repository.test.mjs
  - site/tests/market-discovery-repository.test.mjs
  - site/tests/discovery-handler-ui.test.mjs
autonomous: true
requirements: [REQ-product-readiness, REQ-market-discovery]
must_haves:
  truths:
    - "No readiness, discovery, or proposal behavior can be accepted without executable authority and zero-effect contracts."
  artifacts:
    - path: site/tests/product-readiness-repository.test.mjs
      provides: RED atomic readiness and forbidden-effect contracts
    - path: site/tests/market-discovery-repository.test.mjs
      provides: RED replay, cap, cooldown, and proposal-decision contracts
    - path: site/tests/discovery-handler-ui.test.mjs
      provides: RED owner-bound handler and fail-closed UI contracts
  key_links:
    - from: site/tests/product-readiness-repository.test.mjs
      to: site/tests/helpers/d1.mjs
      via: full migration fixture and forbidden-table snapshot
      pattern: "snapshotForbiddenOperationalRows"
---

<objective>
Create the Phase 3 Wave 0 failing contracts before any authority implementation.

Purpose: Make the readiness, untrusted discovery, and no-downstream-effect boundaries independently executable.
Output: Three focused RED test suites that name the exact persisted and rendered behavior later plans must satisfy.
</objective>

<context>
@.planning/phases/03-product-readiness-and-market-discovery/03-CONTEXT.md
@.planning/phases/03-product-readiness-and-market-discovery/03-VALIDATION.md
@.planning/phases/03-product-readiness-and-market-discovery/03-PATTERNS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write RED Product Ready atomicity contracts</name>
  <files>site/tests/product-readiness-repository.test.mjs</files>
  <action>Using the full D1 chain and fixed clocks, write failing tests for D-01 through D-04: all nine server-derived readiness categories; rejection of Proposed Knowledge, fixture text, and client flags; exact sorted confirmed version-ID/digest plus Product expected-revision checks; zero Play/Profile/Offer readiness; one canonical immutable manifest/configuration, initial key `initial:product:{product_id}:{configuration_id}`, manual availability, monthly Product schedule, and audit in one result. Cover lost-response replay, conflicting idempotency keys, concurrent Ready calls, pause/archive/stale/missing-gate/configuration-failed fail-closed behavior, exhausted initial run as Needs attention without readiness rollback or a second run, and Phase 2-confirmed material Product replacement activation. Per Phase 2 replacement authority, test that only an admitted, lifecycle/configuration-gate-valid, confirmed replacement activation appends an immutable predecessor/successor configuration lineage event and idempotently creates/reuses exactly one material-change discovery trigger bound to the replacement configuration; duplicate/reordered activation events and races converge to one trigger, while unconfirmed/proposed or invalid lifecycle changes create none. Every successful or replayed mutation snapshots and proves zero delta in every Phase 4-7 operational table.</action>
  <verify><automated>cd site && node --check tests/product-readiness-repository.test.mjs && ! node --test tests/product-readiness-repository.test.mjs && node --test tests/request-security.test.mjs tests/pilot-access.test.mjs && npm run build</automated></verify>
  <done>The suite is syntactically valid, initially fails only because the Phase 3 contract is absent, and asserts atomic authority plus the full forbidden-effect manifest.</done>
</task>
<task type="auto">
  <name>Task 2: Write RED replayable discovery and proposal-decision contracts</name>
  <files>site/tests/market-discovery-repository.test.mjs</files>
  <action>Write deterministic failing domain tests for D-05 through D-11. Cover Product-scoped initial/monthly/manual/material-change trigger identities, pinned configuration/policy snapshots, `(last_successful_watermark - 24h, started_at]` windows, successful-run-only watermark advancement, bounded untrusted submissions, malformed/partial authority-unknown results, deterministic ranking capped at three, fingerprint collision evidence attachment, immutable version/split/merge/reopen lineage, and stale/retry/race convergence. Assert one immutable owner decision per exact proposal revision/digest: Explore creates or opens only a Draft Market Play Consensus Interview; Defer records reason/review date and 90 days; Dismiss records reason/confirmation and 180 days; only changed material evidence can reopen. Pair all positive cases with zero deltas for Profile readiness/prospecting, Accounts, Targets, Signals, Candidates, Prospects, contacts, paid effects, exports, approvals, packages, messages, and outbound state.</action>
  <verify><automated>cd site && node --check tests/market-discovery-repository.test.mjs && ! node --test tests/market-discovery-repository.test.mjs && node --test tests/request-security.test.mjs tests/pilot-access.test.mjs && npm run build</automated></verify>
  <done>The failing suite defines the three-proposal ceiling, immutable replay, cooldown rules, Draft-only Explore boundary, and downstream zero-delta proof.</done>
</task>
<task type="auto">
  <name>Task 3: Write RED handler and rendered fail-closed contracts</name>
  <files>site/tests/discovery-handler-ui.test.mjs</files>
  <action>Write failing owner/outsider/no-identity/cross-owner handler tests for D-12 and rendered-state tests for D-07/D-13. Require same-origin, Fetch Metadata, `discovery-mutation` intent, consumed CSRF, bounded JSON, closed action dispatch, no-store neutral denial, idempotency replay/mismatch, and stale conflicts. Cover an immutable server-derived private-proof authorization/evidence record and its separate immutable consumption/winner record: the admitted workspace owner, Product ID, Product expected revision, exact reviewed source revision, migration digest, fixed fixture digest/provenance, opaque non-secret evidence reference, expiry, and capability name are bound on authorization; one authorization may be consumed once, while the same operation digest replays the recorded winner. Require rejection for absent, expired, cross-workspace/cross-owner or cross-Product authorization, revision/source/migration/fixture-digest mismatch, a different replay operation, and second-use/replay abuse. The owner-only `submit_private_synthetic_proof` action must use that server lookup, accept only the fixed non-network fixture/provenance marker, be idempotent/audited, and create no runner, scheduler, retrieval, provider, or downstream effect. Require UI projections for all checklist states, immutable references, blocked/Needs-attention runs, evidence/inference/fit/collision/cooldown data, at most three proposal cards, escaped untrusted text, and Draft-only Explore. Test a third primary `Market Discovery` navigation item, a persisted last-selected Product picker, explicit Product scope from the server projection (never Play/Profile inference), and retained Knowledge/Pilot access. Assert malformed/partial/read-error authority-unknown states expose only explicit read refresh and no enabled readiness, discovery, lifecycle, or decision action; assert future Phase 4-7 controls remain native-disabled with visible reasons.</action>
  <verify><automated>cd site && node --check tests/discovery-handler-ui.test.mjs && ! node --test tests/discovery-handler-ui.test.mjs && node --test tests/request-security.test.mjs tests/pilot-access.test.mjs && npm run build</automated></verify>
  <done>All owner-bound HTTP and UI safety assertions are RED and none permits profile, prospecting, contact, spend, export, or outbound effects.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| owner/client → discovery API | Client revision, IDs, and action payload are untrusted. |
| discovery state → later operations | Phase 3 state must not create Phase 4-7 authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | readiness command | mitigate | RED tests require digest/revision/idempotency and atomic winner behavior. |
| T-03-02 | Elevation | proposal decision | mitigate | RED tests require owner-only Draft-only Explore and forbidden-table zero deltas. |
| T-03-SC | Tampering | test/dependency work | accept | No package install is planned; existing test stack only. |
</threat_model>

<verification>
Run each named suite as a RED gate; the command must fail because the corresponding implementation does not yet exist, not because of syntax or fixture setup errors.
</verification>

<success_criteria>
The complete Phase 3 acceptance surface is encoded in executable failing tests before schema or production code changes.
</success_criteria>

<output>Create `.planning/phases/03-product-readiness-and-market-discovery/03-01-SUMMARY.md` when done.</output>
