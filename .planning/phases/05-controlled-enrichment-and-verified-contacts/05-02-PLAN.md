---
phase: 05-controlled-enrichment-and-verified-contacts
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [site/db/schema.ts, site/drizzle/0008_controlled_enrichment.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0008_snapshot.json, site/domain/enrichment-repository.ts, site/tests/helpers/d1.mjs, site/tests/enrichment-contract.test.mjs]
autonomous: true
requirements: [REQ-controlled-enrichment, REQ-contact-verification]
must_haves:
  truths: ["Immutable exact-source grants, quotes, reservations, contact evidence, and identity decisions have additive durable storage."]
  artifacts: [{path: site/drizzle/0008_controlled_enrichment.sql, provides: additive Phase 5 D1 constraints}, {path: site/drizzle/meta/0008_snapshot.json, provides: Phase 5 Drizzle schema snapshot}, {path: site/domain/enrichment-repository.ts, provides: workspace-scoped transaction repository}]
  key_links: [{from: site/domain/enrichment-repository.ts, to: site/drizzle/0008_controlled_enrichment.sql, via: guarded grant/reservation persistence}, {from: site/drizzle/0008_controlled_enrichment.sql, to: site/tests/helpers/d1.mjs, via: ordered 0000-through-0008 full-chain fixture}]
---
<objective>Add the bounded Phase 5 persistence foundation. Purpose: make one-time authority and immutable evidence enforceable below application code. Output: additive migration and repository contracts.</objective>
<context>@site/db/schema.ts
@site/drizzle/0007_profile_prospecting.sql
@.planning/phases/05-controlled-enrichment-and-verified-contacts/05-PATTERNS.md
@.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-02-PLAN.md
@.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-07-PLAN.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add additive immutable authority and evidence schema</name><files>site/db/schema.ts, site/drizzle/0008_controlled_enrichment.sql, site/drizzle/meta/_journal.json, site/drizzle/meta/0008_snapshot.json, site/tests/enrichment-contract.test.mjs</files><behavior>- Grants bind provider/version, exact Approved Prospect set, current Profile Effective Configuration/quote revision, operation, random nonce/digest, units/maximum cost/currency/expiry/owner/operation key and cannot be reused or mutated.
- Reservation, quote/catalog, runner grant ledger, immutable contact evidence, identity proposal/decision/lineage, and current eligibility references retain all auditable inputs.</behavior><action>Extend `site/db/schema.ts` and create additive migration `0008_controlled_enrichment.sql` after Phase 4's existing `0007_profile_prospecting.sql`; update `_journal.json` and generate the matching `0008_snapshot.json`. Add enums/checks/FKs/partial unique guards for grant+operation reservation, immutable records, currency and state. Preserve existing tables/data and model suppression only as preserved references—do not create or mutate Phase 6 tombstones.</action><verify><automated>cd site && npm run db:generate && node --test tests/enrichment-contract.test.mjs</automated></verify><done>Schema metadata and migration ordinal are consistent, a second reservation/consume for a grant-operation is rejected, and Phase 5 records remain immutable.</done></task>
<task type="auto" tdd="true"><name>Task 2: Prove the full chain and expose server-scoped repository transactions</name><files>site/domain/enrichment-repository.ts, site/tests/helpers/d1.mjs, site/tests/enrichment-contract.test.mjs</files><behavior>- Migrations 0000 through 0008 load in exact order on clean and prior-Phase-4 fixtures, preserving existing rows, constraints, triggers, indexes, and foreign-key integrity.
- Repository loads exact workspace/approved Prospect/current configuration and atomically consumes/reserves with bounded audit state.
- No browser/provider-supplied workspace or authority field is accepted.</behavior><action>Extend the shared D1 fixture to apply the exact 0000-through-0008 chain and assert clean/prior-schema coexistence, Drizzle journal/snapshot alignment, foreign keys, triggers, indexes, stale/duplicate violations, and zero Phase 6/7 or credential/provider-secret effects. Implement typed repository reads/writes for canonical grant-tuple hashing and immutable issuance audit, expected-revision/idempotency guards, and one D1 batch for consume/reserve/cap comparison/audit. Make same-key/same-digest issuance return the original grant and changed-payload reuse conflict; keep credentials, SDK types, raw contact values, and provider endpoints out of schema/domain records.</action><verify><automated>cd site && node --test tests/enrichment-contract.test.mjs</automated></verify><done>The exact full chain through 0008 is additive and replayable, and the transaction repository is the only persistence seam used by later grant issuance and reservation authority work.</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
| authority service → D1 | Durable constraints prevent duplicate consumption/spend. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-05-04 | T/R | D1 grant ledger | mitigate | immutable rows, unique operation guards, audit. |
| T-05-05 | E | repository scope | mitigate | admitted workspace joins and no client authority. |
</threat_model>
<verification>Run `cd site && npm run db:generate && node --test tests/enrichment-contract.test.mjs`; confirm the exact 0000-through-0008 chain and generated journal/snapshot remain additive.</verification>
<success_criteria>Durable storage can represent every Phase 5 decision without granting an external effect.</success_criteria>
<output>Create `.planning/phases/05-controlled-enrichment-and-verified-contacts/05-02-SUMMARY.md` only when complete.</output>
