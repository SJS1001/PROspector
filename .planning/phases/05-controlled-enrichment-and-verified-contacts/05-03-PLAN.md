---
phase: 05-controlled-enrichment-and-verified-contacts
plan: 03
type: tdd
wave: 3
depends_on: [05-02]
files_modified: [site/domain/enrichment-grant-issuance.ts, site/domain/enrichment-authority.ts, site/domain/runner-spend-authority.ts, site/tests/enrichment-contract.test.mjs]
autonomous: true
requirements: [REQ-controlled-enrichment]
must_haves:
  truths: ["Only the admitted owner can issue an immutable exact-source grant, and issuance cannot call a provider.", "Only a committed, bounded reservation can reach a provider port.", "Runner spend authority is independently bounded."]
  artifacts: [{path: site/domain/enrichment-grant-issuance.ts, provides: authoritative immutable grant issuance}, {path: site/domain/enrichment-authority.ts, provides: grant validation and reservation authority}, {path: site/domain/runner-spend-authority.ts, provides: separate billed-runner authority}]
  key_links: [{from: enrichment-grant-issuance.ts, to: enrichment-repository.ts, via: owner-scoped immutable grant transaction}, {from: enrichment-authority.ts, to: enrichment-repository.ts, via: transaction before port acquisition}]
---
<objective>Implement authoritative grant issuance, fail-closed reservation, and separately billed Runner spend authority. Purpose: prohibit provider calls/retries/failover unless exact independently bounded authority is committed. Output: typed authority services.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Issue the immutable exact-source enrichment grant</name><files>site/domain/enrichment-grant-issuance.ts, site/tests/enrichment-contract.test.mjs</files><behavior>- Admitted owner issuance loads the exact current Approved Prospect and Profile Effective Configuration, quote/catalog revision, and expected owner revision; it derives—not accepts—the workspace, owner, canonical prospect ordering, random nonce, and SHA-256 digest.
- The immutable tuple contains provider identity/version, Approved Prospect IDs, permitted operation, maximum units, maximum cost, currency, expiry, owner, random nonce, and digest; same idempotency key plus same tuple returns the original grant, while altered payload or stale upstream revision conflicts.
- Issuance performs no reservation and cannot obtain or invoke `ContactProviderPort`.</behavior><action>Create `issueEnrichmentGrant` as the authoritative domain service over the repository. Validate owner admission, exact Approved lifecycle/current configuration and quote binding, bounded future expiry, and canonical tuple before one immutable grant/audit transaction; expose only a typed issued/blocked/conflict result. Do not accept client workspace, owner, digest, nonce, current configuration, quote, or grant fields as authority and do not import the provider port.</action><verify><automated>cd site && node --test tests/enrichment-contract.test.mjs</automated></verify><done>Only an admitted owner can obtain a replay-safe immutable grant bound to exact current inputs, and every issuance path makes zero provider calls.</done></task>
<task type="auto" tdd="true"><name>Task 2: Validate and reserve the exact granted operation</name><files>site/domain/enrichment-authority.ts, site/tests/enrichment-contract.test.mjs</files><behavior>- Per locked provider-neutral authority decisions: validate owner/workspace, exact Approved prospect/current effective config, issued grant tuple, quote/catalog expiry/version, currency, units and actual-plus-reserved grant/profile/workspace/provider caps before reservation.
- Any failure returns a bounded blocked result and fake provider calls remain zero.</behavior><action>Implement pure tuple validation plus repository-backed atomic reservation. Require server-derived identity and canonical operation key; call no adapter here. Model documented partial settlement/release and uncertain `NeedsReconciliation`; prohibit retry, provider switch, extension, or auto-release for uncertainty.</action><verify><automated>cd site && node --test tests/enrichment-contract.test.mjs</automated></verify><done>All grant/cap/race negatives pass with zero calls; only a committed reservation produces an authorized assignment.</done></task>
<task type="auto" tdd="true"><name>Task 3: Keep separately billed Runner authority separate</name><files>site/domain/runner-spend-authority.ts, site/tests/enrichment-contract.test.mjs</files><behavior>- Runner grant has provider/model/catalog/run-type/scope/per-run/monthly/currency/expiry/retry bounds.
- No enrichment grant or reservation can satisfy Runner work, and no Runner budget can pay enrichment.</behavior><action>Implement the narrow runner-grant validator/reservation accounting required by the roadmap; reuse no enrichment authorization object except shared primitive money validation. Keep it non-dispatching and do not alter Phase 4 assignment transport.</action><verify><automated>cd site && node --test tests/enrichment-contract.test.mjs</automated></verify><done>Separate cap and retry tests prove no cross-authority borrowing.</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
| owner request → authority services | Client intent never becomes spend authority without durable predicates. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-05-06 | T/E/R | grant issuance and reservation decision | mitigate | canonical issuance tuple, expected revision/idempotency, then complete cap validation and atomic commit. |
| T-05-07 | E | billed Runner work | mitigate | distinct grant type and ledgers. |
</threat_model>
<verification>Run `cd site && node --test tests/enrichment-contract.test.mjs`.</verification>
<success_criteria>Calls are structurally unreachable before reservation and separate billing cannot inherit authority.</success_criteria>
<output>Create `.planning/phases/05-controlled-enrichment-and-verified-contacts/05-03-SUMMARY.md` only when complete.</output>
