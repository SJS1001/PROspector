---
phase: 05-controlled-enrichment-and-verified-contacts
plan: 07
type: execute
wave: 5
depends_on: [05-06]
files_modified: [site/domain/contacts-handler.ts, site/app/prospects/contacts-workspace.tsx, site/app/prospects/contact-leaves.tsx, site/tests/contacts-ui.test.mjs]
autonomous: true
requirements: [REQ-controlled-enrichment, REQ-contact-verification]
must_haves:
  truths: ["Owner sees exact authority/evidence/freshness/identity state before acting.", "Grant creation and granted run are explicit distinct actions with no misleading later-phase controls."]
  artifacts: [{path: site/domain/contacts-handler.ts, provides: owner-admitted contact projections and commands}, {path: site/app/prospects/contacts-workspace.tsx, provides: transport-owning Contacts surface}, {path: site/app/prospects/contact-leaves.tsx, provides: pure safe rendering}]
  key_links: [{from: contacts-workspace.tsx, to: contacts-handler.ts, via: CSRF/idempotent owner transport}, {from: contacts-handler.ts, to: enrichment-operation.ts, via: server-derived command only}]
---
<objective>Expose the owner-only Contacts control surface and closed request boundary. Purpose: show evidence before action without UI-derived authority or live-provider implication. Output: Contacts leaf, safe projections, and UI tests.</objective>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Add closed owner contact transport and projections</name><files>site/domain/contacts-handler.ts, site/tests/contacts-ui.test.mjs</files><behavior>- Server derives workspace/principal and rejects foreign origin, missing CSRF, malformed/oversized body, stale revision, wrong prospect, missing grant, and retry with no call/mutation.
- Create-grant calls the authoritative grant-issuance service using only bounded intent plus server-derived Approved Prospect/current configuration/quote inputs; same idempotency tuple replays the original grant, altered or stale intent conflicts, and grant creation makes zero provider calls.
- Projection separates eligibility, verified points, Contact Suggestions, authority/reservation/reconciliation, and identity impact without raw unsafe content.</behavior><action>Follow established admitted owner handler patterns. Offer explicit create-grant, execute-granted-operation, and merge/split decision commands only; execute calls the existing operation seam and never accepts provider/configuration/authority from client fields. Render external excerpt/content as bounded text and do not surface package/export/call/send/suppression commands.</action><verify><automated>cd site && node --test tests/contacts-ui.test.mjs tests/enrichment-contract.test.mjs</automated></verify><done>All browser mutations are authenticated and fail closed; read model communicates exact state safely.</done></task>
<task type="auto" tdd="true"><name>Task 2: Build accessible read-first Contacts UI</name><files>site/app/prospects/contacts-workspace.tsx, site/app/prospects/contact-leaves.tsx, site/tests/contacts-ui.test.mjs</files><behavior>- Ordered sections are eligibility, verified contacts, Contact Suggestions, authority/identity; exact class/method/source/time/freshness and blocked reasons are visible.
- Copy includes `Contact Suggestion`, `No provider call will be made`, stale Needs review, and uncertain reservation/no-retry; disabled controls have adjacent focusable explanations.</behavior><action>Reuse existing shell/data-owner/pure-leaf styling and native semantic controls/live status. Make grant creation a confirmation screen that displays the server-returned immutable tuple/digest and states it creates no provider request; only a later, separately confirmed Run granted operation action can reach the existing fake-port seam after reservation. Reload the authoritative projection after stale/idempotent/race results. On narrow screens use labelled cards; no component library and no enabled later-governed controls.</action><verify><automated>cd site && node --test tests/contacts-ui.test.mjs</automated></verify><done>UI is keyboard-accessible, enforces a two-stage owner confirmation in which grant issuance cannot execute a provider operation, and cannot imply Contact Suggestions are usable.</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
| browser → contacts handler | Untrusted request cannot select scope or manufacture grant authority. |
| handler → UI | External/provider content is data, not executable markup. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-05-14 | S/E | owner mutations | mitigate | server admission, origin/CSRF/revision/idempotency checks. |
| T-05-15 | I | evidence rendering | mitigate | bounded escaped text and safe projection. |
</threat_model>
<verification>Run UI and authority focused suites.</verification>
<success_criteria>The owner can review all contact authority safely, but no visual control substitutes for a server predicate.</success_criteria>
<output>Create `.planning/phases/05-controlled-enrichment-and-verified-contacts/05-07-SUMMARY.md` only when complete.</output>
