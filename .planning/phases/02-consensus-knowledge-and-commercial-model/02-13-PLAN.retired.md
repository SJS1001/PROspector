---
phase: 02-consensus-knowledge-and-commercial-model
plan: "13"
type: execute
wave: 11
depends_on: ["02-12"]
files_modified: [.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md]
autonomous: true
requirements: [REQ-commercial-hierarchy, REQ-consensus-interview, REQ-versioned-knowledge-and-drift]
user_setup: []
must_haves:
  truths:
    - "Backward-compatible Phase 2 code is deployed to the existing private project while hosted D1 remains schema 0003."
    - "Owner GET is neutral schema-unavailable and every Phase 2 POST is 503 with no state delta or missing-table error."
    - "No gate, project, access policy, secret, migration, upload, or later capability changes."
  artifacts:
    - { path: ".planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md", provides: "Exact compatibility deployment evidence" }
  key_links:
    - { from: "site/domain/knowledge-handler.ts", to: "schema 0003 hosted D1", via: "old-schema safe probe", pattern: "phase2_schema_unavailable" }
---
<objective>Deploy the backward-compatible release behind an absent gate and prove it against the old schema.</objective>
<execution_context>@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/02-consensus-knowledge-and-commercial-model/02-12-SUMMARY.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md
@.openai/hosting.json
@site/domain/knowledge-handler.ts
@site/app/api/knowledge/route.ts
@site/tests/knowledge-handler.test.mjs
<decision_map>D-01 through D-16 remain unavailable because schema 0004 and the gate are absent.</decision_map>
<interfaces>Reuse the exact existing private Sites project and existing bindings. Save/deploy an immutable version tied to the tested source digest.</interfaces></context>
<tasks>
<task type="auto">
  <name>Task 1: Deploy and verify the old-schema compatibility release</name>
  <files>.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md</files>
  <read_first>.planning/phases/02-consensus-knowledge-and-commercial-model/02-12-SUMMARY.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md, .openai/hosting.json, site/domain/knowledge-handler.ts, site/app/api/knowledge/route.ts, site/tests/knowledge-handler.test.mjs</read_first>
  <action>Run full tests/lint/build and capture the reviewed source digest. Using the established Sites build/save/deploy workflow, update only the existing private project; never clone, create a replacement, delete, rename, or expose it. Preserve bindings/access policy and never reveal, rotate, remove, or rewrite secret values. Deploy code that is backward-compatible with schema 0003 and has no gate row. With the existing owner session, verify GET `/api/knowledge` returns neutral no-store `phase2_schema_unavailable` and POST returns 503 `phase2_writes_not_activated` without a missing-table exception or D1/audit delta; verify Phase 1 interview/capability routes remain healthy. Inspect fresh logs for bounded outcomes and no identity, cookie/token, pepper, raw knowledge, source excerpt, workspace/entity ID, object ref, or secret. Record exact source/version/deployment IDs and redacted outcomes.</action>
  <acceptance_criteria>
    - Deployment is the same private project and exact tested source.
    - Hosted route is tested after deployment and before migration; GET/POST fail closed with zero delta.
    - Schema remains 0003 and activation remains absent.
  </acceptance_criteria>
  <verify><automated>cd site &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>The compatibility release is live on the existing private project and safely inert against schema 0003.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Tested source -> private deployment | Version/source drift could bypass compatibility guarantees. |
| New route -> old D1 | Missing tables must fail closed without error leakage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-19 | Tampering | Sites deployment | mitigate | Exact source/version IDs and existing-project-only constraint. |
| T-02-20 | Elevation / Disclosure | old-schema route | mitigate | Neutral GET, 503 POST, absent gate, zero delta, fresh log review. |
| T-02-SC | Tampering | deploy dependencies | mitigate | Existing locked toolchain only. |
</threat_model>
<verification>Full build is green and hosted compatibility outcomes are recorded after deployment.</verification>
<success_criteria>Backward-compatible code is deployed while every Phase 2 hosted write remains impossible.</success_criteria>
<output>Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-13-SUMMARY.md` when done.</output>
