---
phase: 02-consensus-knowledge-and-commercial-model
plan: "05"
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-04"
files_modified:
  - site/domain/commercial-model.ts
  - site/domain/knowledge.ts
autonomous: true
requirements:
  - REQ-commercial-hierarchy
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "The owner can initialize and inspect the exact commercial hierarchy and create Draft Product/Play/Profile entities, while the first Offer can arise only from an explicit confirmed hierarchy-interview decision."
    - "Every research, edit, import, reuse, or quarantined-upload intake is an immutable Proposed Knowledge record until a separate owner decision."
    - "Accept, Correct, and Rescope append immutable Knowledge Versions; Reject records history without creating authority or operational rows."
  artifacts:
    - path: "site/domain/commercial-model.ts"
      provides: "Owner-scoped hierarchy aggregate, fixed seed, scope paths, and Draft creation"
      exports: ["initializeCommercialModel", "readCommercialModel", "createHierarchyDraft", "materializeOfferFromConfirmedHierarchyDecision"]
    - path: "site/domain/knowledge.ts"
      provides: "Proposal/provenance/review/reuse/version authority"
      exports: ["createKnowledgeProposal", "proposeOwnerEdit", "proposeRepositoryResearch", "importPlainText", "proposeReuse", "proposeAllowlistedPackage", "reviewKnowledgeProposal", "readKnowledgeLibrary"]
  key_links:
    - from: "site/domain/commercial-model.ts"
      to: "site/db/schema.ts"
      via: "prepared workspace-rooted D1 statements and exact parent joins"
      pattern: "workspace_id|profile_id"
    - from: "site/domain/knowledge.ts"
      to: "site/tests/knowledge-repository.test.mjs"
      via: "immutable proposal and decision behavior"
      pattern: "reviewKnowledgeProposal"
---

<objective>
Implement the owner-scoped commercial aggregate and proposed/confirmed knowledge authority model.

Purpose: Make hierarchy and knowledge authoritative in domain code without exposing HTTP, uploads, or any later operational workflow.
Output: Tested commercial-model and knowledge domain modules.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@docs/DIRECTION.md
@docs/IMPLEMENTATION-SPEC.md
@docs/adr/0001-generic-company-product-play-model.md
@docs/adr/0002-confirmed-knowledge-and-effective-configuration.md
@site/db/schema.ts
@site/domain/interview.ts
@site/domain/pilot-access.ts
@site/tests/commercial-model-repository.test.mjs
@site/tests/knowledge-repository.test.mjs
@site/tests/helpers/d1.mjs

<decision_map>
D-01 exact hierarchy; D-02 knowledge ownership by Product/Play/Profile/Offer; D-03 Company-wide identity versus scoped relationships; D-04 Product/Market Play divergence rule; D-08 append-only versions/lineage; D-09 Proposed intake/provenance; D-10 separate promotion with no operational creation; D-11 reuse order; D-12 cross-Company exclusions.
</decision_map>

<interfaces>
From site/domain/interview.ts:
`export type InterviewPrincipal = { subject: string; legacySubject: string; displayName: string }`.

New stable contracts:
- CommercialModelProjection contains server-derived workspace/company, nested Product -> MarketPlay -> CustomerProfile -> Offer nodes, scope path, lifecycle, confirmed/proposed/drift counts, and the Company-wide/scoped relationship legend.
- KnowledgeProposalSnapshot binds origin, destination, value, source/custody/privacy/license/reuse metadata, prerequisites, current-version comparison, and digest.
- KnowledgeDecision is the discriminated union accept | reject(reason) | correct(correctedValue, reason) | rescope(destination, reason).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the authoritative commercial aggregate</name>
  <files>site/domain/commercial-model.ts</files>
  <read_first>site/domain/commercial-model.ts (create if absent), site/domain/interview.ts, site/domain/pilot-access.ts, site/db/schema.ts, site/tests/commercial-model-repository.test.mjs, site/tests/helpers/d1.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md, docs/adr/0001-generic-company-product-play-model.md</read_first>
  <behavior>
    - Initializing twice or concurrently yields one Digitalrain Company, ONE Product, ONE for Mining Play, Operating Profile, and Greenfield Draft/nurture Profile.
    - No initialization or generic Draft command creates an Offer; the first Offer requires the exact accepted/corrected/rescoped hierarchy-interview decision lineage under one Profile.
    - Wrong-parent, cross-workspace, stale-revision, and lifecycle-escalation inputs fail with no state delta.
  </behavior>
  <action>Implement CommercialModelConflictError, initializeCommercialModel, readCommercialModel, createHierarchyDraft, and the transaction-only materializeOfferFromConfirmedHierarchyDecision helper using admitted InterviewPrincipal and D1 as injected dependencies. Use `v7()` for independent new entity IDs and server-derived operation digests/idempotency guards. Resolve workspace by principal subject before all other IDs. Seed exactly Digitalrain -> ONE -> ONE for Mining -> Operating plus Greenfield with Draft/nurture presentation; never seed ONE for Marine or an Offer. Permit createHierarchyDraft only for Draft Product under Company, Draft Market Play under Product, and Draft Customer Profile under Play; reject Offer even when a valid Profile locator is supplied. materializeOfferFromConfirmedHierarchyDecision is callable only inside recordInterviewDecision's database transaction and requires an exact stored hierarchy-completion proposal plus accepted/corrected/rescoped decision, Profile destination, question, answer, Knowledge Version, authority-command, and audit references; verify their workspace/digests/revisions and insert the first Draft Offer exactly once. Reject must never call it. Derive Product/Play/Profile ownership categories and scope legend per D-02/D-03 in the projection. Fundamental divergence is represented by a new Product, not a sibling Play, per D-04. Every write uses bound prepared statements, expected revision, an FK-backed authority command, idempotency digest comparison, and returns a fresh authoritative projection. Do not implement Organization, Contact, Account, Target, Prospect, Run, schedule, readiness, discovery, or outbound creation.</action>
  <acceptance_criteria>
    - All commercial repository tests pass against the full migration chain.
    - The projection contains no fixture-only authority and no client-provided workspace root.
    - Parentage, stale revisions, one seed, idempotency, and concurrency are database-backed.
    - Every mutation preserves the forbidden operational snapshot.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/commercial-model-repository.test.mjs</automated>
  </verify>
  <done>The exact commercial model is readable and Draft-editable under owner scope, while generic commands cannot create an Offer and the transaction-only lineage contract is ready for Plan 06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement proposed and confirmed knowledge authority</name>
  <files>site/domain/knowledge.ts</files>
  <read_first>site/domain/knowledge.ts (create if absent), site/domain/commercial-model.ts, site/domain/interview.ts, site/db/schema.ts, site/tests/knowledge-repository.test.mjs, site/tests/helpers/d1.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md, docs/IMPLEMENTATION-SPEC.md, docs/adr/0002-confirmed-knowledge-and-effective-configuration.md</read_first>
  <behavior>
    - Owner edit, safe repository research, bounded plain-text import, same-Company/same-Product reuse, and allowlisted-package intake create Proposed Knowledge with complete immutable provenance and no authority.
    - Accept/Correct/Rescope append one version and exact decision lineage; Reject appends no version.
    - Unscanned uploads and excluded cross-Company categories cannot be reviewed/promoted.
  </behavior>
  <action>Implement KnowledgeConflictError; a closed TrustedKnowledgeOrigin union (`owner_edit`, `repository_research`, `plain_text_import`, `same_company_reuse`, `same_product_reuse`, `allowlisted_package`, `quarantined_upload`); createKnowledgeProposal; proposeOwnerEdit; proposeRepositoryResearch; importPlainText; proposeReuse; proposeAllowlistedPackage; reviewKnowledgeProposal; and readKnowledgeLibrary. Repository research accepts bounded owner-submitted public/authorized source metadata plus plain-text excerpt, validates an HTTPS URL with no credentials, loopback/private/link-local target, fragments, or secret-bearing query keys, and transactionally persists an immutable research_candidate plus Proposed Knowledge; it performs no network fetch and labels the custody as owner-submitted research metadata. Plain-text import accepts bounded UTF-8 text and explicit source/custody/privacy/license/destination fields; it rejects multipart, HTML interpretation, filenames/paths as authority, binary/control-character payloads, and parser dispatch. Reuse resolves candidates server-side in D-11 order; package intake requires a persisted positive allowlist and carries package/license/use restrictions. Construct fixed-order server snapshots and SHA-256 digests from validated destination, bounded value/summary, source/custody/retrieval, privacy, license/use restriction, reuse eligibility, current confirmed comparison, and sorted prerequisite versions/digests. `quarantined_upload` is repository-only metadata for pre-existing opaque custody and remains non-reviewable; expose no runtime creation/release/parser/renderer function because no scanner exists. Enforce D-12 exclusions. Implement the generalized Accept/Reject/Correct/Rescope review transaction with FK-backed expected-revision command guards; Accept/Correct/Rescope append immutable Knowledge Version and current pointer/revision, Reject appends decision/audit only. Preserve predecessor/successor and proposal/decision/audit lineage; never update value_json or configuration manifests in place. Promotion may create knowledge/hierarchy authority only and must not write any forbidden operational table.</action>
  <acceptance_criteria>
    - Knowledge repository tests pass for all origins, bounded repository/import inputs, allowlisted reuse/package decisions, retries, races, and exclusions.
    - Every Proposed/Confirmed projection includes exact destination and provenance; source excerpts are bounded plain text.
    - No upload content is parsed/rendered and no runtime upload function/route is exported.
    - Confirmed version value/digest rows are append-only and every write has zero forbidden-table delta.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-repository.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Every authorized non-upload intake reaches Proposed Knowledge with provenance; review, immutable Confirmed Knowledge, reuse, and fail-closed quarantine satisfy D-08 through D-12 without operational side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client locators -> commercial aggregate | IDs may locate rows but never choose workspace or legal parentage. |
| Untrusted proposal/source metadata -> confirmed authority | Intake can propose but only exact owner review may append authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Elevation | commercial-model.ts | mitigate | Principal-derived workspace, exact parent joins, revision guards, unique constraints, and zero-effect tests. |
| T-02-04 | Tampering | knowledge proposal/version lifecycle | mitigate | Fixed-order server snapshot/digest, immutable rows, separate decision, FK-backed stale guard, and append-only audit. |
| T-02-08 | Information Disclosure / Tampering | upload/package provenance | mitigate | Quarantine opaque uploads, plain bounded metadata only, positive package allowlist, and hard exclusions. |
| T-02-SC | Tampering | uuid runtime use | mitigate | Import only v7 from the exact approved pinned package; no dynamic package loading or custom codec. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/commercial-model-repository.test.mjs tests/knowledge-repository.test.mjs &amp;&amp; npm run lint` passes with shared full-chain/zero-effect fixtures.
</verification>

<success_criteria>
- Exact commercial hierarchy and scope projections are authoritative.
- All knowledge intake remains Proposed until an exact owner decision.
- Immutable versions/reuse/quarantine rules are enforced with no downstream operation.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-05-SUMMARY.md` when done.
</output>
