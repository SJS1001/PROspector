---
phase: 02-consensus-knowledge-and-commercial-model
plan: "07"
type: execute
wave: 5
depends_on:
  - "02-02"
  - "02-05"
files_modified:
  - site/domain/drift.ts
  - site/domain/replacement.ts
autonomous: true
requirements:
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "Knowledge Drift records an explicit source -> version -> configuration -> artifact path and reaches only connected artifacts."
    - "High-risk classification is a closed allowlist and never becomes a Company-wide pause without recorded reach."
    - "Replacement candidate creation and activation are separate exact-digest transactions that preserve prior configurations and create no operational work."
  artifacts:
    - path: "site/domain/drift.ts"
      provides: "Pure risk classification, deterministic graph reach, and immutable impact projection"
      exports: ["classifyDriftRisk", "reachedArtifacts", "buildDriftImpact"]
    - path: "site/domain/replacement.ts"
      provides: "Replacement candidate and activation authority"
      exports: ["createReplacementCandidate", "activateReplacement", "readReplacementState"]
  key_links:
    - from: "site/domain/drift.ts"
      to: "site/db/schema.ts"
      via: "persisted dependency joins and sorted impact snapshot"
      pattern: "configuration_knowledge_dependencies|artifact_configuration_dependencies"
    - from: "site/domain/replacement.ts"
      to: "site/tests/drift-replacement.test.mjs"
      via: "candidate/activation concurrency and zero-effect assertions"
      pattern: "activateReplacement"
---

<objective>
Implement deterministic dependency-scoped drift and immutable replacement authority without executing future-phase effects.

Purpose: Let the owner understand and approve exact knowledge/configuration impact while preserving historical replay and avoiding broad pauses.
Output: Pure drift evaluator and transactional replacement service.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md
@docs/DIRECTION.md
@docs/IMPLEMENTATION-SPEC.md
@docs/adr/0002-confirmed-knowledge-and-effective-configuration.md
@site/db/schema.ts
@site/domain/knowledge.ts
@site/domain/interview.ts
@site/tests/drift-replacement.test.mjs
@site/tests/helpers/d1.mjs

<decision_map>
D-13 exact dependency graph; D-14 high-risk allowlist and reached-only pause; D-15 immutable candidate plus separate activation; D-16 preserve history and record future schedule/in-flight/requalification/invalidation/reactivation impact without performing later-phase work.
</decision_map>

<interfaces>
`DependencyEdge = { fromType; fromId; toType; toId }` and `DriftImpact` must be serializable in stable sorted order. High-risk kinds are exactly `capability`, `proof_point`, `claim_guardrail`, `offer`, and `suppression`; unknown kinds classify as standard/no operational effect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic reached-only drift evaluation</name>
  <files>site/domain/drift.ts</files>
  <read_first>site/domain/drift.ts (create if absent), site/db/schema.ts, site/domain/knowledge.ts, site/tests/drift-replacement.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md, docs/adr/0002-confirmed-knowledge-and-effective-configuration.md</read_first>
  <behavior>
    - Exact allowlisted kinds classify high risk; unknown kinds classify standard with no inferred pause.
    - Traversal returns only sorted artifacts reached through persisted source/version/configuration/artifact edges.
    - Impact snapshots are stable and distinguish affected, not affected, and not present categories.
  </behavior>
  <action>Implement classifyDriftRisk, reachedArtifacts, and buildDriftImpact as pure functions over validated persisted edges. Traverse from challenged source/version only through the normalized dependency graph; deduplicate and sort node IDs and paths before canonical JSON/digest generation. Project current-versus-proposed versions, exact dependency paths, counts by artifact type/status, containment (`no_operational_effect`, `affected_outbound_paused`, or `operational_effects_disabled`), future schedule roll-forward, in-flight historical treatment, affected-unreviewed requalification, dependent approval/package/message invalidation, reactivation requirements, and preserved contacted/exported history as impact categories only. Do not update an artifact by workspace/category filter, and do not import DB/provider/UI code into this pure module.</action>
  <acceptance_criteria>
    - Pure graph tests pass for cycles, duplicates, unrelated branches, empty graphs, and unknown kinds.
    - Same logical graph in different input order produces identical paths and digest.
    - No broad pause or operational state mutation exists in drift.ts.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/drift-replacement.test.mjs --test-name-pattern="drift|reach|risk|impact"</automated>
  </verify>
  <done>Drift risk and impact are deterministic, explicit, and limited to recorded dependency reach.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate replacement candidate and activation transactions</name>
  <files>site/domain/replacement.ts</files>
  <read_first>site/domain/replacement.ts (create if absent), site/domain/drift.ts, site/domain/knowledge.ts, site/domain/interview.ts, site/db/schema.ts, site/tests/drift-replacement.test.mjs, site/tests/helpers/d1.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md, docs/adr/0002-confirmed-knowledge-and-effective-configuration.md</read_first>
  <behavior>
    - Candidate creation persists exact current/candidate digests and immutable impact but leaves candidate inactive.
    - Activation requires a second key plus exact preview/revisions, preserves prior configuration, and converges under retries/races.
    - Activation records containment/invalidation directives only and changes no forbidden operational table.
  </behavior>
  <action>Implement ReplacementConflictError, createReplacementCandidate, activateReplacement, and readReplacementState using admitted principal workspace, `v7()`, prepared D1 statements, authority_commands, and exact idempotency digests. Candidate creation validates current Knowledge Versions/configuration/dependency rows, stores the full buildDriftImpact snapshot+digest, creates immutable candidate configuration data, and leaves it inactive. Activation is a separate transaction that revalidates candidate digest, expected owner/current/candidate revisions, and current dependency snapshot; preserves the prior configuration/history; moves only the active configuration pointer/status; appends configuration_activation, containment directive, and bounded audit rows. Record D-16 effects as immutable directives for later consumers—do not create/update Runs, schedules, Prospects, qualifications, approvals, packages, messages, exports, contacts, or provider work. If no active typed configuration exists, return `no_replacement_required` and leave the accepted Knowledge Version authoritative for future readiness.</action>
  <acceptance_criteria>
    - Candidate/activation/race tests pass and prove exactly one winning activation.
    - Candidate creation never changes the active pointer; activation preserves the prior digest and lineage.
    - Stale preview/dependency/revision conflicts create zero partial rows.
    - Every successful path preserves the forbidden operational snapshot.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/drift-replacement.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Replacement authority is exact, immutable, selective, race-safe, and operationally inert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted dependency graph -> containment decision | Missing or extra edges can under-contain or over-pause future artifacts. |
| Candidate preview -> activation | Stale graph/configuration state must not activate an unreviewed impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | drift traversal | mitigate | Validated edge types, deterministic sorted traversal/digest, synthetic branch/cycle tests, and no broad filters. |
| T-02-10 | Tampering / Repudiation | replacement candidate | mitigate | Persist exact current/candidate/config/dependency revisions and immutable preview digest. |
| T-02-11 | Elevation | activation transaction | mitigate | Separate owner command, FK-backed guard, exact digest revalidation, one active index, zero forbidden-table assertions. |
| T-02-SC | Tampering | dependencies | mitigate | No new package in this plan. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/drift-replacement.test.mjs &amp;&amp; npm run lint` passes with full-chain and forbidden-table helpers.
</verification>

<success_criteria>
- Drift impact is reproducible and reached-only.
- Replacement candidate and activation are separate exact transactions.
- History is preserved and no future-phase work is created or authorized.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-07-SUMMARY.md` when done.
</output>
