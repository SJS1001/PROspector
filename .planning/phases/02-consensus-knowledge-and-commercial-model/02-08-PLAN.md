---
phase: 02-consensus-knowledge-and-commercial-model
plan: "08"
type: execute
wave: 6
depends_on:
  - "02-05"
  - "02-06"
  - "02-07"
files_modified:
  - site/domain/knowledge-handler.ts
  - site/app/api/knowledge/route.ts
autonomous: true
requirements:
  - REQ-commercial-hierarchy
  - REQ-consensus-interview
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "Only the configured owner can read or mutate Phase 2 authority, and every client scope/workspace value remains a locator."
    - "All mutation commands pass same-origin, intent, bounded JSON, one-time CSRF, closed-schema, and Phase 2 activation checks before domain dispatch."
    - "Hosted writes fail closed while the Phase 2 activation gate is absent, without blocking local gated tests or authorizing any later capability."
  artifacts:
    - path: "site/domain/knowledge-handler.ts"
      provides: "Secure owner-only read and closed-command mutation boundary"
      exports: ["handleKnowledgeGet", "handleKnowledgePost", "KnowledgeHandlerDependencies"]
    - path: "site/app/api/knowledge/route.ts"
      provides: "Thin Cloudflare binding and trusted identity wiring"
      exports: ["GET", "POST"]
  key_links:
    - from: "site/app/api/knowledge/route.ts"
      to: "site/domain/knowledge-handler.ts"
      via: "provider-only dependency injection"
      pattern: "handleKnowledge(Get|Post)"
    - from: "site/domain/knowledge-handler.ts"
      to: "site/domain/pilot-access.ts"
      via: "admission before gate/query/CSRF/body work"
      pattern: "admitPilotOwner"
---

<objective>
Expose the complete Phase 2 read model and closed owner-command set through the proven security boundary.

Purpose: Connect domain authority to HTTP without trusting browser scope, leaking private state, or enabling hosted writes before accepted proof.
Output: Secure knowledge handler and thin route, green handler/interview security tests.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-VALIDATION.md
@docs/IMPLEMENTATION-SPEC.md
@site/domain/interview-handler.ts
@site/app/api/interview/route.ts
@site/domain/pilot-access.ts
@site/domain/request-security.ts
@site/domain/csrf.ts
@site/domain/commercial-model.ts
@site/domain/knowledge.ts
@site/domain/interview.ts
@site/domain/drift.ts
@site/domain/replacement.ts
@site/tests/knowledge-handler.test.mjs
@site/tests/interview-handler.test.mjs

<decision_map>
All D-01 through D-16 domain decisions are exposed only through admitted projections and the closed actions listed below. The phase boundary forbids upload activation, discovery, prospecting, enrichment, exports, spend, Gmail, calling, scheduling, Runners, or outbound effects.
</decision_map>

<interfaces>
Mirror `InterviewHandlerDependencies`: database, subjectPepper, pilotOwnerEmail, getIdentity(). Add no browser identity source. `handleKnowledgeGet` returns one no-store authoritative workspace projection. `handleKnowledgePost` accepts exactly this twelve-command discriminated union and nothing else: initialize_commercial_model, create_hierarchy_draft, propose_owner_edit, propose_repository_research, import_plain_text, propose_reuse, propose_allowlisted_package, submit_interview_answer, record_interview_decision, review_knowledge_proposal, create_replacement_candidate, activate_replacement. It returns the refreshed projection plus a new one-time HttpOnly CSRF cookie.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement admitted, gated knowledge reads and commands</name>
  <files>site/domain/knowledge-handler.ts</files>
  <read_first>site/domain/knowledge-handler.ts (create if absent), site/domain/interview-handler.ts, site/domain/pilot-access.ts, site/domain/request-security.ts, site/domain/csrf.ts, site/domain/commercial-model.ts, site/domain/knowledge.ts, site/domain/interview.ts, site/domain/drift.ts, site/domain/replacement.ts, site/db/schema.ts, site/tests/knowledge-handler.test.mjs, site/tests/interview-handler.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, docs/IMPLEMENTATION-SPEC.md</read_first>
  <behavior>
    - Authorized GET returns only admitted workspace hierarchy/interview/library/drift/replacement projection with no-store and fresh CSRF cookie.
    - Missing/outsider identity is neutral before any D1/CSRF/body work; inactive Phase 2 gate makes every POST return 503 with zero delta.
    - Exact closed commands dispatch field-by-field and preserve CSRF/replay/stale/conflict/forbidden-effect guarantees.
  </behavior>
  <action>Implement KnowledgeHandlerDependencies, handleKnowledgeGet, and handleKnowledgePost by copying the established admission/error/response shape, not the single-question dispatch assumptions. Authenticate via getIdentity -> admitPilotOwner before any schema, gate, repository, CSRF, or body operation. Probe migration state with an old-schema-safe query: before 0004, GET returns neutral no-store `phase2_schema_unavailable` with no private projection and POST returns 503 without referencing a missing Phase 2 table. After 0004, GET assembles commercial model, interview, Proposed/Confirmed library, drift, and replacement projections and issues a one-time HttpOnly cookie. POST validates same origin/fetch metadata, exact `knowledge-mutation` intent, JSON, 8 KiB limit, consumes the one-time cookie, then requires a matching accepted `phase_activation_gates` row for `consensus_knowledge`; absent/stale/mismatched gate returns 503 `phase2_writes_not_activated`. Dispatch exactly initialize_commercial_model, create_hierarchy_draft, propose_owner_edit, propose_repository_research, import_plain_text, propose_reuse, propose_allowlisted_package, submit_interview_answer, record_interview_decision, review_knowledge_proposal, create_replacement_candidate, and activate_replacement. create_hierarchy_draft rejects Offer; only record_interview_decision may materialize the first Offer through the exact accepted/corrected/rescoped hierarchy-completion transaction and lineage contract. Repository source resolution and reuse/package lookup stay server-side; the browser supplies bounded text/metadata and locators only. import_plain_text accepts bounded UTF-8 text plus explicit provenance/destination and creates Proposed Knowledge only; reject multipart/file upload, import_batch promotion, filename/path authority, HTML/binary/control content, parser dispatch, and operational import effects. Reconstruct all typed inputs field-by-field and reject unknown authority-bearing fields, origin/source scan status, and workspace claims. Map admission 404, CSRF 403, unsupported/invalid 400, size 413, domain stale/conflict 409, unavailable schema/inactive gate 503, and unexpected opaque 500; every response is no-store/nosniff. Add no runtime file upload, readiness, discovery, operational, or provider action.</action>
  <acceptance_criteria>
    - The complete handler test matrix passes and denied requests produce zero D1 changes.
    - Positive local tests work only after seeding the exact Phase 2 activation fixture and cover repository research, bounded plain-text import, reuse, and allowlisted-package Proposed Knowledge.
    - The deployed code is safe against schema 0003: GET is neutral and POST is 503 without a missing-table exception.
    - No error body leaks company, hierarchy, counts, sources, versions, IDs, or configuration state.
    - Every successful command returns a fresh authoritative projection and forbidden-table zero delta.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-handler.test.mjs tests/interview-handler.test.mjs</automated>
  </verify>
  <done>The handler securely exposes Phase 2 authority locally while hosted writes remain fail-closed until the external gate is truly satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the thin Cloudflare knowledge route</name>
  <files>site/app/api/knowledge/route.ts</files>
  <read_first>site/app/api/knowledge/route.ts (create if absent), site/app/api/interview/route.ts, site/app/chatgpt-auth.ts, site/domain/knowledge-handler.ts, site/tests/knowledge-handler.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md</read_first>
  <action>Create a force-dynamic GET/POST route that imports `env` from `cloudflare:workers`, `getChatGPTUser`, and the knowledge handler. Require the existing DB, OWNER_SUBJECT_PEPPER, and PILOT_OWNER_EMAIL bindings unchanged; inject only database, secret strings, and trusted identity callback. Keep all admission, gate, parsing, business, scope, and error decisions in knowledge-handler.ts. Do not add a Phase 2 enablement environment flag, client-header identity, R2/scanner binding, scheduler, Runner, Gmail, provider, or export binding.</action>
  <acceptance_criteria>
    - Route exports force-dynamic GET and POST and delegates to the handler; it remains deployable before migration 0004.
    - Source contains getChatGPTUser and no client authentication/workspace headers.
    - Missing secure bindings fail closed without a fallback identity or database.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-handler.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>The provider route is shallow, server-only, and cannot bypass admitted/gated domain authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trusted Sites identity -> owner admission | Only the configured owner may reach any Phase 2 projection or command. |
| Browser request -> domain command | Origin, replay, body, scope, and activation claims are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing / Elevation | knowledge-handler.ts | mitigate | Admission first, server-derived workspace, neutral 404, exact local gate row, and cross-owner zero-delta tests. |
| T-02-07 | Tampering | mutation request | mitigate | Origin/fetch/intent/JSON/8KiB/one-time cookie checks and closed field-by-field discriminants. |
| T-02-08 | Information Disclosure | handler errors/projections | mitigate | no-store/nosniff, opaque errors, neutral denial, and no raw source/private body logging. |
| T-02-13 | Elevation | hosted Phase 2 gate | mitigate | No env-presence authority; every POST requires the exact accepted D1 gate row, which this plan never creates. |
| T-02-SC | Tampering | dependencies | mitigate | No new package in this plan. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/knowledge-handler.test.mjs tests/interview-handler.test.mjs &amp;&amp; npm run lint` passes; source review confirms no later-effect imports or activation bypass.
</verification>

<success_criteria>
- Owner-only, same-origin, one-time-CSRF Phase 2 HTTP behavior is green.
- Hosted writes remain inactive without accepted evidence.
- Safe repository research, bounded owner plain-text import, and allowlisted reuse/package proposals are reachable; no route can upload files, discover, prospect, enrich, export, spend, schedule, call, send, or invoke a Runner/provider.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-08-SUMMARY.md` when done.
</output>
