---
phase: 02-consensus-knowledge-and-commercial-model
plan: "11"
type: execute
wave: 9
depends_on:
  - "02-04"
  - "02-08"
  - "02-10"
files_modified:
  - site/scripts/phase2-hosted-preflight.mjs
  - site/tests/phase2-hosted-preflight.test.mjs
  - site/scripts/phase2-gate.mjs
  - site/tests/phase2-gate.test.mjs
  - .planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md
autonomous: true
requirements: [REQ-commercial-hierarchy, REQ-consensus-interview, REQ-versioned-knowledge-and-drift]
user_setup: []
must_haves:
  truths:
    - "Old-schema and post-0004 hosted state can be proved with fixed read-only queries and redacted output."
    - "The only writable release helper can insert one consensus_knowledge gate after validating the complete eight-field authorization tuple; it cannot accept SQL or another capability."
    - "The evidence ledger starts blocked and documentation can never grant authority."
  artifacts:
    - { path: "site/scripts/phase2-hosted-preflight.mjs", provides: "Read-only old/post-migration proof CLI" }
    - { path: "site/scripts/phase2-gate.mjs", provides: "Fixed consensus_knowledge activation CLI" }
    - { path: ".planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md", provides: "Fail-closed release evidence ledger" }
  key_links:
    - { from: "site/scripts/phase2-hosted-preflight.mjs", to: "hosted D1", via: "fixed Wrangler argument arrays", pattern: "old-schema|post-migration" }
    - { from: "site/scripts/phase2-gate.mjs", to: "phase_activation_gates", via: "fixed scoped insert", pattern: "consensus_knowledge" }
---

<objective>
Build the safe release tooling and blocked evidence ledger before touching hosted state.

Purpose: Make the ordered hosted release executable without arbitrary SQL, secret exposure, or documentary authority.
Output: Tested read-only preflight, tested narrow gate writer, and blocked activation ledger.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-private-pilot-boundary/01-HOSTED-PROOF.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-VALIDATION.md
@docs/adr/0004-private-sites-pilot-and-portability.md
@site/scripts/hosted-boundary-proof.mjs
@site/tests/hosted-boundary-proof.test.mjs
@site/tests/migration-chain.test.mjs
@site/drizzle/0004_consensus_knowledge.sql
@site/domain/knowledge-handler.ts

<decision_map>D-01 through D-16 remain inert until hosted proof and separately authorized consensus_knowledge activation complete. Runtime file upload and every later capability remain disabled.</decision_map>
<interfaces>The preflight accepts only `--mode old-schema|post-migration|inspect-gate` and a validated database name/ID. The gate CLI accepts only `inspect` or `activate` and this exact complete tuple: `capability`, `authorization_reference`, `target_project_deployment`, `reviewed_source_digest`, `migration_identity_status`, `post_migration_evidence_reference`, `independent_review_reference`, `deployed_boundary_proof_reference`. It computes one fixed-order SHA-256 tuple digest; no alias, omitted field, or extra authority field is accepted.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixed read-only hosted evidence queries</name>
  <files>site/scripts/phase2-hosted-preflight.mjs, site/tests/phase2-hosted-preflight.test.mjs</files>
  <read_first>site/scripts/hosted-boundary-proof.mjs, site/tests/hosted-boundary-proof.test.mjs, site/tests/migration-chain.test.mjs, site/drizzle/0004_consensus_knowledge.sql, site/domain/knowledge-handler.ts, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md</read_first>
  <behavior>
    - old-schema requires exactly migrations 0000-0003, preserves historian counts/digests/FKs, and proves no Phase 2 gate/table assumption.
    - post-migration requires exactly 0000-0004, empty foreign_key_check, identical protected counts/digests, valid binding/quarantine counts, and absent gate.
    - Output is statuses/counts/timestamps/migration IDs/opaque digests only; every mismatch exits nonzero and blocked.
  </behavior>
  <action>Implement project-local Wrangler invocation with argument arrays and fixed `d1 migrations list` plus internally defined SELECT/PRAGMA statements. Accept no SQL/command option. Validate database identifiers against a conservative allowlist and reject control characters. Old-schema mode must run before deployment/migration and must not reference 0004 tables. Post-migration mode verifies additive lineage, complete forbidden operational counts, and absence of an accepted consensus_knowledge gate. Tests use fake spawn/local fixtures to prove read-only verbs, redaction, mismatch blocking, and rejection of INSERT/UPDATE/DELETE/ALTER/DROP/CREATE/REPLACE/ATTACH/DETACH/VACUUM. Never print values, rows, emails, IDs, cookies, tokens, object refs, source excerpts, or secrets.</action>
  <acceptance_criteria>
    - Both proof modes have deterministic allowlisted fields and under-30-second local tests.
    - Old-schema mode cannot accidentally require 0004; post-migration mode cannot pass without 0004 and unchanged protected digests.
    - No caller-controlled SQL or shell interpolation exists.
  </acceptance_criteria>
  <verify><automated>cd site &amp;&amp; node --test tests/phase2-hosted-preflight.test.mjs &amp;&amp; node scripts/phase2-hosted-preflight.mjs --help</automated></verify>
  <done>A redacted, fixed, read-only proof tool covers both sides of migration 0004.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the separately authorized narrow gate writer</name>
  <files>site/scripts/phase2-gate.mjs, site/tests/phase2-gate.test.mjs</files>
  <read_first>site/drizzle/0004_consensus_knowledge.sql, site/domain/knowledge-handler.ts, site/scripts/phase2-hosted-preflight.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md</read_first>
  <behavior>
    - inspect is read-only and reports absent/exact/overbroad without values.
    - activate refuses unless all eight canonical fields are present and valid: capability, authorization reference, target project/deployment, reviewed source, migration identity/status, post-migration evidence, independent-review reference, and deployed-boundary proof.
    - Activation is idempotent only for the same fixed-order eight-field tuple digest and conflicts when any field differs; no delete/update/deactivate or arbitrary SQL exists.
  </behavior>
  <action>Build a fixed D1 CLI that can inspect or insert exactly one accepted `consensus_knowledge` gate using bound values and project-local Wrangler argument arrays. Parse and validate exactly the eight canonical tuple fields named in the interface, verify each against the owning evidence artifact, serialize them in that fixed order, and compute/store the tuple digest. Reject every other capability, missing/blank/sentinel field, source/migration/evidence/review/boundary/project mismatch, pre-existing partial or overbroad gate, extra authority field, and any SQL/command parameter. Same complete tuple retry is idempotent; changing any one field conflicts and performs no update/insert. The activate path is never run in this plan; Plan 19 invokes it only after Plan 18 records explicit authorization. Tests prove the generated statement permits only bounded UTF-8 import_plain_text to Proposed Knowledge within consensus_knowledge. Precisely reject multipart/file upload, import_batch promotion, filename/path authority, HTML/binary parser dispatch, and operational import effects, as well as discovery, prospecting, contacts, schedules, exports, credentials, paid work, Runners, Gmail, calling, messaging, or outbound effects.</action>
  <acceptance_criteria>
    - Local tests prove exact-scope/idempotency/conflict behavior and no secret/raw-data output.
    - Tests cover all eight tuple fields, every single-field omission/difference, same-tuple retry, bounded import_plain_text permission, and rejection of every excluded file/import behavior.
    - No activation can occur from environment-variable presence, ledger status, or automated evidence alone.
    - This task performs zero hosted writes.
  </acceptance_criteria>
  <verify><automated>cd site &amp;&amp; node --test tests/phase2-gate.test.mjs &amp;&amp; node scripts/phase2-gate.mjs --help</automated></verify>
  <done>A fixed, testable gate writer exists but cannot run without later explicit authorization.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize the non-authorizing release ledger</name>
  <files>.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md</files>
  <read_first>.planning/STATE.md, .planning/phases/01-private-pilot-boundary/01-HOSTED-PROOF.md, site/scripts/phase2-hosted-preflight.mjs, site/scripts/phase2-gate.mjs, docs/adr/0004-private-sites-pilot-and-portability.md</read_first>
  <action>Create status `blocked` sections in exact order: Phase 1 real-principal prerequisite; old-schema preflight; compatibility deployment; additive 0004; post-migration proof; independent review; exact-source deployment; post-deploy real-principal/negative/log proof; explicit consensus_knowledge authorization; gate activation; owner lifecycle. Record only statuses, timestamps, source/deployment/migration identifiers, counts, and opaque digests/references. State that `human_needed` pauses and never completes a task, the ledger never grants authority, the existing private Sites project must be reused without clone/replace/delete/public exposure, and secrets are never revealed/rotated/removed. Keep arbitrary file upload and every later capability disabled.</action>
  <acceptance_criteria>
    - Every release stage begins pending/blocked and has a non-authorizing evidence field.
    - Prohibited project/secret/upload/capability side effects are explicit.
    - No completion language permits absent human evidence.
  </acceptance_criteria>
  <verify><automated>rg -n "status: blocked|human_needed.*pause|existing private Sites project|consensus_knowledge|arbitrary file upload|never grants authority" .planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md</automated></verify>
  <done>The release ledger truthfully starts blocked and cannot be mistaken for an activation mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local CLI -> hosted D1 | Fixed reads and one separately authorized scoped insert cross the control plane. |
| Evidence ledger -> authority | Documentation is not executable authorization. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-14 | Tampering / Disclosure | preflight | mitigate | Fixed read allowlist, argument arrays, redacted schema, nonzero mismatch. |
| T-02-15 | Elevation | gate writer | mitigate | Exact eight-field tuple, fixed-order digest, same-tuple-only idempotency, single-field mismatch conflict, and no generic SQL. |
| T-02-16 | Repudiation | activation ledger | mitigate | Ordered status/evidence references and explicit non-authority semantics. |
| T-02-SC | Tampering | dependencies | mitigate | Existing Node/Wrangler only; no install. |
</threat_model>

<verification>Run both focused test files, both `--help` commands, `npm test`, and `npm run lint`; confirm no remote mode or gate activation ran.</verification>
<success_criteria>Release tooling is safe, scoped, redacted, and blocked by default.</success_criteria>
<output>Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-11-SUMMARY.md` when done.</output>
