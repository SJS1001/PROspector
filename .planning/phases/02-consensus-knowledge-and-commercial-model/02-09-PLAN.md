---
phase: 02-consensus-knowledge-and-commercial-model
plan: "09"
type: execute
wave: 7
depends_on:
  - "02-05"
  - "02-06"
  - "02-07"
  - "02-08"
files_modified:
  - site/app/knowledge/commercial-model.tsx
  - site/app/knowledge/consensus-interview.tsx
  - site/app/knowledge/knowledge-library.tsx
  - site/app/knowledge/drift-replacements.tsx
autonomous: true
requirements:
  - REQ-commercial-hierarchy
  - REQ-consensus-interview
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "The owner can understand exact commercial scope, one interview decision, Proposed versus Confirmed authority, and reached-only drift before acting."
    - "Every leaf receives typed projections, logical command callbacks, and stable operation keys from KnowledgeWorkspace and renders authoritative conflict/unknown outcomes without optimistic success."
    - "Source and imported content render only as text, while all later operational actions remain absent or natively disabled with visible reasons."
  artifacts:
    - path: "site/app/knowledge/commercial-model.tsx"
      provides: "Exact nested hierarchy, detail, scope legend, and Draft creation UI"
    - path: "site/app/knowledge/consensus-interview.tsx"
      provides: "One-question, two-stage, four-decision interview UI"
    - path: "site/app/knowledge/knowledge-library.tsx"
      provides: "Proposed and Confirmed Knowledge review/provenance UI"
    - path: "site/app/knowledge/drift-replacements.tsx"
      provides: "Drift, impact preview, candidate, activation, and history UI"
  key_links:
    - from: "site/app/knowledge/consensus-interview.tsx"
      to: "site/app/knowledge/knowledge-workspace.tsx"
      via: "typed projection plus logical answer/decision callbacks and workspace-owned stable operation keys"
      pattern: "onSubmitAnswer|onRecordDecision"
    - from: "site/app/knowledge/drift-replacements.tsx"
      to: "site/domain/replacement.ts"
      via: "server projection types and exact candidate/activation references"
      pattern: "Candidate.*not active|Activate replacement"
---

<objective>
Build the four authoritative Knowledge leaf views against the secure server projection.

Purpose: Present scope, evidence, proposal authority, concurrency, drift, and replacement consequences exactly as the owner must review them.
Output: Four tested semantic React components; shell composition and global CSS arrive in Plan 10.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@site/app/prospector-app.tsx
@site/app/globals.css
@site/domain/commercial-model.ts
@site/domain/knowledge.ts
@site/domain/interview.ts
@site/domain/drift.ts
@site/domain/replacement.ts
@site/domain/knowledge-handler.ts
@site/tests/knowledge-ui.test.mjs

<decision_map>
D-01 through D-04 drive Commercial Model scope; D-05 through D-08 drive Interview; D-09 through D-12 drive Knowledge Library; D-13 through D-16 drive Drift &amp; Replacements. The UI never creates authority locally or enables deferred operational work.
</decision_map>

<interfaces>
Consume the handler's authoritative projection types directly; do not re-declare looser client authority types. Every leaf receives only typed state, exact logical command callbacks, and workspace-supplied stable operation keys. `KnowledgeWorkspace` alone constructs `{ action, expectedRevision(s), idempotencyKey, exact action fields }`, performs same-origin/no-store transport, owns CSRF, key generation/reuse, unknown-outcome recovery, retry, and response normalization. Unknown/malformed projections hide mutation controls. No leaf may call fetch, read cookies/CSRF, generate keys, retry, or normalize responses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the exact Commercial Model view</name>
  <files>site/app/knowledge/commercial-model.tsx</files>
  <read_first>site/app/knowledge/commercial-model.tsx (create if absent), site/app/prospector-app.tsx, site/app/globals.css, site/domain/commercial-model.ts, site/domain/knowledge-handler.ts, site/tests/knowledge-ui.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md</read_first>
  <action>Implement a pure typed projection component: props contain the authoritative commercial projection plus logical callbacks such as `onCreateDraft`/`onProposeChange`; this leaf performs no fetch, cookie/CSRF access, idempotency generation, retry, or response normalization. Render a nested semantic list for Company -> Product -> Market Play -> Customer Profile -> Offer with independent disclosure/selection buttons and stable database IDs as keys. Render entity type, name, parent path, lifecycle, confirmed/proposed/drift counts, D-02 ownership categories, and the persistent Company-wide identity versus Play/Profile-scoped relationship legend. Scope links/buttons are at least 44px and current scope uses aria-current. Callback payloads carry expected revision and exact logical fields while KnowledgeWorkspace supplies/owns the stable operation key. Create Draft Product/Play/Profile only; expose no direct Add/Create Offer command. When no Offer exists, direct the owner to the hierarchy-completion interview; the first Offer appears only after its accepted/corrected/rescoped decision and the projection exposes the decision/Knowledge Version/audit lineage. Editing confirmed fields says `Propose change`. Show no unspecified Offer seed, no fixture authority, no Ready label, and no Add account/contact/prospect, discovery, schedule, or other operational command. If a retained fixture control appears, it must be natively disabled with adjacent `Available in a later governed phase.`</action>
  <acceptance_criteria>
    - The five-level nesting and Profile-parented Offer are semantic and server-derived.
    - Scope, lifecycle, counts, and legend are visible text and never inferred from the last edit.
    - Product/Play/Profile draft operations keep the workspace-supplied idempotency key after unknown outcomes; no generic Offer callback/control or operational control exists.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-ui.test.mjs --test-name-pattern="commercial|hierarchy|scope"</automated>
  </verify>
  <done>The commercial hierarchy and scope ownership are understandable and editable only within Phase 2 authority.</done>
</task>

<task type="auto">
  <name>Task 2: Build the one-question two-stage Consensus Interview view</name>
  <files>site/app/knowledge/consensus-interview.tsx</files>
  <read_first>site/app/knowledge/consensus-interview.tsx (create if absent), site/app/prospector-app.tsx, site/domain/interview.ts, site/domain/knowledge-handler.ts, site/tests/knowledge-ui.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md</read_first>
  <action>Build a pure typed projection/callback component with no direct fetch, cookie/CSRF access, key generation, retries, or response normalization. Render fixed order: QUESTION N/scope/revision/destination/prerequisites; one h2 question; separate Evidence cards with source/date/retrieval/excerpt/ref; Inference card with `This is an inference, not a sourced fact.`; recommendation; answer controls; and the separate-confirmation boundary note. Stage 1 offers Use recommendation, Write correction, and Change scope with required fields, and CTA `Submit answer for confirmation`; keep the snapshot visible while pending. Stage 2 heading is `Confirm submitted answer`, shows exact stored answer/proposal digest, question revision, prerequisites, destination, and note from 02-UI-SPEC.md, then exactly Accept, Reject, Correct, Rescope with required payloads. For hierarchy_completion_offer, visibly state that no Offer exists yet and only Accept/Correct/Rescope of this exact stored proposal will create the first Offer under the displayed Profile; Reject creates none. After success, render the returned Offer plus question/answer/decision/Knowledge Version/audit references. Emit distinct logical callbacks for answer and decision; KnowledgeWorkspace supplies and owns separate stable idempotency keys and unknown-outcome recovery. Render all specified stale/other-tab/superseded/network-unknown/malformed states from typed props; focus Stage 2 only after explicit transition. Never use a generic Confirm/Save label, optimistic question number, chat transcript, or auto-retry mutation.</action>
  <acceptance_criteria>
    - Evidence, inference, recommendation, answer, and confirmed result are distinct semantic regions.
    - Exact Stage 2 snapshot remains visible and all four decision contracts/copy are present.
    - Concurrency/unknown states match 02-UI-SPEC.md and only explicit read-only refresh checks authority.
    - Source excerpts are JSX text; no raw HTML path exists; hierarchy-completion Offer creation and lineage are explicit in the Stage 2/result states.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-ui.test.mjs --test-name-pattern="interview|question|confirmation|conflict"</automated>
  </verify>
  <done>The owner can answer and separately decide one exact researched question without ambiguous or optimistic authority.</done>
</task>

<task type="auto">
  <name>Task 3: Build Knowledge Library and Drift &amp; Replacements views</name>
  <files>site/app/knowledge/knowledge-library.tsx, site/app/knowledge/drift-replacements.tsx</files>
  <read_first>site/app/knowledge/knowledge-library.tsx (create if absent), site/app/knowledge/drift-replacements.tsx (create if absent), site/app/prospector-app.tsx, site/domain/knowledge.ts, site/domain/drift.ts, site/domain/replacement.ts, site/domain/knowledge-handler.ts, site/tests/knowledge-ui.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md</read_first>
  <action>Build both leaves as pure typed projections/callbacks with stable database keys supplied by KnowledgeWorkspace and no fetch, cookie/CSRF access, key generation, retry, or response normalization. Knowledge Library uses Proposed/Confirmed text tabs with counts, filters, exact empty states, and visible destination on every card. Add closed intake panels for safe repository research (bounded HTTPS source metadata, retrieval/publication time, plain-text excerpt, public/owner-authorized custody, destination, and explicit copy that the app records but does not fetch the URL), bounded UTF-8 import_plain_text (visible size/type limits and explicit provenance/destination), same-Company/same-Product reuse, and allowlisted-package reuse; each CTA says it creates Proposed Knowledge and emits only the matching logical callback. The import panel exposes no multipart/file picker, drag/drop, import_batch promotion, filename/path authority, HTML/parser choice, scanner/quarantine release, or operational import control. Proposed detail shows origin, complete provenance/custody/privacy/license/reuse, current difference, immutable ref, and `Proposed Knowledge has no authority until you promote it.`; Review proposal uses exact snapshot and four decisions. Confirmed detail is read-only and shows value/scope/kind/version/decision/Toronto time/audit/provenance/supersedes/dependencies plus `Propose change`. Drift view lists unresolved first, then candidates, then activation history; cards show Standard/High-risk label, current/proposed, provenance, explicit dependency paths, counts/status, exact containment, and four decisions. Impact preview renders each D-16 category as Affected/Not affected/Not present; candidate CTA is `Create replacement candidate`, status `Candidate — not active`, and activation is a second surface with exact boundary note/CTA `Activate replacement`. Success still leaves all operational controls absent/disabled. Use role=alert for conflicts, polite live region for explicit success, plain JSX text, and no modal or animated graph.</action>
  <acceptance_criteria>
    - Proposed and Confirmed authority/copy cannot be confused and no confirmed value is editable.
    - Repository research, bounded plain-text import, same-Company/same-Product reuse, and allowlisted-package intake are visible owner actions that create Proposed Knowledge only.
    - Cross-Company excluded categories and unscanned uploads are never promotable/rendered as content; arbitrary file upload has no UI.
    - Drift paths/counts/containment are exact and candidate/activation are visibly separate.
    - All empty/error/stale/unknown states and native disabled reasons are present.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/knowledge-ui.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>The owner can review proposal/version lineage and exact drift/replacement impact without UI-created authority or downstream activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server projection -> browser state | The browser must not infer or retain authority beyond verified responses. |
| Source/import text -> DOM | Untrusted text must never become active HTML/script. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure / Tampering | all four UI components | mitigate | Typed authoritative projection, escaped text, neutral/malformed hiding, no dangerous HTML, and exact source refs only. |
| T-02-07 | Tampering | interview/proposal mutations | mitigate | Stable logical keys, pending lockout, exact snapshots, no optimistic success, and read-only unknown-outcome recovery. |
| T-02-10 | Repudiation | drift/replacement review | mitigate | Display exact versions/digests/paths/impact/audit refs before separate candidate and activation actions. |
| T-02-SC | Tampering | UI dependencies | mitigate | No component/icon/registry package is added. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/knowledge-ui.test.mjs &amp;&amp; npm run lint` passes; source search finds no `dangerouslySetInnerHTML`, component library, raw upload parsing, or enabled later-phase control.
</verification>

<success_criteria>
- All four leaf views match the approved semantics/copy/state contract.
- Exact authority and conflict state remains server-driven.
- UI introduces no external or operational effect.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-09-SUMMARY.md` when done.
</output>
