---
phase: 02-consensus-knowledge-and-commercial-model
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - site/package.json
  - site/package-lock.json
  - site/db/schema.ts
  - site/drizzle/0004_consensus_knowledge.sql
  - site/drizzle/meta/0004_snapshot.json
  - site/drizzle/meta/_journal.json
autonomous: true
requirements:
  - REQ-commercial-hierarchy
  - REQ-consensus-interview
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "The database enforces the exact commercial parentage, one-current authority, immutable decision/version lineage, and explicit dependency graph."
    - "Migration 0004 preserves every existing historian value, digest, decision, and audit reference while binding valid authority exactly once and quarantining unbound authority."
    - "Runtime upload content remains quarantined and unusable; schema presence never enables file upload or an operational workflow."
  artifacts:
    - path: "site/db/schema.ts"
      provides: "Normalized commercial, proposal, decision, dependency, drift, replacement, and activation-gate schema"
    - path: "site/drizzle/0004_consensus_knowledge.sql"
      provides: "Additive schema/backfill/constraint migration"
    - path: "site/package.json"
      provides: "Pinned audited UUIDv7 dependency"
      contains: "\"uuid\": \"14.0.1\""
  key_links:
    - from: "site/drizzle/0004_consensus_knowledge.sql"
      to: "site/tests/migration-chain.test.mjs"
      via: "full-chain backfill and invariant assertions"
      pattern: "knowledge_items|proposal_decisions|replacement_candidates"
    - from: "site/db/schema.ts"
      to: "site/drizzle/0004_consensus_knowledge.sql"
      via: "Drizzle declaration plus reviewed additive SQL"
      pattern: "companies|offers|knowledgeProposals"
---

<objective>
Install the explicitly approved UUIDv7 package and establish the additive Phase 2 authority schema/backfill.

Purpose: Put hierarchy, immutable knowledge decisions, concurrency guards, dependency-scoped drift, and hosted-write activation behind database-enforced invariants without risking existing historian authority.
Output: Pinned package metadata, normalized Drizzle declarations, migration 0004, and green full-chain migration tests.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-VALIDATION.md
@docs/IMPLEMENTATION-SPEC.md
@docs/adr/0001-generic-company-product-play-model.md
@docs/adr/0002-confirmed-knowledge-and-effective-configuration.md
@site/package.json
@site/package-lock.json
@site/db/schema.ts
@site/drizzle.config.ts
@site/drizzle/0000_jittery_meteorite.sql
@site/drizzle/0001_true_spencer_smythe.sql
@site/drizzle/0002_eager_supreme_intelligence.sql
@site/drizzle/0003_acoustic_magik.sql
@site/tests/helpers/d1.mjs
@site/tests/migration-chain.test.mjs

<decision_map>
D-01 through D-04 define hierarchy and scope. D-06 through D-08 require exact immutable decisions, one live authority, and retained lineage. D-09 through D-12 require proposal/provenance/reuse records and exclusions. D-13 through D-16 require dependency, drift, candidate, activation, history, and selective containment records. The phase boundary prohibits any schema trigger or route from starting later operational work.
</decision_map>

<interfaces>
Existing schema exports workspaces, products, marketPlays, profiles, knowledgeVersions, configurations, interviewSessions, interviewQuestions, interviewAnswers, interviewConfirmations, csrfTokens, importBatches, importItems, prospects, suppressions, and auditEvents. Preserve their current Phase 1 columns and runtime compatibility while adding authoritative relations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the approved UUIDv7 dependency</name>
  <files>site/package.json, site/package-lock.json</files>
  <read_first>site/package.json, site/package-lock.json, .planning/phases/02-consensus-knowledge-and-commercial-model/02-03-SUMMARY.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md</read_first>
  <action>Proceed only if Plan 03 records the exact resume signal `approved uuid@14.0.1`. Run `cd site &amp;&amp; npm install uuid@14.0.1 --save-exact`; verify package.json pins `14.0.1`, the lockfile resolves the official package, and no postinstall script or unrelated dependency change appears. Do not use a caret/range, an alternate UUID package, copied source, or a custom codec. Production modules will import only `v7` from `uuid`.</action>
  <acceptance_criteria>
    - package.json contains exactly `"uuid": "14.0.1"`.
    - `npm ls uuid --depth=0` resolves exactly 14.0.1.
    - Lockfile diff contains only the package and its legitimate dependency metadata; no script/config changes are hidden in the install.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; npm ls uuid --depth=0 &amp;&amp; node -e "import('uuid').then(({v7})=&gt;{const id=v7(); if(!/^[0-9a-f-]{36}$/.test(id)) process.exit(1)})"</automated>
  </verify>
  <done>The exact human-approved UUIDv7 package is pinned and loadable with no implicit substitute.</done>
</task>

<task type="auto">
  <name>Task 2: Declare and migrate the authoritative Phase 2 model</name>
  <files>site/db/schema.ts, site/drizzle/0004_consensus_knowledge.sql, site/drizzle/meta/0004_snapshot.json, site/drizzle/meta/_journal.json</files>
  <read_first>site/db/schema.ts, site/drizzle.config.ts, site/drizzle/0000_jittery_meteorite.sql, site/drizzle/0001_true_spencer_smythe.sql, site/drizzle/0002_eager_supreme_intelligence.sql, site/drizzle/0003_acoustic_magik.sql, site/tests/migration-chain.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md, docs/IMPLEMENTATION-SPEC.md, docs/adr/0001-generic-company-product-play-model.md, docs/adr/0002-confirmed-knowledge-and-effective-configuration.md</read_first>
  <action>Extend schema.ts with companies; offers(parent `profile_id` plus immutable hierarchy-interview question/answer/proposal/decision/Knowledge Version/authority-command/audit lineage); Company-wide organizations/contacts; Play/Profile-scoped accounts/targets/contact_relevance read-model tables; sources/source_excerpts/source_custody with quarantine/scan status; persisted public/authorized research_candidates whose immutable source/excerpt/provenance can be selected by opaque locator; stable knowledge_items; immutable knowledge_proposals, proposal_decisions, authority_commands, prerequisite joins, and augmented knowledge_versions; configuration_knowledge_dependencies and artifact_configuration_dependencies; knowledge_drifts, drift_impact_snapshots, replacement_candidates, configuration_activations; and phase_activation_gates scoped to `consensus_knowledge`. The gate row persists the exact complete tuple with canonical fields `capability`, `authorization_reference`, `target_project_deployment`, `reviewed_source_digest`, `migration_identity_status`, `post_migration_evidence_reference`, `independent_review_reference`, and `deployed_boundary_proof_reference`, plus a fixed-order SHA-256 tuple digest; every field is NOT NULL/nonempty and the exact tuple is immutable. Use workspace-inclusive indexes/FKs, exact enum allowlists, revisions, immutable digests, and partial unique live-state indexes for one Company/workspace, live interview session/destination, active question/session, decision/reviewed snapshot, current knowledge version/item, and active typed configuration/owner. Generate with `npm run db:generate -- --name consensus_knowledge`, require the exact filename `0004_consensus_knowledge.sql`, then review/customize it as an additive expand/migrate release: do not drop/rename columns or disable foreign keys. Add nullable compatibility columns and transaction-failing triggers where SQLite cannot add a retroactive FK without rebuilding. Backfill one Company per existing workspace and stable knowledge item/lineage links for valid historian versions without changing their value_json, source/proposal/operation digests, confirmations, or audit rows; retain legacy-unbound/coexistence quarantine. Add no seed Offer, no Ready lifecycle, no gate acceptance row, no upload release, and no operational rows.</action>
  <acceptance_criteria>
    - The exact Company -> Product -> Play -> Profile -> Offer path is database-enforced and Offer cannot reference a Play directly.
    - Every live-state uniqueness rule and stale-command guard is enforced by D1 constraints/indexes/triggers, not a read-before-write convention alone.
    - phase_activation_gates stores all eight canonical authorization-tuple fields and its fixed-order digest; no partial tuple can satisfy an accepted gate.
    - Migration SQL is additive, contains no `DROP`, no `PRAGMA foreign_keys=OFF`, no accepted activation row, and no operational seed.
    - Existing valid historian values/digests/decisions/audits remain byte-for-byte unchanged and unbound authority remains quarantined.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; npm run db:generate -- --name consensus_knowledge --help >/dev/null &amp;&amp; node --test tests/migration-chain.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Migration 0004 and schema declarations enforce all Phase 2 authority invariants while preserving existing history and leaving hosted writes inactive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing hosted D1 -> additive migration | Live Phase 1 authority must not detach, duplicate, or be rewritten. |
| Domain commands -> D1 constraints | Concurrency and workspace scope must fail transactionally even when application reads are stale. |
| Package registry -> authority identifiers | Third-party UUID generation enters immutable record identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Repudiation | 0004 migration/backfill | mitigate | Full 0000-0004 tests for counts, digests, lineage, foreign keys, idempotency, and all legacy/coexistence fixtures; no hosted migration in this plan. |
| T-02-02 | Elevation | hierarchy/scope schema | mitigate | Workspace-inclusive FKs/indexes/triggers and no client-derived root scope. |
| T-02-05 | Tampering | authority_commands/live indexes | mitigate | FK-backed expected-revision guard plus unique partial indexes make stale/duplicate authority a failed transaction. |
| T-02-08 | Information Disclosure | source custody | mitigate | Opaque refs, bounded metadata, quarantine status, and no parser/render/release route. |
| T-02-SC | Tampering | uuid install | mitigate | Exact human-approved 14.0.1 pin; inspect lockfile/postinstall and reject unrelated dependency changes. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/migration-chain.test.mjs &amp;&amp; npm run lint` must pass. Review `git diff -- site/db/schema.ts site/drizzle site/package.json site/package-lock.json` for additive-only behavior and the exact approved package.
</verification>

<success_criteria>
- Full migration chain and every legacy/backfill invariant are green.
- All required Phase 2 authority artifacts exist with database-enforced scope/concurrency.
- Hosted Phase 2 writes, runtime upload, and every downstream operation remain disabled.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-04-SUMMARY.md` when done.
</output>
