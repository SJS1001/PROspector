---
phase: 02-consensus-knowledge-and-commercial-model
plan: 22
type: execute
wave: 11
depends_on: ["02-12"]
files_modified: [docs/GREENFIELD-BASELINE.md, docs/adr/0006-greenfield-baseline-after-inaccessible-hosting.md, site/scripts/greenfield-baseline.mjs, site/tests/greenfield-baseline.test.mjs]
autonomous: true
requirements: [REQ-commercial-hierarchy, REQ-consensus-interview, REQ-versioned-knowledge-and-drift]
must_haves:
  truths: ["The inaccessible original project is retired without claiming a migration occurred.", "A fresh disposable local database is reproducibly built from checked migrations with zero authority or operational rows.", "Greenfield preparation grants no hosted, provider, production, export, or outbound authority."]
  artifacts: [{path: docs/GREENFIELD-BASELINE.md, provides: controlling greenfield boundary}, {path: site/scripts/greenfield-baseline.mjs, provides: safe local baseline attestation}, {path: site/tests/greenfield-baseline.test.mjs, provides: regression proof}]
  key_links: [{from: site/scripts/greenfield-baseline.mjs, to: site/scripts/local-bootstrap.mjs, via: explicit disposable reset}, {from: docs/GREENFIELD-BASELINE.md, to: later phase preparation, via: no-original-dependency rule}]
---
<objective>Replace the inaccessible original-target recovery dependency with a reproducible greenfield local baseline. Purpose: continue safe implementation without fabricating migration provenance. Output: durable decision, local attestation, and regression proof only.</objective>
<tasks>
<task type="auto"><name>Task 1: Retire original-target execution dependencies</name><files>docs/GREENFIELD-BASELINE.md, docs/adr/0006-greenfield-baseline-after-inaccessible-hosting.md, AGENTS.md, docs/CODEX-CONTINUATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md</files><action>Record the explicit owner decision. Preserve incident and recovery plans as retired history outside executor discovery. State that provenance evidence is intentionally waived because the original target will never be reused, that no migration occurrence is claimed, and that no hosted write or target provisioning is authorized.</action><verify><automated>rg -n "inaccessible|waive|no claim|greenfield|no hosted" docs/GREENFIELD-BASELINE.md docs/adr/0006-greenfield-baseline-after-inaccessible-hosting.md .planning/STATE.md .planning/ROADMAP.md</automated></verify><done>Every active continuation source points to greenfield work and never instructs an agent to access the original target.</done></task>
<task type="auto" tdd="true"><name>Task 2: Prove the disposable greenfield baseline</name><files>site/scripts/greenfield-baseline.mjs, site/tests/greenfield-baseline.test.mjs, site/package.json</files><action>Create an explicit local-only attestation that resets only a caller-selected path below site/.local, delegates the checked migration chain to local-bootstrap, checks selected authority/operational row counts are zero, and emits allowlisted safe JSON. Include an explicit no-original-migration-claim field. Add adversarial tests for missing reset, path traversal, zero-state enforcement, and exact output. Do not access a network or hosted target.</action><verify><automated>cd site && node --test tests/greenfield-baseline.test.mjs && npm test && npm run lint && npm run build</automated></verify><done>A fresh disposable local baseline is reproducible, empty, and explicitly not hosted evidence.</done></task>
</tasks>
<verification>Focused baseline test, full suite, lint, build, and production audit pass. No hosted action occurs.</verification>
<success_criteria>The repository has one unambiguous greenfield starting point and no active dependency on original-project evidence.</success_criteria>
<output>Create `02-22-SUMMARY.md` only after both tasks genuinely pass.</output>
