---
phase: 02-consensus-knowledge-and-commercial-model
plan: "12"
type: execute
wave: 10
depends_on: ["02-11"]
files_modified: [.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md]
autonomous: false
requirements: [REQ-commercial-hierarchy, REQ-consensus-interview, REQ-versioned-knowledge-and-drift]
user_setup:
  - service: Codex Sites and hosted D1
    why: "Real Phase 1 principal isolation and old-schema state are non-substitutable release prerequisites."
    dashboard_config:
      - task: "Provide a controlled second real signed-in principal and read-only access to the existing private project's D1 database."
        location: "Existing private Sites access policy and D1 control plane"
must_haves:
  truths:
    - "The Phase 1 real second-principal proof is accepted before Phase 2 hosted work begins."
    - "A read-only hosted baseline proves schema 0003, protected counts/digests/FKs, and zero Phase 2 activation."
    - "human_needed pauses this plan and never satisfies done."
  artifacts:
    - { path: ".planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md", provides: "Accepted or still-blocked prerequisite/baseline evidence" }
  key_links:
    - { from: "site/scripts/phase2-hosted-preflight.mjs", to: "existing hosted D1", via: "old-schema read-only mode", pattern: "old-schema" }
---
<objective>Prove the real identity boundary and capture the old-schema baseline before any deployment or migration.</objective>
<execution_context>@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/STATE.md
@.planning/phases/01-private-pilot-boundary/01-HOSTED-PROOF.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md
@site/scripts/hosted-boundary-proof.mjs
@site/scripts/phase2-hosted-preflight.mjs
<decision_map>All D-01 through D-16 remain local/inert until this prerequisite passes.</decision_map>
<interfaces>Use the real Sites identity boundary; forged headers and local principals do not count.</interfaces></context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Accept real-principal and old-schema baseline evidence</name>
  <files>.planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md</files>
  <read_first>.planning/STATE.md, .planning/phases/01-private-pilot-boundary/01-HOSTED-PROOF.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-ACTIVATION.md, site/scripts/hosted-boundary-proof.mjs, site/scripts/phase2-hosted-preflight.mjs</read_first>
  <action>First run the local suite and preflight security tests. Then require the existing Phase 1 hosted second-real-principal proof, or perform it now against the existing private Sites project: neutral denial across app/interview/capability routes and zero owner D1/R2/audit delta. With operator access kept outside Git/logs, run preflight `--mode old-schema` before any deploy or migration. Require exact 0000-0003 migrations, empty foreign-key violations, accepted historian lineage/count/digest baseline, complete forbidden-table counts, and no Phase 2 gate/table assumption. Record redacted outcomes only. Do not deploy, migrate, write, change project/access/secrets, upload, or activate anything. If any real principal/access/evidence is absent or mismatched, return `human_needed`, leave status blocked, do not create this plan's SUMMARY, and pause; `human_needed` is not approval or done.</action>
  <acceptance_criteria>
    - Real second-principal isolation and zero delta are observed at the hosting boundary.
    - Old-schema baseline is read-only, schema-0003 exact, redacted, and accepted before any hosted mutation.
    - Only `approved old-schema baseline` resumes; missing evidence pauses without completion.
  </acceptance_criteria>
  <verify><automated>cd site &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; node --test tests/phase2-hosted-preflight.test.mjs</automated><human-check>Confirm real second-principal isolation and the redacted old-schema baseline from the existing private project.</human-check></verify>
  <done>Only complete when the real-principal prerequisite and old-schema baseline are both accepted.</done>
  <resume-signal>Reply `approved old-schema baseline`; otherwise reply `human_needed` with the missing proof and the plan pauses.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Real second principal -> private Sites edge | Only hosted identity proves isolation. |
| Operator control plane -> proof ledger | Sensitive transport stays ephemeral and redacted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Spoofing | principal proof | mitigate | Real hosted identity plus zero-state delta; mocks forbidden. |
| T-02-18 | Tampering / Disclosure | old baseline | mitigate | Fixed read-only tool, exact schema expectation, redacted evidence. |
| T-02-SC | Tampering | dependencies | mitigate | No install. |
</threat_model>
<verification>Automated checks do not substitute for the two human/hosted proofs.</verification>
<success_criteria>Both non-substitutable prerequisites pass; otherwise execution remains paused.</success_criteria>
<output>Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-12-SUMMARY.md` only after approval.</output>
