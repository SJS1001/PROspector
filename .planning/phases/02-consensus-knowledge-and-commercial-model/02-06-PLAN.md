---
phase: 02-consensus-knowledge-and-commercial-model
plan: "06"
type: execute
wave: 5
depends_on:
  - "02-02"
  - "02-05"
files_modified:
  - site/domain/interview.ts
autonomous: true
requirements:
  - REQ-consensus-interview
  - REQ-versioned-knowledge-and-drift
user_setup: []
must_haves:
  truths:
    - "The interview presents one authoritative decision-bearing question with separately typed evidence, inference, recommendation, destination, and prerequisite versions."
    - "Answer submission and Accept/Reject/Correct/Rescope are separate immutable transactions bound to the exact stored snapshot; only an accepted/corrected/rescoped hierarchy-completion decision can create the first Offer."
    - "Reload, retry, response loss, stale prerequisites, supersession, and two-tab races converge without duplicate answers, decisions, versions, or hidden overwrite."
  artifacts:
    - path: "site/domain/interview.ts"
      provides: "Generalized research-first interview state machine and compatibility with valid Phase 1 historian authority"
      exports: ["readInterviewState", "submitInterviewAnswer", "recordInterviewDecision", "restartUnboundReview"]
  key_links:
    - from: "site/domain/interview.ts"
      to: "site/domain/knowledge.ts"
      via: "reviewed proposal snapshot and immutable Knowledge Version append"
      pattern: "reviewKnowledgeProposal|KnowledgeDecision"
    - from: "site/domain/interview.ts"
      to: "site/tests/interview-repository.test.mjs"
      via: "race/idempotency/state assertions"
      pattern: "submitInterviewAnswer|recordInterviewDecision"
---

<objective>
Generalize the proven Phase 1 interview into the complete research-first, four-decision, concurrency-safe knowledge workflow.

Purpose: Make each answer and owner decision reproducible against exact evidence, scope, prerequisites, and revisions without losing legacy historian authority.
Output: One tested interview module; no route/UI or external research integration yet.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md
@.planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md
@docs/IMPLEMENTATION-SPEC.md
@docs/adr/0002-confirmed-knowledge-and-effective-configuration.md
@site/domain/interview.ts
@site/domain/knowledge.ts
@site/domain/commercial-model.ts
@site/db/schema.ts
@site/tests/interview-repository.test.mjs
@site/tests/knowledge-repository.test.mjs

<decision_map>
D-05 research-first one-question presentation; D-06 separate exact Answer and Accept/Reject/Correct/Rescope; D-07 one Active question with revision/idempotency/transactional race handling; D-08 append versions and retain superseded/unconfirmed lineage.
</decision_map>

<interfaces>
Preserve these existing exports or compatibility wrappers: PILOT_COMPANY_NAME, INITIAL_QUESTION, InterviewPrincipal, InterviewState, InterviewConflictError, principalFromIdentity, readInterviewState, bootstrapInterview, submitRecommendationAnswer, confirmSubmittedAnswer, restartUnboundReview.

Add submitInterviewAnswer and recordInterviewDecision as the generalized APIs. InterviewState must carry exact server-stored destination, evidence findings/source refs, labelled inference, recommendation, sorted prerequisites, answer/proposal digest, revisions, and immutable result references.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize the interview state machine and transactional decisions</name>
  <files>site/domain/interview.ts</files>
  <read_first>site/domain/interview.ts, site/domain/knowledge.ts, site/domain/commercial-model.ts, site/db/schema.ts, site/tests/interview-repository.test.mjs, site/tests/knowledge-repository.test.mjs, .planning/phases/02-consensus-knowledge-and-commercial-model/02-CONTEXT.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-RESEARCH.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-PATTERNS.md, .planning/phases/02-consensus-knowledge-and-commercial-model/02-UI-SPEC.md, docs/adr/0002-confirmed-knowledge-and-effective-configuration.md</read_first>
  <behavior>
    - One session has at most one Active question; every projection separates facts/source refs, inference, recommendation, destination, and prerequisite versions.
    - Stage 1 stores one immutable answer snapshot; Stage 2 stores one exact Accept/Reject/Correct/Rescope result with the required version effect.
    - Same logical operation retries converge; stale revisions/prerequisites/destination and competing tabs fail visibly without partial inserts.
  </behavior>
  <action>Refactor the current single-question assumptions without discarding its owner-scoped workspace lookup, valid historian snapshot, legacy-unbound/coexistence quarantine, or public exports. Expand QuestionView/InterviewState to use separately typed evidence findings (source title/ref/type/dates/retrieval/excerpt), labelled inference, recommendation with optional recommended value, exact destination, and sorted prerequisite version IDs/digests. Repository-seeded public/authorized excerpts may support questions; add no runtime crawler, upload parser, Runner, or external API. If no reliable evidence exists, project that fact explicitly and never invent an Offer/value. Implement submitInterviewAnswer for `use_recommendation`, `write_correction`, and `change_scope`, storing fixed-order JSON/digest and moving to awaiting_confirmation only. Implement recordInterviewDecision with the exact Accept/Reject/Correct/Rescope discriminant and delegate version authority to knowledge.ts. When and only when the exact stored proposal kind is hierarchy_completion_offer, call materializeOfferFromConfirmedHierarchyDecision inside the same transaction after Accept/Correct/Rescope has appended its Knowledge Version and decision/audit records; bind the Profile parent and Offer name/value to the stored/corrected/rescoped snapshot and persist all question/answer/proposal/decision/version/authority-command/audit references. Reject, answer submission, generic hierarchy drafting, non-hierarchy review, stale/conflicting/replayed mismatches, and partial failure create no Offer. Bind both transactions to expected session/question/proposal/prerequisite/destination revisions through authority_commands and FK/unique guards so a zero-row update cannot permit later inserts. Reuse the same key only for the same digest; on a constraint race read and compare the authoritative winner. Preserve superseded questions/unconfirmed answers in history and never increment question numbers optimistically. Keep compatibility wrappers for the existing historian methods and exact valid confirmed value.</action>
  <acceptance_criteria>
    - Existing Phase 1 historian/quarantine tests and all generalized interview tests pass.
    - The stored snapshot, not current constants/client state, drives Stage 2 and final authority.
    - Every concurrency case returns one authoritative state or explicit InterviewConflictError; no duplicate/partial rows exist.
    - Accepted/corrected/rescoped hierarchy-completion decisions create one Profile-parented first Offer with complete immutable lineage; all other paths create zero Offers.
    - No path fetches the web, parses uploads, starts a Run/schedule, or writes forbidden operational tables.
  </acceptance_criteria>
  <verify>
    <automated>cd site &amp;&amp; node --test tests/interview-repository.test.mjs tests/knowledge-repository.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>The interview is research-first, one-question, two-stage, four-decision, immutable, legacy-safe, and deterministic under all tested retry/race cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Question/source snapshot -> owner decision | Current source or code changes must not alter what the owner reviews and confirms. |
| Concurrent browser tabs -> D1 authority | Stale and competing commands must not create duplicate or partial authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering / Repudiation | answer/decision snapshots | mitigate | Fixed-order stored JSON+digest binds facts, inference, recommendation, prerequisites, destination, decision payload, and revisions. |
| T-02-07 | Tampering | active question/session races | mitigate | Partial unique indexes plus FK-backed authority commands and exact winner comparison. |
| T-02-08 | Information Disclosure | source excerpts | mitigate | Only bounded plain text and opaque refs; no raw HTML/private documents/credentials. |
| T-02-SC | Tampering | package use | mitigate | Only pinned uuid.v7 for new IDs; no new install in this plan. |
</threat_model>

<verification>
`cd site &amp;&amp; node --test tests/interview-repository.test.mjs tests/knowledge-repository.test.mjs &amp;&amp; npm run lint` passes, including all Phase 1 regression and full-migration fixtures.
</verification>

<success_criteria>
- Evidence, inference, recommendation, and Confirmed Knowledge are structurally distinct.
- Answer and decision are separate immutable operations.
- All reload/retry/stale/concurrent cases converge without hidden overwrite or operational side effect.
</success_criteria>

<output>
Create `.planning/phases/02-consensus-knowledge-and-commercial-model/02-06-SUMMARY.md` when done.
</output>
