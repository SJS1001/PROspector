---
phase: 01-private-pilot-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - site/tests/pilot-access.test.mjs
  - site/tests/capability-state.test.mjs
  - site/tests/object-storage.test.mjs
  - site/tests/capabilities-route.test.mjs
  - site/tests/interview-handler.test.mjs
  - site/tests/interview-repository.test.mjs
autonomous: true
requirements:
  - REQ-private-human-governed-gtm
  - REQ-company-workspace-isolation
must_haves:
  truths:
    - "The test contract rejects every non-owner before workspace data is read or created."
    - "Capability tests distinguish proven, blocked, and unproven and reject binding presence as proof."
    - "Object-storage tests prove server-derived workspace prefixes and the write/read/digest/delete/absence lifecycle."
  artifacts:
    - path: site/tests/pilot-access.test.mjs
      provides: Single-owner admission and neutral-denial contract
    - path: site/tests/capability-state.test.mjs
      provides: Evidence-backed capability projection contract
    - path: site/tests/object-storage.test.mjs
      provides: Provider-neutral object isolation and lifecycle contract
    - path: site/tests/capabilities-route.test.mjs
      provides: Capability API privacy and fail-closed response contract
  key_links:
    - from: site/tests/pilot-access.test.mjs
      to: site/domain/pilot-access.ts
      via: imported admission contract
    - from: site/tests/object-storage.test.mjs
      to: site/domain/ports/object-storage.ts
      via: in-memory port implementation
---

<objective>
Create the failing security and behavior contract that all Phase 1 implementation must satisfy.

Purpose: Prevent the existing multi-workspace behavior and binding-presence capability claim from surviving implementation through ambiguous tests.
Output: Four focused Wave 0 test files plus corrected expectations in the existing interview tests.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-private-pilot-boundary/01-CONTEXT.md
@.planning/phases/01-private-pilot-boundary/01-RESEARCH.md
@.planning/phases/01-private-pilot-boundary/01-VALIDATION.md
@site/tests/interview-handler.test.mjs
@site/tests/interview-repository.test.mjs
@site/tests/request-security.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the single-owner route and repository contract</name>
  <files>site/tests/pilot-access.test.mjs, site/tests/interview-handler.test.mjs, site/tests/interview-repository.test.mjs</files>
  <read_first>
    site/tests/interview-handler.test.mjs
    site/tests/interview-repository.test.mjs
    site/domain/interview-handler.ts
    site/domain/interview.ts
    .planning/phases/01-private-pilot-boundary/01-CONTEXT.md
  </read_first>
  <behavior>
    - Trusted identity matching normalized `PILOT_OWNER_EMAIL` is admitted; missing or mismatched identity is denied before workspace lookup.
    - A non-owner cannot bootstrap, read, answer, confirm, or create a second workspace.
    - Unauthorized responses contain no company name, workspace ID, audit ID, capability state, or owner email.
    - Existing Answer and Confirmation concurrency/idempotency behavior remains owner-scoped.
  </behavior>
  <action>Create `pilot-access.test.mjs` using the existing Vite/Miniflare test style. Replace the current outsider-bootstrap expectations in both existing interview tests with the locked one-owner deny behavior (D-01 through D-04 and D-10). Use distinct owner and outsider identities, assert workspace/audit counts do not change after outsider attempts, and assert a neutral error body. Do not add invitations, roles, client-provided workspace IDs, or a second-workspace fixture.</action>
  <verify>
    <automated>cd site && node --test tests/pilot-access.test.mjs tests/interview-handler.test.mjs tests/interview-repository.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pilot-access.test.mjs` contains explicit owner, missing-identity, and second-principal cases.
    - Existing tests no longer assert that `outsiderActive.status` is `active` or that an outsider creates a second workspace.
    - The targeted command initially fails only because the Phase 1 admission implementation is not present.
  </acceptance_criteria>
  <done>The executable test contract makes the current second-workspace behavior a failure while preserving owner interview semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock capability evidence, object isolation, and route privacy contracts</name>
  <files>site/tests/capability-state.test.mjs, site/tests/object-storage.test.mjs, site/tests/capabilities-route.test.mjs</files>
  <read_first>
    site/app/api/capabilities/route.ts
    site/db/schema.ts
    site/domain/request-security.ts
    site/tests/request-security.test.mjs
    site/tests/fixture-safety.test.mjs
    .planning/phases/01-private-pilot-boundary/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Capability projection returns only `proven`, `blocked`, or `unproven` and requires immutable evidence for `proven`.
    - An R2 binding with no successful proof remains `unproven`.
    - Object keys are generated under the admitted workspace prefix; caller-supplied arbitrary keys and payloads are impossible.
    - Probe lifecycle verifies written bytes by digest, deletes them, and confirms absence.
    - Capability denial exposes no authenticated email or workspace evidence and all responses are `no-store`.
  </behavior>
  <action>Create the three missing tests using Node's test runner and small in-memory fakes. Define expected imports and exported contracts for `CapabilityStatus`, `projectCapabilityState`, `ObjectStoragePort`, `workspaceObjectPrefix`, and the capability route handler so later tasks implement stable interfaces. Include binding-presence-negative, failed-delete, cross-prefix, foreign-origin, missing-CSRF, and unauthorized-response cases per D-05, D-09, D-11 through D-14. Do not require a new package, live R2, arbitrary bucket keys, or real operational data.</action>
  <verify>
    <automated>cd site && node --test tests/capability-state.test.mjs tests/object-storage.test.mjs tests/capabilities-route.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Each file exists and names the exact expected source module/export.
    - Tests explicitly prove `Boolean(FILES)` cannot produce `proven`.
    - Tests explicitly prove cross-prefix access and incomplete deletion cannot produce a successful proof.
    - The targeted command initially fails only because the Phase 1 modules/handler are not present.
  </acceptance_criteria>
  <done>Wave 0 contains fast deterministic tests for every new security-critical domain contract and route behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → production contract | Tests must not normalize unsafe multi-tenant behavior. |
| Fake storage → hosted R2 | Local proof validates logic but cannot claim hosted durability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing/Elevation | identity and bootstrap tests | mitigate | Require second-principal deny tests before implementation. |
| T-01-02 | Tampering | capability tests | mitigate | Assert only immutable evidence yields `proven`. |
| T-01-03 | Information disclosure | route tests | mitigate | Assert neutral bodies contain no owner/workspace/evidence detail. |
| T-01-04 | Repudiation | proof lifecycle | mitigate | Assert proof result carries an audit/evidence reference. |
</threat_model>

<verification>
Run each targeted command and confirm failures are behavioral RED failures, not syntax, import-resolution, or fixture failures.
</verification>

<success_criteria>
- Every missing test named in 01-VALIDATION.md exists.
- Current second-workspace and R2-binding-as-proof behaviors are executable failures.
- No new dependency or live data is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-private-pilot-boundary/01-01-SUMMARY.md` when done.
</output>
