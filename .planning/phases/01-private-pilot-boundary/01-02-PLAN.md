---
phase: 01-private-pilot-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - site/domain/pilot-access.ts
  - site/domain/interview-handler.ts
  - site/app/api/interview/route.ts
  - site/tests/pilot-access.test.mjs
  - site/tests/interview-handler.test.mjs
  - site/tests/interview-repository.test.mjs
autonomous: true
requirements:
  - REQ-private-human-governed-gtm
  - REQ-company-workspace-isolation
user_setup:
  - service: Codex Sites
    why: Identify the sole admitted pilot owner in trusted server code
    env_vars:
      - name: PILOT_OWNER_EMAIL
        source: Sites runtime secret containing the normalized owner email
must_haves:
  truths:
    - "Only the configured owner can enter or bootstrap the private workspace."
    - "A second authenticated principal receives the same neutral denial as any unauthorized identity."
    - "No client value can select a principal or workspace."
  artifacts:
    - path: site/domain/pilot-access.ts
      provides: Single-owner admission and neutral authorization error
      exports: [admitPilotOwner, PilotAccessError]
    - path: site/domain/interview-handler.ts
      provides: Admission before every interview read or mutation
  key_links:
    - from: site/app/api/interview/route.ts
      to: site/domain/pilot-access.ts
      via: injected trusted identity and PILOT_OWNER_EMAIL
    - from: site/domain/interview-handler.ts
      to: site/domain/interview.ts
      via: admitted principal only
---

<objective>
Enforce the one-owner, one-workspace pilot boundary before any interview route or repository action.

Purpose: Close the current defect that lets any authenticated principal bootstrap another workspace.
Output: A reusable admission module wired into the interview API with all owner and negative tests green.
</objective>

<execution_context>
@/Users/stevensmith/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevensmith/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-private-pilot-boundary/01-CONTEXT.md
@.planning/phases/01-private-pilot-boundary/01-RESEARCH.md
@.planning/phases/01-private-pilot-boundary/01-01-SUMMARY.md
@site/app/chatgpt-auth.ts
@site/domain/interview-handler.ts
@site/app/api/interview/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement normalized single-owner admission</name>
  <files>site/domain/pilot-access.ts, site/tests/pilot-access.test.mjs</files>
  <read_first>
    site/app/chatgpt-auth.ts
    site/domain/interview.ts
    site/tests/pilot-access.test.mjs
    site/tests/interview-handler.test.mjs
  </read_first>
  <behavior>
    - Matching is case-insensitive after trim/lowercase normalization.
    - Missing identity, missing/invalid configured owner, and identity mismatch all fail closed.
    - The denial object exposes a stable neutral code and no email or workspace metadata.
  </behavior>
  <action>Create `pilot-access.ts` with `admitPilotOwner(identity, configuredOwnerEmail, subjectPepper)` returning the existing `InterviewPrincipal` only after normalized trusted email equals normalized `PILOT_OWNER_EMAIL`; otherwise throw `PilotAccessError` with neutral code `private_workspace_unavailable`. Reuse `principalFromIdentity` after admission. Reject empty or malformed config and never export normalized email. Implement D-01 through D-04 and D-14 in server code, not client state.</action>
  <verify>
    <automated>cd site && node --test tests/pilot-access.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pilot-access.ts` exports `admitPilotOwner` and `PilotAccessError`.
    - Source contains no persistence, logging, invitation, role, or client-workspace behavior.
    - Owner, missing identity/config, case-normalization, and outsider tests pass.
  </acceptance_criteria>
  <done>The admitted principal exists only for the configured trusted owner and every other identity fails neutrally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire admission before interview reads and mutations</name>
  <files>site/domain/interview-handler.ts, site/app/api/interview/route.ts, site/tests/interview-handler.test.mjs, site/tests/interview-repository.test.mjs</files>
  <read_first>
    site/domain/interview-handler.ts
    site/app/api/interview/route.ts
    site/domain/request-security.ts
    site/tests/interview-handler.test.mjs
    site/tests/interview-repository.test.mjs
    site/domain/pilot-access.ts
  </read_first>
  <behavior>
    - GET and POST perform owner admission before workspace lookup, CSRF consumption, or body processing.
    - Non-owner attempts create no workspace, session, question, answer, confirmation, knowledge, or audit row.
    - Owner Answer/Confirmation, concurrency, idempotency, and no-store behavior remain green.
  </behavior>
  <action>Extend `InterviewHandlerDependencies` with `pilotOwnerEmail`; replace direct principal derivation in `authenticatedPrincipal` with `admitPilotOwner`; map `PilotAccessError` to the neutral denied response without distinguishing missing identity from mismatch. In the route, require `DB`, `OWNER_SUBJECT_PEPPER`, and `PILOT_OWNER_EMAIL` bindings and inject them without returning or logging values. Update repository fixtures to operate only through the owner principal; keep any second-principal object solely for negative cross-owner calls. Do not weaken Origin, Fetch Metadata, intent, CSRF, content-type, body-size, or idempotency checks (D-06 and D-11).</action>
  <verify>
    <automated>cd site && node --test tests/pilot-access.test.mjs tests/interview-handler.test.mjs tests/interview-repository.test.mjs tests/request-security.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both route methods deny before any D1 creation/read for a non-owner.
    - `PILOT_OWNER_EMAIL` is accessed only in server route dependencies.
    - All targeted tests pass and retain explicit Answer-versus-Confirmation assertions.
  </acceptance_criteria>
  <done>The interview slice is accessible only to the configured owner and remains fail-closed at every mutation boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sites headers → application principal | Authenticated does not mean admitted owner. |
| Route dependencies → domain handler | Secrets and trusted identity enter server code only. |
| Admitted context → D1 | Only admitted principal may resolve a workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Spoofing | trusted identity | mitigate | Consume only server identity adapter output; ignore request identity headers. |
| T-01-06 | Elevation | second principal | mitigate | Compare normalized identity to server-only PILOT_OWNER_EMAIL before principal derivation. |
| T-01-07 | Information disclosure | denial response | mitigate | Use one neutral code with no owner/workspace details. |
| T-01-08 | Tampering | mutation route | mitigate | Preserve Origin, Fetch Metadata, intent, one-time CSRF, and bounded JSON guards. |
</threat_model>

<verification>
Run the targeted suite, then `cd site && npm run lint && npm test`.
</verification>

<success_criteria>
- The current outsider-bootstrap behavior is removed.
- Owner interview lifecycle remains durable, idempotent, and separately confirmed.
- No secret or identity detail reaches client responses, Git, or audit payloads.
</success_criteria>

<output>
Create `.planning/phases/01-private-pilot-boundary/01-02-SUMMARY.md` when done.
</output>
