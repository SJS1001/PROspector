---
phase: 04-profile-readiness-and-evidence-based-prospecting
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [site/domain/profile-readiness.ts, site/domain/prospecting-schedule.ts, site/tests/profile-prospecting-contract.test.mjs]
autonomous: true
requirements: [REQ-profile-readiness]
must_haves:
  truths: ["Only a complete Profile can be separately activated into one immutable effective configuration, initial run, and timezone schedule."]
  artifacts:
    - path: site/domain/profile-readiness.ts
      provides: server-authoritative completeness/candidate/activation workflow
  key_links:
    - from: site/domain/profile-readiness.ts
      to: typed_configurations
      via: immutable profile_effective digest activation
      pattern: profile_effective
---
<objective>Implement Profile readiness and durable schedule intent. Purpose: make activation an atomic owner boundary, not an edit or external run. Output: readiness and schedule domain modules.</objective>
<context>@.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-CONTEXT.md
@site/domain/product-readiness.ts
@site/domain/market-discovery.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Build immutable Profile readiness candidate and activation commands</name><files>site/domain/profile-readiness.ts, site/tests/profile-prospecting-contract.test.mjs</files><behavior>- All listed Profile categories and exact Phase 3 parent/policy inputs are returned as complete, missing, stale, or wrong-scoped.
- Candidate creation never starts work; activation atomically yields exactly one active immutable configuration, initial run, schedule, audit, and same-digest retry winner.</behavior><action>Follow the admitted-principal, canonical digest, expected-revision, authority-command, unique-index, reload pattern. Per D-01, derive Profile/Product/Play/Offer, confirmed versions, source/runner policy, rubric, guardrails, strategy, compliance, schedule/timezone, and output policy on the server. Create a candidate distinct from activation; activation revalidates all snapshots and records replacement directives without mutating historical configurations. Reject client-supplied configuration, runner, schedule, or scope authority and assert forbidden-table zero deltas.</action><verify><automated>cd site && node --test tests/profile-prospecting-contract.test.mjs</automated></verify><done>Stale/racing/replayed activation produces one authoritative result and no downstream external effect.</done></task>
<task type="auto" tdd="true"><name>Task 2: Persist fail-closed Profile schedule and run intent</name><files>site/domain/prospecting-schedule.ts, site/tests/profile-prospecting-contract.test.mjs</files><behavior>- Profile slot keys cannot collide with Product slots and encode intended local instant plus offset.
- Initial/manual/scheduled/material-change intents preserve watermark/window; only succeeded runs advance it, overlap becomes SkippedOverlap, and unproven transport remains blocked.</behavior><action>Reuse Phase 3's proven durable intent conventions while giving Profile its own owner/slot namespace. Implement timezone/DST, 24-hour overlap, <=24-hour misfire once/older skip, active-run limit, manual idempotency, and terminal event history. Per D-01/D-02, this module records intent only: it must not dispatch a scheduler, call a Runner/provider, retrieve a URL, or change downstream tables.</action><verify><automated>cd site && node --test tests/profile-prospecting-contract.test.mjs</automated></verify><done>Every run intent is replayable, schedule-safe, and blocked from external execution by default.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| client → readiness domain | Client locators select records but never grant configuration or schedule authority. |
## STRIDE Threat Register
| T-04-05 | Elevation | readiness activation | mitigate | Server-derived parent snapshots, digest/revision guard, atomic D1 batch. |
| T-04-06 | Denial/Tampering | schedule slots | mitigate | Profile namespace, DST offset key, watermark/overlap/misfire tests. |
</threat_model>
<verification><automated>cd site && node --test tests/profile-prospecting-contract.test.mjs</automated></verification>
<success_criteria>Activation creates durable but reject-only prospecting intent exactly once.</success_criteria>
<output>Create `.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-03-SUMMARY.md` only when complete.</output>
