---
phase: 04-profile-readiness-and-evidence-based-prospecting
plan: 06
type: tdd
wave: 4
depends_on: [04-02, 04-05]
files_modified: [site/domain/qualification.ts, site/tests/qualification.test.mjs]
autonomous: true
requirements: [REQ-deterministic-qualification, REQ-evidence-provenance]
must_haves:
  truths: ["The application, not a runner, reproduces all four outcomes using the exact immutable Profile configuration."]
  artifacts: [{path: site/domain/qualification.ts, provides: pure Mining qualification evaluator}]
  key_links: [{from: site/domain/qualification.ts, to: immutable assessments, via: canonical output, pattern: configurationDigest}]
---
<objective>Implement the pure deterministic Mining rubric. Purpose: make qualification explainable/replayable with no external I/O. Output: table-driven evaluator and canonical output contract.</objective>
<context>@docs/IMPLEMENTATION-SPEC.md
@.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-CONTEXT.md
@site/tests/qualification.test.mjs</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Drive the complete rubric RED-to-GREEN</name><files>site/domain/qualification.ts, site/tests/qualification.test.mjs</files><behavior>- Five 0–2 dimensions yield Passed only at >=7 with pain/timing >=1, no hard gate, complete fields, and one Tier 1 or two independent Tier 2 sources.
- Missing required fields gives InsufficientEvidence and score 0 for absent support; hard gates give Disqualified; complete below threshold gives NotQualified.</behavior><action>First run the existing RED vectors, then implement a pure total function with no database, time, network, model, or mutable configuration read. Return canonical score anchors, cited source IDs/tiers/groups, recency, missing fields, hard-gate results, exact outcome, deterministic pain/timing/account-fit/freshness/Prospect-ID sort inputs, evaluation version, and config/rubric digest. Implement every locked hard disqualifier exactly.</action><verify><automated>cd site && node --test tests/qualification.test.mjs</automated></verify><done>All exhaustive boundary vectors pass and a fixed input/configuration produces byte-stable structured output.</done></task>
<task type="auto" tdd="true"><name>Task 2: Lock deterministic regression and safety assertions</name><files>site/tests/qualification.test.mjs</files><behavior>- Replaying a fixture after unrelated state changes returns the same assessment.
- Tier 3, duplicate independence group, stale unreconfirmed evidence, and runner score claims cannot upgrade an outcome.</behavior><action>Add fixture replay and property-style permutations for evidence order, duplicate inputs, tie order, and every gate. Keep the evaluator independently importable so later persistence code cannot patch its output. Do not add a provider/model dependency or a fuzzy fallback.</action><verify><automated>cd site && node --test tests/qualification.test.mjs</automated></verify><done>Qualification is deterministic, complete, and isolated from external effects.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| validated evidence → qualification | Evidence must be evaluated with immutable policy rather than trusted as a recommendation. |
## STRIDE Threat Register
| T-04-11 | Tampering | scoring | mitigate | Pure canonical evaluator with exhaustive boundary/replay tests. |
| T-04-12 | Elevation | runner recommendation | mitigate | Evaluator accepts only validated trusted evidence fields. |
</threat_model>
<verification><automated>cd site && node --test tests/qualification.test.mjs</automated></verification>
<success_criteria>Application code produces the exact four outcomes and never infers missing evidence upward.</success_criteria>
<output>Create `.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-06-SUMMARY.md` only when complete.</output>
