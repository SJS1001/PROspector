---
phase: 04-profile-readiness-and-evidence-based-prospecting
plan: 08
type: execute
wave: 6
depends_on: [04-04, 04-07]
files_modified: [site/domain/prospecting-handler.ts, site/app/api/prospecting/route.ts, site/tests/profile-prospecting-contract.test.mjs, site/tests/runner-assignment.test.mjs]
autonomous: true
requirements: [REQ-profile-readiness, REQ-deterministic-qualification, REQ-evidence-provenance, REQ-untrusted-runner-boundary]
must_haves:
  truths: ["Owner and runner interfaces are thin, scoped, CSRF-protected where applicable, and return authoritative no-store projections."]
  artifacts: [{path: site/domain/prospecting-handler.ts, provides: closed owner and runner request boundary}, {path: site/app/api/prospecting/route.ts, provides: dependency injection only}]
  key_links: [{from: site/app/api/prospecting/route.ts, to: site/domain/prospecting-handler.ts, via: request handler, pattern: handleProspecting}]
---
<objective>Expose secure Phase 4 transport without enabling runner execution. Purpose: apply existing admission/mutation discipline to all actions. Output: handler and route.</objective>
<context>@site/domain/discovery-handler.ts
@site/domain/request-security.ts
@.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Implement closed owner read/mutation transport</name><files>site/domain/prospecting-handler.ts, site/app/api/prospecting/route.ts, site/tests/profile-prospecting-contract.test.mjs</files><behavior>- Owner reads return readiness/configuration/run/evidence/assessment/queue projections only after server admission.
- Candidate, activation, manual intent, assignment issuance/revocation, and review commands require same-origin, Fetch Metadata, intent, CSRF, bounded JSON, strict fields, expected revision, and idempotency.</behavior><action>Mirror discovery-handler: admit before parsing locators/body; resolve workspace on server; use neutral no-store/nosniff denial; map conflict without leaking details. Allow browser fields only as selectors and operation keys. Manual Find Prospects queues a run with server-selected source window/quota and cannot accept runner/config/tool/credential/URL. Unknown/malformed authority removes mutation controls; do not auto-retry unknown outcomes.</action><verify><automated>cd site && node --test tests/profile-prospecting-contract.test.mjs</automated></verify><done>All owner mutation security negatives pass and projections contain complete immutable authority for UI rendering.</done></task>
<task type="auto" tdd="true"><name>Task 2: Keep runner ingress separate and capability-bound</name><files>site/domain/prospecting-handler.ts, site/tests/runner-assignment.test.mjs</files><behavior>- Runner endpoint accepts only assignment-bound submission schema and returns neutral failures.
- It cannot use browser CSRF as authorization, query owner projections, or activate provider/retrieval/scheduler work.</behavior><action>Route runner ingress only to runner-assignment validation with body limits/rate limits. Never accept bearer data in logs/projections, use client workspace/profile/configuration values, or expose a generic callback. Make the adapter unavailable by default until the later hosted capability checkpoint, while local synthetic tests exercise it through dependency injection only.</action><verify><automated>cd site && node --test tests/runner-assignment.test.mjs</automated></verify><done>Transport preserves the untrusted-runner boundary and has no path to external effects.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| browser → owner API | Mutation inputs are hostile until owner admission and request security pass. |
| runner → runner API | Capability token is narrow and never becomes an owner session. |
## STRIDE Threat Register
| T-04-15 | CSRF/Elevation | owner mutations | mitigate | Existing request-security pattern plus strict commands and server scope. |
| T-04-16 | Spoofing/Disclosure | runner endpoint | mitigate | Separate assignment validation and neutral responses. |
</threat_model>
<verification><automated>cd site && node --test tests/profile-prospecting-contract.test.mjs tests/runner-assignment.test.mjs</automated></verification>
<success_criteria>All Phase 4 transport is secure, authoritative, and reject-only for unproven external execution.</success_criteria>
<output>Create `.planning/phases/04-profile-readiness-and-evidence-based-prospecting/04-08-SUMMARY.md` only when complete.</output>
