"""ONE for Mining — OSS contact enrichment pipeline.

Self-hosted alternative to a paid enrichment connector. Takes companies the
nightly prospector found, applies the ICP certification gate, and only then
discovers + verifies contacts (so credit/effort is spent on warm leads only).
"""
__version__ = "0.1.0"
