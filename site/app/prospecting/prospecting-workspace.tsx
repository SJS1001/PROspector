import React from "react";
import { ProfileReadiness } from "./profile-readiness"; import { ProspectWorkspace } from "./prospect-workspace"; import { ReviewQueue } from "./review-queue";
export function ProspectingWorkspace({ projection }: { projection: { authority?: string; readiness?: { status?: string }; configuration?: unknown; evidence?: unknown[]; candidates?: unknown[]; queue?: unknown[] } }) { return <main aria-live="polite"><h1>Profile Readiness and Prospect Workspace</h1>{projection.authority === "blocked" && <p role="alert">Load the current Product, Market Play, Offer, and Phase 3 authorities before preparing this profile.</p>}<ProfileReadiness readiness={projection.readiness ?? {}} /><ProspectWorkspace evidence={projection.evidence} /><ReviewQueue queue={projection.queue} /><section><h2>Later-phase controls</h2>{["Enrich contact", "Buy credits", "Export CRM", "Approve package", "Send email", "Call prospect"].map(label => <button key={label} disabled>{label} disabled</button>)}</section></main>; }
