"use client";

import { useCallback, useEffect, useRef, useState } from "react";
import { ProductReadinessView, type ReadinessProjection } from "./product-readiness";
import { ProposalCards, isAuthoritativeProposal, type Proposal } from "./proposal-cards";

const UNKNOWN = "Authoritative discovery results could not be verified. Reload this view.";
const READ_ERROR = "Authoritative Product discovery could not be loaded. No readiness, run, or proposal authority has changed. Reload this view.";
const authorityUnknown = "authority_unknown";
type ProductProjection = { id: string; name: string; lifecycle: "draft" | "ready" | "paused" | "archived"; revision: number };
type Projection = { products: ProductProjection[]; selectedProductId: string | null; readiness: ReadinessProjection | null; runs: Array<Record<string, unknown>>; proposals: Proposal[]; privateSyntheticProof: Record<string, unknown> | null };

export function DiscoveryWorkspace({ onUnauthorized }: { onUnauthorized: () => void }) {
  const [projection, setProjection] = useState<Projection | null>(null); const [state, setState] = useState<"loading" | "ready" | "unknown" | "error">("loading"); const [selectedProductId, setSelectedProductId] = useState<string | null>(null); const [pending, setPending] = useState<string | null>(null); const keys = useRef(new Map<string, string>());
  const operationKey = (logical: string) => { const existing = keys.current.get(logical); if (existing) return existing; const key = crypto.randomUUID(); keys.current.set(logical, key); return key; };
  const load = useCallback(async (requested?: string | null) => { setState("loading"); try { const response = await fetch(`/api/discovery${requested ? `?productId=${encodeURIComponent(requested)}` : ""}`, { credentials: "same-origin", cache: "no-store" }); if (response.status === 404) return onUnauthorized(); if (!response.ok) throw new Error("read_error"); const next = validate(await response.json()); const stored = typeof window === "undefined" ? null : localStorage.getItem("prospector.discovery.product"); const usable = [requested, next.selectedProductId, stored].find((id): id is string => typeof id === "string" && next.products.some((product) => product.id === id)) ?? null; setProjection(next); setSelectedProductId(usable); setState("ready"); } catch (error) { setProjection(null); setState(error instanceof Error && error.message === "malformed_projection" ? "unknown" : "error"); } }, [onUnauthorized]);
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [load]);
  useEffect(() => { if (selectedProductId && typeof window !== "undefined") localStorage.setItem("prospector.discovery.product", selectedProductId); }, [selectedProductId]);
  const mutate = async (body: Record<string, unknown>, logical: string) => { setPending(logical); try { const response = await fetch("/api/discovery", { method: "POST", credentials: "same-origin", cache: "no-store", headers: { "content-type": "application/json", "x-prospector-intent": "discovery-mutation" }, body: JSON.stringify({ ...body, idempotencyKey: operationKey(logical) }) }); if (response.status === 404) return onUnauthorized(); if (!response.ok) throw new Error("mutation_error"); const next = validate(await response.json()); setProjection(next); setSelectedProductId(next.selectedProductId); setState("ready"); } catch { setState("unknown"); } finally { setPending(null); } };
  if (state === "loading") return <section className="panel loading-state" role="status">Loading authoritative Product readiness…</section>;
  if (state === "unknown") return <section className="panel discovery-unknown" data-authority={authorityUnknown} role="alert"><h1>{UNKNOWN}</h1><p>{READ_ERROR}</p><button className="outline" type="button" onClick={() => void load(selectedProductId)}>Reload this view</button></section>;
  if (state === "error" || !projection) return <section className="panel discovery-unknown" role="alert"><h1>{READ_ERROR}</h1><button className="outline" type="button" onClick={() => void load(selectedProductId)}>Reload this view</button></section>;
  const selected = projection.products.find((product) => product.id === selectedProductId) ?? null;
  return <section className="discovery-workspace"><header className="page-heading"><div><span className="eyebrow">PRODUCT · OWNER-ONLY</span><h1>Market Discovery</h1><p>Product discovery can create bounded Market Play proposals. Prospecting, contacts, schedules for Customer Profiles, spend, exports, and outbound effects remain unavailable until their separate gates are met.</p></div></header><nav className="discovery-local-nav" aria-label="Market Discovery sections"><a href="#readiness" aria-current="page">Readiness</a><a href="#discovery">Discovery</a><a href="#proposal-history">Proposal history</a></nav><section className="panel product-picker"><label htmlFor="product-picker">Product picker</label><select id="product-picker" value={selectedProductId ?? ""} onChange={(event) => { const id = event.target.value || null; setSelectedProductId(id); void load(id); }}><option value="">Select a Product</option>{projection.products.map((product) => <option key={product.id} value={product.id}>{product.name} · {product.lifecycle}</option>)}</select></section>{!selected ? <section className="panel"><h2>Select a Product</h2><p>Choose an authoritative Product. Market Plays and Customer Profiles do not select Product scope.</p></section> : <><p className="discovery-breadcrumb"><a href="#knowledge">Digitalrain</a> / <button type="button" onClick={() => void load(selected.id)}>{selected.name}</button> / <span aria-current="page">Market Discovery</span></p>{projection.readiness ? <div id="readiness"><ProductReadinessView projection={projection.readiness} pending={pending === "ready" ? "ready" : pending === "discover" ? "discover" : null} onMakeReady={() => void mutate({ action: "make_product_ready", productId: selected.id, expectedProductRevision: selected.revision, confirmedVersions: projection.readiness?.confirmedVersions ?? [] }, "ready")} onDiscover={() => void mutate({ action: "start_manual_discovery", productId: selected.id, expectedProductRevision: selected.revision }, "discover")} /></div> : <section className="panel discovery-unknown" role="alert">{UNKNOWN}</section>}<section id="discovery" className="panel discovery-run"><h2>Latest Market Discovery run</h2>{projection.runs[0] ? <code>{projection.runs[0].id}</code> : <p>No Product discovery run is currently projected.</p>}</section>{projection.privateSyntheticProof?.authorizationId && <section className="panel synthetic-proof"><h2>Private synthetic proof</h2><p>Owner-authorized non-network synthetic proof only. It is not discovery execution.</p><code>{projection.privateSyntheticProof.evidenceReference}</code><button className="outline" type="button" disabled={projection.privateSyntheticProof.consumed || Boolean(pending)} onClick={() => void mutate({ action: "submit_private_synthetic_proof", productId: selected.id, expectedProductRevision: selected.revision }, "synthetic")}>Submit non-network synthetic proof</button></section>}<div id="proposal-history"><ProposalCards authority={projection.readiness ? "known" : "unknown"} proposals={projection.proposals} triggerLabel={projection.runs[0]?.triggerKind ?? "current"} pendingProposalId={pending?.startsWith("proposal:") ? pending.slice(9) : null} onDecision={(proposal, decision, fields) => void mutate({ action: "decide_proposal", proposalId: proposal.id, expectedProposalRevision: proposal.revision, expectedProposalDigest: proposal.digest, decision, ...fields }, `proposal:${proposal.id}`)} /></div></>}</section>;
}
function validate(value: unknown): Projection {
  const projection = record(value);
  if (!projection || !Array.isArray(projection.products) || !Array.isArray(projection.runs) || !Array.isArray(projection.proposals) || !Object.hasOwn(projection, "readiness") || !Object.hasOwn(projection, "privateSyntheticProof")) throw malformed();
  if (!projection.products.every(validProduct) || projection.products.some((product, index, all) => all.findIndex((candidate) => candidate.id === product.id) !== index)) throw malformed();
  const selectedProductId = projection.selectedProductId === null ? null : opaqueId(projection.selectedProductId) ? projection.selectedProductId : malformed();
  const selected = selectedProductId === null ? null : projection.products.find((product) => product.id === selectedProductId) ?? null;
  if (selectedProductId !== null && !selected) throw malformed();
  if ((selected === null && (projection.readiness !== null || projection.runs.length !== 0 || projection.proposals.length !== 0 || projection.privateSyntheticProof !== null)) || (selected !== null && (!validReadiness(projection.readiness, selected) || !projection.runs.every((run) => validRun(run, selected.id)) || projection.runs.length > 1 || projection.proposals.length > 3 || !projection.proposals.every(isAuthoritativeProposal) || !validPrivateProof(projection.privateSyntheticProof)))) throw malformed();
  return { products: projection.products, selectedProductId, readiness: projection.readiness as ReadinessProjection | null, runs: projection.runs as Array<Record<string, unknown>>, proposals: projection.proposals as Proposal[], privateSyntheticProof: projection.privateSyntheticProof as Record<string, unknown> | null };
}

function validProduct(value: unknown): value is ProductProjection { const product = record(value); return Boolean(product && opaqueId(product.id) && text(product.name) && ["draft", "ready", "paused", "archived"].includes(string(product.lifecycle)) && positiveInteger(product.revision)); }
function validReadiness(value: unknown, product: ProductProjection) { const readiness = record(value); if (!readiness || !record(readiness.product) || readiness.product.id !== product.id || readiness.product.name !== product.name || readiness.product.lifecycle !== product.lifecycle || readiness.product.revision !== product.revision || !Array.isArray(readiness.checklist) || !Array.isArray(readiness.missingCategories) || !Array.isArray(readiness.confirmedVersions)) return false; const categories = new Set(["capability", "limitation", "delivery", "proof", "ownership", "claim_guardrail", "source_policy", "discovery_policy", "default_runner_policy"]); if (readiness.checklist.length !== 9 || readiness.checklist.some((item) => { const row = record(item); return !row || !categories.delete(string(row.category)) || !["confirmed", "missing"].includes(string(row.status)); })) return false; return readiness.confirmedVersions.every(validVersionReference) && (readiness.status === "incomplete" || validReadyAuthority(readiness)); }
function validReadyAuthority(readiness: Record<string, unknown>) { const configuration = record(readiness.configuration); const initialRun = record(readiness.initialRun); const schedule = record(readiness.monthlySchedule); const manual = record(readiness.manualDiscovery); return Boolean(configuration && opaqueId(configuration.id) && digest(configuration.digest) && configuration.active === true && configuration.immutable === true && initialRun && opaqueId(initialRun.id) && opaqueId(initialRun.configurationId) && digest(initialRun.configurationDigest) && positiveInteger(initialRun.revision) && text(initialRun.executionState) && schedule && opaqueId(schedule.id) && positiveInteger(schedule.revision) && schedule.cadence === "monthly" && text(schedule.executionState) && positiveTimestamp(schedule.nextRunAt) && manual && manual.available === true && manual.executionState === "blocked_missing_capability" && record(readiness.descendants)); }
function validVersionReference(value: unknown) { const version = record(value); return Boolean(version && opaqueId(version.id) && digest(version.digest) && text(version.category)); }
function validRun(value: unknown, productId: string) { const run = record(value); const configuration = run && record(run.configuration); const trigger = run && record(run.trigger); const window = run && record(run.window); return Boolean(run && opaqueId(run.id) && positiveInteger(run.revision) && run.productId === productId && configuration && opaqueId(configuration.id) && digest(configuration.digest) && trigger && ["initial", "monthly", "manual", "material_change"].includes(string(trigger.kind)) && text(trigger.key) && positiveTimestamp(run.startedAt) && window && (window.lowerExclusive === null || positiveTimestamp(window.lowerExclusive)) && positiveTimestamp(window.upperInclusive) && text(run.executionState)); }
function validPrivateProof(value: unknown) { if (value === null) return true; const proof = record(value); return Boolean(proof && proof.capability === "private-hosted-synthetic-proposal-proof" && (proof.authorizationId === null || opaqueId(proof.authorizationId)) && (proof.evidenceReference === null || text(proof.evidenceReference)) && (proof.expiresAt === null || positiveTimestamp(proof.expiresAt)) && typeof proof.consumed === "boolean" && (proof.consumedAt === null || positiveTimestamp(proof.consumedAt))); }
function record(value: unknown): Record<string, unknown> | null { return value && typeof value === "object" && !Array.isArray(value) ? value as Record<string, unknown> : null; }
function opaqueId(value: unknown) { return typeof value === "string" && /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value); }
function digest(value: unknown) { return typeof value === "string" && /^[a-f0-9]{64}$/i.test(value); }
function positiveInteger(value: unknown) { return Number.isSafeInteger(value) && Number(value) > 0; }
function positiveTimestamp(value: unknown) { return positiveInteger(value); }
function text(value: unknown) { return typeof value === "string" && value.trim().length > 0; }
function string(value: unknown) { return typeof value === "string" ? value : ""; }
function malformed(): never { throw new Error("malformed_projection"); }
