import React, { useEffect, useRef, useState } from "react";

const ENDPOINT = "/api/contacts/person-discovery";
const PEOPLE_LIMIT = 5;
type DiscoveryStatus = "not_started" | "requested" | "completed" | "needs_reconciliation" | "stale_authority";
type Decision = "no_match" | "create_new" | "link_existing";
type Notice = { kind: "loading" | "ready" | "error" | "stale"; text: string };
type Candidate = Readonly<{ candidateId: string; ordinal: number; state: "suggestion_not_contact" | "payload_unavailable"; eligible: false; displayName?: string; roleTitle?: string; roleSummary?: string; provenance?: { sourceReference: string; excerpt: string; retrievedAt: number } | null }>;
type PersonPage = Readonly<{ runId: string | null; resultDigest: string | null; status: DiscoveryStatus; items: readonly Candidate[]; pageInfo: { limit: 5; returned: number; hasNext: boolean; nextCursor: string | null } }>;
type Prospect = Readonly<{ prospectId: string; prospectRevision: number; label: string; knownPerson: boolean }>;
type Contact = Readonly<{ contactId: string; contactRevision: number; label: string }>;
type Relevance = Readonly<{ relevanceId: string; prospectId: string; contactId: string; contactRevision: number; contactLabel: string; decisionId: string; roleTitle: string }>;
type StaleObservation = Readonly<{ sourceObservationId: string; relevanceId: string; channel: "email" | "phone"; verifiedAt: number; status: "stale" }>;
export type PersonDiscoveryProjection = Readonly<{ capability: "reject_only" | "test_composed_only"; approvedProspects: readonly Prospect[]; linkableContacts: readonly Contact[]; people: PersonPage; history: { runs: readonly { runId: string; prospectId: string; state: "requested" | "completed" | "needs_reconciliation"; resultDigest: string | null }[]; relevance: readonly Relevance[]; verificationIntents: readonly { relevanceId: string; intent: "initial_verification" | "stale_refresh"; channel: "email" | "phone" }[]; staleTrustedObservations: readonly StaleObservation[] } }>;

export type PersonDiscoveryUiState = Readonly<{ candidateId: string; contactId: string; decision: Decision | ""; confirmed: boolean; cursor: string | null; previous: readonly (string | null)[]; notice: Notice; pending: boolean }>;
export const initialPersonDiscoveryUiState: PersonDiscoveryUiState = Object.freeze({ candidateId: "", contactId: "", decision: "", confirmed: false, cursor: null, previous: Object.freeze([]), notice: { kind: "loading" as const, text: "Loading person-discovery status…" }, pending: false });
export function reducePersonDiscoveryUi(state: PersonDiscoveryUiState, event: "select_candidate" | "select_contact" | "choose_no_match" | "choose_create" | "choose_link" | "confirm" | "next" | "previous" | "cursor_drift" | "unknown_result" | "refresh_loaded"): PersonDiscoveryUiState {
  if (event === "select_candidate") return { ...state, candidateId: "selected-candidate", confirmed: false };
  if (event === "select_contact") return { ...state, contactId: "selected-contact", confirmed: false };
  if (event === "choose_no_match") return { ...state, decision: "no_match", confirmed: false };
  if (event === "choose_create") return { ...state, decision: "create_new", confirmed: false };
  if (event === "choose_link") return { ...state, decision: "link_existing", confirmed: false };
  if (event === "confirm") return { ...state, confirmed: true };
  if (event === "next") return { ...state, previous: [...state.previous, state.cursor], cursor: "next-cursor" };
  if (event === "previous") return { ...state, previous: state.previous.slice(0, -1), cursor: state.previous[state.previous.length - 1] ?? null };
  if (event === "cursor_drift") return { ...state, candidateId: "", contactId: "", decision: "", confirmed: false, cursor: null, previous: [], notice: { kind: "stale", text: "This list changed while you were reviewing it. The first page was reloaded; select a person again." } };
  if (event === "unknown_result") return { ...state, notice: { kind: "stale", text: "The request result is unknown. Status was refreshed; it was not retried." } };
  return { ...state, pending: false };
}
export function createPersonDiscoveryMutationGuard() { let attempted = false; return Object.freeze({ begin() { if (attempted) return false; attempted = true; return true; }, finish() { attempted = false; } }); }
export function personDiscoveryUrl(prospectId: string, cursor: string | null = null) { const query = new URLSearchParams({ prospectId }); if (cursor) query.set("peopleCursor", cursor); return `${ENDPOINT}?${query}`; }
export async function postPersonDiscoveryCommand(fetcher: typeof fetch, command: Record<string, unknown>) { return fetcher(ENDPOINT, { method: "POST", credentials: "same-origin", headers: { "content-type": "application/json", "x-prospector-intent": "person-discovery-mutation" }, body: JSON.stringify(command) }); }

export function normalizePersonDiscoveryProjection(value: unknown): PersonDiscoveryProjection | null {
  if (!record(value) || !member(value.capability, ["reject_only", "test_composed_only"]) || !Array.isArray(value.approvedProspects) || !Array.isArray(value.linkableContacts) || !record(value.people) || !record(value.history) || !Array.isArray(value.history.runs) || !Array.isArray(value.history.relevance) || !Array.isArray(value.history.verificationIntents) || !Array.isArray(value.history.staleTrustedObservations)) return null;
  const approvedProspects = value.approvedProspects.map(prospect), linkableContacts = value.linkableContacts.map(contact), people = personPage(value.people), runs = value.history.runs.map(run), relevance = value.history.relevance.map(relevanceRow), verificationIntents = value.history.verificationIntents.map(intent), staleTrustedObservations = value.history.staleTrustedObservations.map(staleObservation);
  if (!approvedProspects.every(isPresent) || !linkableContacts.every(isPresent) || !people || !runs.every(isPresent) || !relevance.every(isPresent) || !verificationIntents.every(isPresent) || !staleTrustedObservations.every(isPresent)) return null;
  return { capability: value.capability as PersonDiscoveryProjection["capability"], approvedProspects, linkableContacts, people, history: { runs: runs as PersonDiscoveryProjection["history"]["runs"], relevance, verificationIntents: verificationIntents as PersonDiscoveryProjection["history"]["verificationIntents"], staleTrustedObservations } };
}

export function PersonDiscoveryWorkspace() {
  const [projection, setProjection] = useState<PersonDiscoveryProjection | null>(null);
  const [selectedProspectId, setSelectedProspectId] = useState("");
  const [ui, setUi] = useState<PersonDiscoveryUiState>(initialPersonDiscoveryUiState);
  const guard = useRef(createPersonDiscoveryMutationGuard());
  const statusRef = useRef<HTMLParagraphElement>(null);
  const chosen = projection?.approvedProspects.find((item) => item.prospectId === selectedProspectId) ?? null;
  const person = chosen && !chosen.knownPerson ? chosen : null;
  const candidate = projection?.people.items.find((item) => item.candidateId === ui.candidateId && item.state === "suggestion_not_contact") ?? null;
  const selectedContact = projection?.linkableContacts.find((item) => item.contactId === ui.contactId) ?? null;
  const relevance = projection?.history.relevance.find((item) => item.prospectId === selectedProspectId) ?? null;
  const stale = relevance ? projection?.history.staleTrustedObservations.find((item) => item.relevanceId === relevance.relevanceId) ?? null : null;
  const resultDigest = projection?.people.resultDigest ?? projection?.history.runs.find((item) => item.runId === projection.people.runId)?.resultDigest ?? null;
  const canMutate = projection?.capability === "test_composed_only" && !!person && !ui.pending;
  const decisionReady = !!ui.decision && ui.confirmed && (ui.decision === "no_match" || !!candidate) && (ui.decision !== "link_existing" || !!selectedContact);
  const focusStatus = () => queueMicrotask(() => statusRef.current?.focus());
  const setNotice = (notice: Notice) => setUi((state) => ({ ...state, notice }));
  const resetReview = () => setUi((state) => ({ ...state, candidateId: "", contactId: "", decision: "", confirmed: false }));
  async function load(prospectId: string, cursor: string | null, preserveNotice = false): Promise<void> {
    if (!prospectId) return;
    setUi((state) => ({ ...state, pending: true, ...(preserveNotice ? {} : { notice: { kind: "loading", text: "Loading authoritative person-discovery status…" } }) }));
    try {
      const response = await fetch(personDiscoveryUrl(prospectId, cursor), { headers: { accept: "application/json" }, credentials: "same-origin" });
      if (response.status === 409) { setUi((state) => reducePersonDiscoveryUi(state, "cursor_drift")); if (!preserveNotice) await load(prospectId, null, true); return; }
      if (!response.ok) { setNotice({ kind: "error", text: "Person discovery is unavailable. Reload and try again; no provider call was made." }); return; }
      const parsed = normalizePersonDiscoveryProjection(await response.json());
      if (!parsed) { setNotice({ kind: "error", text: "The person-discovery response was incomplete. No action was taken." }); return; }
      setProjection(parsed); setUi((state) => ({ ...state, cursor, pending: false, candidateId: "", contactId: "", decision: "", confirmed: false, notice: preserveNotice ? state.notice : { kind: "ready", text: parsed.capability === "reject_only" ? "Person discovery is unavailable in this runtime. No provider call can be made." : "Authoritative person-discovery status loaded." } }));
    } catch { setNotice({ kind: "error", text: "Person discovery could not be loaded. Check your connection; no provider call was made." }); }
    finally { setUi((state) => ({ ...state, pending: false })); focusStatus(); }
  }
  useEffect(() => { void fetch(ENDPOINT, { headers: { accept: "application/json" }, credentials: "same-origin" }).then(async (response) => response.ok ? normalizePersonDiscoveryProjection(await response.json()) : null).then((value) => { if (!value) { setNotice({ kind: "error", text: "Person discovery is unavailable. No provider call was made." }); return; } setProjection(value); const first = value.approvedProspects.find((item) => !item.knownPerson); if (first) { setSelectedProspectId(first.prospectId); void load(first.prospectId, null); } else setNotice({ kind: "ready", text: "No current approved prospect needs a person decision." }); }).catch(() => setNotice({ kind: "error", text: "Person discovery could not be loaded. No provider call was made." })); // initial read only
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
  async function recoverUnknown(message: string) { if (!selectedProspectId) return; setNotice({ kind: "stale", text: message }); await load(selectedProspectId, null, true); }
  async function command(body: Record<string, unknown>, unknown: string) { if (!guard.current.begin()) return; setUi((state) => ({ ...state, pending: true })); try { const response = await postPersonDiscoveryCommand(fetch, body); if (!response.ok) await recoverUnknown(unknown); else if (selectedProspectId) await load(selectedProspectId, null); } catch { await recoverUnknown(unknown); } finally { guard.current.finish(); setUi((state) => ({ ...state, pending: false })); focusStatus(); } }
  function startDiscovery() { if (!canMutate || !person || projection?.people.status === "requested" || projection?.people.status === "needs_reconciliation") return; resetReview(); void command({ action: "start_person_discovery", prospectId: person.prospectId, expectedProspectRevision: person.prospectRevision, maxCandidates: 20, maxProvenancePerCandidate: 8, idempotencyKey: crypto.randomUUID() }, "The request result is unknown. Status was refreshed; it was not retried."); }
  function recordDecision() { if (!canMutate || !person || !projection?.people.runId || !resultDigest || !decisionReady) return; void command({ action: "decide_person_discovery", runId: projection.people.runId, expectedResultDigest: resultDigest, decision: ui.decision, candidateId: ui.decision === "no_match" ? null : candidate!.candidateId, existingContactId: ui.decision === "link_existing" ? selectedContact!.contactId : null, expectedProspectRevision: person.prospectRevision, idempotencyKey: crypto.randomUUID() }, "The decision result is unknown. Status was refreshed; it was not retried."); }
  function verificationIntent(kind: "initial_verification" | "stale_refresh") { if (!canMutate || !person || !relevance || (kind === "stale_refresh" && !stale)) return; const channel = kind === "stale_refresh" ? stale!.channel : "email"; void command({ action: "record_verification_intent", relevanceId: relevance.relevanceId, intent: kind, channel, sourceObservationId: kind === "stale_refresh" ? stale!.sourceObservationId : null, expectedProspectRevision: person.prospectRevision, expectedContactRevision: relevance.contactRevision, idempotencyKey: crypto.randomUUID() }, "The verification intent result is unknown. Status was refreshed; it was not retried."); }
  function selectProspect(id: string) { setSelectedProspectId(id); setUi((state) => ({ ...state, cursor: null, previous: [], candidateId: "", contactId: "", decision: "", confirmed: false })); if (id) void load(id, null); }
  function move(direction: "next" | "previous") { if (!selectedProspectId || ui.pending) return; if (direction === "next" && projection?.people.pageInfo.nextCursor) { setUi((state) => ({ ...state, previous: [...state.previous, state.cursor] })); void load(selectedProspectId, projection.people.pageInfo.nextCursor); } if (direction === "previous" && ui.previous.length) { const prior = ui.previous[ui.previous.length - 1] ?? null; setUi((state) => ({ ...state, previous: state.previous.slice(0, -1) })); void load(selectedProspectId, prior); } }
  const serviceReason = projection?.capability === "reject_only" ? "Unavailable: this ordinary runtime has no authorized discovery service, so no provider call can be made." : !person ? "Choose a current approved prospect with no known person first." : ui.pending ? "Wait for the current request to finish." : "";
  return <section className="person-discovery-workspace" aria-labelledby="person-discovery-heading"><header><span className="eyebrow">CONTACTS · PERSON DISCOVERY</span><h2 id="person-discovery-heading">Find suitable people</h2><p>Discovery suggests people for review. It is separate from verification and never makes a provider call unless an authorized service exists.</p></header><p ref={statusRef} tabIndex={-1} role="status" aria-live="polite" data-status={ui.notice.kind}>{ui.notice.text}</p><fieldset className="panel"><legend>Approved prospect</legend><label>Prospect needing a person <select aria-label="Approved prospect needing a person" value={selectedProspectId} disabled={ui.pending} onChange={(event) => selectProspect(event.target.value)}><option value="">Select an approved prospect</option>{projection?.approvedProspects.filter((item) => !item.knownPerson).map((item) => <option key={item.prospectId} value={item.prospectId}>{item.label}</option>)}</select></label>{person ? <p><strong>No known person</strong> is linked to this approved prospect.</p> : <p>Select an approved prospect with no known person.</p>}</fieldset><section className="panel" aria-labelledby="discovery-run-heading"><h3 id="discovery-run-heading">Discovery run</h3><p>{runMessage(projection?.people.status ?? "not_started")}</p>{serviceReason || projection?.people.status === "requested" || projection?.people.status === "needs_reconciliation" ? <DisabledAction id="find-suitable-people-reason" text={serviceReason || (projection?.people.status === "requested" ? "A discovery run is already requested. Wait for its authoritative result; it will not be retried automatically." : "This outcome needs reconciliation before another discovery run can be requested.")}>Find suitable people</DisabledAction> : <button type="button" className="primary" onClick={startDiscovery}>Find suitable people</button>}</section>{projection?.people.status === "completed" ? <section className="panel" aria-labelledby="suggested-people-heading"><h3 id="suggested-people-heading">Suggested people</h3><p>Every result is a suggestion, not a Contact. No send, call, export, package, or verified-contact control is available here.</p>{projection.people.items.length ? <ul className="person-candidate-list">{projection.people.items.map((item) => item.state === "payload_unavailable" ? <li key={item.candidateId}><strong>Suggestion unavailable</strong><p>Its retained review data is no longer available. Select another current suggestion.</p></li> : <li key={item.candidateId}><label><input type="radio" name="person-candidate" checked={ui.candidateId === item.candidateId} onChange={() => setUi((state) => ({ ...state, candidateId: item.candidateId, confirmed: false }))}/> <strong>Suggested person — not yet a contact</strong></label><dl><div><dt>Name</dt><dd>{item.displayName}</dd></div><div><dt>Title</dt><dd>{item.roleTitle}</dd></div><div><dt>Role</dt><dd>{item.roleSummary}</dd></div><div><dt>Provenance</dt><dd>{item.provenance ? item.provenance.sourceReference : "No retained provenance is available for this suggestion."}</dd></div><div><dt>Time</dt><dd>{item.provenance ? new Date(item.provenance.retrievedAt).toLocaleDateString() : "Not available"}</dd></div><div><dt>State</dt><dd>Not eligible for contact actions</dd></div></dl>{item.provenance ? <details><summary>Review bounded evidence</summary><p>{item.provenance.excerpt}</p></details> : null}</li>)}</ul> : <p>No suitable people were returned. You may explicitly record No match.</p>}<PersonPageControls info={projection.people.pageInfo} canGoBack={ui.previous.length > 0} loading={ui.pending} onPrevious={() => move("previous")} onNext={() => move("next")}/></section> : null}{projection?.people.status === "completed" ? <fieldset className="panel"><legend>Explicit owner decision</legend><p>Choose one outcome. Nothing is selected automatically.</p><label><input type="radio" name="person-decision" checked={ui.decision === "no_match"} onChange={() => setUi((state) => ({ ...state, decision: "no_match", confirmed: false }))}/> No match</label><label><input type="radio" name="person-decision" checked={ui.decision === "create_new"} onChange={() => setUi((state) => ({ ...state, decision: "create_new", confirmed: false }))}/> Create new person</label><label><input type="radio" name="person-decision" checked={ui.decision === "link_existing"} onChange={() => setUi((state) => ({ ...state, decision: "link_existing", confirmed: false }))}/> Link existing person</label>{ui.decision === "link_existing" ? <label>Exact existing Contact <select value={ui.contactId} onChange={(event) => setUi((state) => ({ ...state, contactId: event.target.value, confirmed: false }))}><option value="">Select one current Contact</option>{projection.linkableContacts.map((item) => <option key={item.contactId} value={item.contactId}>{item.label}</option>)}</select></label> : null}<label><input type="checkbox" checked={ui.confirmed} disabled={!ui.decision || ui.pending} onChange={(event) => setUi((state) => ({ ...state, confirmed: event.target.checked }))}/> I confirm this explicit decision.</label>{!decisionReady || !canMutate ? <DisabledAction id="record-person-decision-reason" text={!canMutate ? serviceReason || "This action is unavailable in the current runtime." : ui.decision === "link_existing" && !selectedContact ? "Select one exact current Contact before linking." : ui.decision === "no_match" ? "Confirm the No match decision before recording it." : !candidate ? "Select one current suggested person before recording this decision." : "Confirm this explicit decision before recording it."}>Record decision</DisabledAction> : <button type="button" onClick={recordDecision}>Record decision</button>}</fieldset> : null}{relevance ? <fieldset className="panel"><legend>Verify business contact details</legend><p>A person link is not verification. Recording an intent never calls a provider or makes any contact detail eligible.</p>{canMutate ? <button type="button" onClick={() => verificationIntent("initial_verification")}>Verify business contact details</button> : <DisabledAction id="verify-contact-details-reason" text={serviceReason || "The current runtime cannot record a verification intent."}>Verify business contact details</DisabledAction>}{stale ? canMutate ? <button type="button" onClick={() => verificationIntent("stale_refresh")}>Refresh verification</button> : <DisabledAction id="refresh-verification-reason" text={serviceReason || "The current runtime cannot record a refresh intent."}>Refresh verification</DisabledAction> : <DisabledAction id="refresh-verification-unavailable" text="No stale trusted contact evidence is available to refresh.">Refresh verification</DisabledAction>}<p>Generated, directory, domain, or MX data is never eligible.</p></fieldset> : null}</section>;
}
export function PersonPageControls({ info, canGoBack, loading, onPrevious, onNext }: { info: PersonPage["pageInfo"]; canGoBack: boolean; loading: boolean; onPrevious(): void; onNext(): void }) { const previousReason = loading ? "Wait for the current page request to finish." : "There is no earlier people page."; const nextReason = loading ? "Wait for the current page request to finish." : "There is no later people page."; return <nav className="person-page-controls" aria-label="Suggested people pages"><span>Showing {info.returned} people on this page.</span>{loading || !canGoBack ? <DisabledAction id="previous-people-reason" text={previousReason}>Previous people</DisabledAction> : <button type="button" onClick={onPrevious}>Previous people</button>}{loading || !info.hasNext || !info.nextCursor ? <DisabledAction id="next-people-reason" text={nextReason}>Next people</DisabledAction> : <button type="button" onClick={onNext}>Next people</button>}</nav>; }
export function DisabledAction({ children, text, id }: { children: string; text: string; id: string }) { return <span className="disabled-action"><button type="button" disabled aria-describedby={id}>{children}</button><span id={id} tabIndex={0}>{text}</span></span>; }
function runMessage(status: DiscoveryStatus) { return ({ not_started: "No discovery run has been requested.", requested: "Discovery is requested. Wait for an authoritative result; do not retry automatically.", completed: "The latest discovery run is complete. Review suggestions before deciding.", needs_reconciliation: "The latest discovery outcome needs reconciliation. It was not retried.", stale_authority: "The prior authority is stale. Reload and select a current approved prospect." } as const)[status]; }
function prospect(value: unknown): Prospect | null { return record(value) && exact(value, ["prospectId", "prospectRevision", "label", "knownPerson"]) && opaque(value.prospectId) && positive(value.prospectRevision) && text(value.label, 160) && typeof value.knownPerson === "boolean" ? value as Prospect : null; }
function contact(value: unknown): Contact | null { return record(value) && exact(value, ["contactId", "contactRevision", "label"]) && opaque(value.contactId) && positive(value.contactRevision) && text(value.label, 160) ? value as Contact : null; }
function personPage(value: Record<string, unknown>): PersonPage | null { if (!exact(value, ["runId", "status", "items", "pageInfo"]) && !exact(value, ["runId", "status", "items", "pageInfo", "resultDigest"])) return null; if ((value.runId !== null && !opaque(value.runId)) || (value.resultDigest !== undefined && value.resultDigest !== null && !digest(value.resultDigest)) || !member(value.status, ["not_started", "requested", "completed", "needs_reconciliation", "stale_authority"]) || !Array.isArray(value.items) || !record(value.pageInfo)) return null; const items = value.items.map(candidate), pageInfo = page(value.pageInfo); return items.every(isPresent) && pageInfo ? { runId: value.runId, resultDigest: digest(value.resultDigest) ? value.resultDigest : null, status: value.status as DiscoveryStatus, items, pageInfo } : null; }
function candidate(value: unknown): Candidate | null { if (!record(value) || !opaque(value.candidateId) || !ordinal(value.ordinal) || value.eligible !== false || !member(value.state, ["suggestion_not_contact", "payload_unavailable"])) return null; if (value.state === "payload_unavailable") return exact(value, ["candidateId", "ordinal", "state", "eligible"]) ? { candidateId: value.candidateId, ordinal: value.ordinal, state: "payload_unavailable", eligible: false } : null; const allowed = exact(value, ["candidateId", "ordinal", "displayName", "roleTitle", "roleSummary", "candidateDigest", "provenance", "state", "eligible"]); const provenance = value.provenance === null ? null : provenanceRow(value.provenance); return allowed && text(value.displayName, 160) && text(value.roleTitle, 160) && text(value.roleSummary, 1000) && digest(value.candidateDigest) && (value.provenance === null || !!provenance) ? { candidateId: value.candidateId, ordinal: value.ordinal, displayName: value.displayName, roleTitle: value.roleTitle, roleSummary: value.roleSummary, provenance, state: "suggestion_not_contact", eligible: false } : null; }
function provenanceRow(value: unknown): Candidate["provenance"] { return record(value) && exact(value, ["sourceReference", "excerpt", "retrievedAt"]) && text(value.sourceReference, 1000) && text(value.excerpt, 1000) && positive(value.retrievedAt) ? { sourceReference: value.sourceReference, excerpt: value.excerpt, retrievedAt: value.retrievedAt } : null; }
function run(value: unknown): PersonDiscoveryProjection["history"]["runs"][number] | null { return record(value) && exact(value, ["runId", "prospectId", "state", "resultDigest"]) && opaque(value.runId) && opaque(value.prospectId) && member(value.state, ["requested", "completed", "needs_reconciliation"]) && (value.resultDigest === null || digest(value.resultDigest)) ? value as PersonDiscoveryProjection["history"]["runs"][number] : null; }
function relevanceRow(value: unknown): Relevance | null { return record(value) && exact(value, ["relevanceId", "prospectId", "contactId", "contactRevision", "contactLabel", "decisionId", "roleTitle"]) && opaque(value.relevanceId) && opaque(value.prospectId) && opaque(value.contactId) && positive(value.contactRevision) && text(value.contactLabel, 160) && opaque(value.decisionId) && text(value.roleTitle, 160) ? value as Relevance : null; }
function intent(value: unknown): PersonDiscoveryProjection["history"]["verificationIntents"][number] | null { return record(value) && opaque(value.relevanceId) && member(value.intent, ["initial_verification", "stale_refresh"]) && member(value.channel, ["email", "phone"]) ? { relevanceId: value.relevanceId, intent: value.intent as "initial_verification" | "stale_refresh", channel: value.channel as "email" | "phone" } : null; }
function staleObservation(value: unknown): StaleObservation | null { return record(value) && exact(value, ["sourceObservationId", "relevanceId", "channel", "verifiedAt", "status"]) && opaque(value.sourceObservationId) && opaque(value.relevanceId) && member(value.channel, ["email", "phone"]) && positive(value.verifiedAt) && value.status === "stale" ? value as StaleObservation : null; }
function page(value: Record<string, unknown>): PersonPage["pageInfo"] | null { return exact(value, ["limit", "returned", "hasNext", "nextCursor"]) && value.limit === PEOPLE_LIMIT && nonNegative(value.returned) && value.returned <= PEOPLE_LIMIT && typeof value.hasNext === "boolean" && (value.nextCursor === null || cursor(value.nextCursor)) && value.hasNext === (value.nextCursor !== null) ? { limit: 5, returned: value.returned, hasNext: value.hasNext, nextCursor: value.nextCursor } : null; }
function record(value: unknown): value is Record<string, unknown> { return !!value && typeof value === "object" && !Array.isArray(value); } function exact(value: Record<string, unknown>, keys: readonly string[]) { const actual = Object.keys(value).sort(), expected = [...keys].sort(); return actual.length === expected.length && actual.every((key, index) => key === expected[index]); } function member<T extends readonly string[]>(value: unknown, choices: T): value is T[number] { return typeof value === "string" && choices.includes(value); } function opaque(value: unknown): value is string { return typeof value === "string" && /^[A-Za-z0-9][A-Za-z0-9_-]{0,159}$/.test(value); } function digest(value: unknown): value is string { return typeof value === "string" && /^[a-f0-9]{64}$/.test(value); } function positive(value: unknown): value is number { return typeof value === "number" && Number.isSafeInteger(value) && value > 0; } function ordinal(value: unknown): value is number { return typeof value === "number" && Number.isSafeInteger(value) && value >= 0 && value < 20; } function nonNegative(value: unknown): value is number { return typeof value === "number" && Number.isSafeInteger(value) && value >= 0; } function cursor(value: unknown): value is string { return typeof value === "string" && /^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/.test(value) && value.length <= 1200; } function text(value: unknown, maximum: number): value is string { return typeof value === "string" && value.length > 0 && value.length <= maximum; } function isPresent<T>(value: T | null): value is T { return value !== null; }
