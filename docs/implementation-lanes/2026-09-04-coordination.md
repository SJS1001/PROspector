# Parallel implementation coordination — 2026-09-04

## Authority and baseline

The owner authorizes isolated cloud code implementation and focused synthetic
validation across the remaining product plan. This expands code preparation,
not operational activation or phase acceptance. Preflight remains on hold.
Do not run preflight, canonical full validation, deployment, hosted writes,
Access changes, credentials, provider spending, real prospecting, outbound
communications, or exports to external recipients in these lanes.

Read `AGENTS.md` and its required sources before changing code. Preserve the
retired-target prohibition and all exact-evidence acceptance gates. No new
phase completion credit follows from this dispatch record.

GitHub read on this date confirms PR #2 was merged at 14:37:08 UTC.
The new integration base is `38d86681cd7a8e9f5be70b56365d9be2a786f0ad`
(`origin/main`); its tree is identical to pilot checkpoint
`6affd08682c7fa4167ccf7cccf6acd8eb41d44bc`. The old pilot checkout has no
tracked edits. Unrelated untracked directories are not implementation inputs.
Use isolated branches; never push work directly to main or the pilot branch.

## Capability evidence

One ChatGPT Work cloud probe reported an actual repository checkout at the
pilot SHA, isolated branch, successful scratch write/read/remove, Node
`v24.19.0`, npm `11.9.0`, and 12 passing tests from
`node --test tests/phase7-preparation-weekly-outcome.test.mjs`, with a clean
tree and no commit or external activation. This is delegated capability
evidence, not acceptance of subsequent implementations. Each new task must
independently verify its checkout/runtime and return tested code or an exact
blocker. The local Codex CLI separately exposed no selectable cloud execution
environment; that limitation does not describe the working ChatGPT route.

## First implementation wave

All six cloud tasks were accepted and observed active. No implementation
result was accepted at dispatch time. Branch names, file ownership and Git
results are the durable retrieval keys; task IDs are optional navigation only.

| Branch suffix (`codex/cloud-…`) | Exclusive responsibility | Required proof |
|---|---|---|
| `contact-evidence-ui` | Contacts leaves/workspace, optional confirmation-state helper, two new focused tests | Sanitized evidence method/time; confirmation cleared on stale, refresh and uncertain outcomes; operations still disabled |
| `enrichment-integration` | New controlled-enrichment integration test and dedicated fixture helper | Existing-service synthetic lifecycle, replay, uncertainty and current-authority rejection; no forged acceptance |
| `crm-csv-codec` | `domain/crm-csv-codec.ts` and focused test | Actual canonical 22-column bytes, escaping/formula safety, stable ordering and exact-byte digest; no persistence/delivery/eligibility grant |
| `weekly-outcome` | `domain/weekly-outcome.ts` and focused test | First-ever Export-ready history, local week/DST and separate losses/counts; incomplete history is unavailable, not invented success |
| `outreach-persistence` | New `0010` migration/metadata, outreach schema additions, repository and dedicated tests/helper | Immutable scope-bound records, separate approvals, atomicity/replay/rollback and importer-safe triggers; no active handlers or effects |
| `mail-boundary` | Mail port, disconnected unconfigured Gmail adapter, focused boundary test | Bounded types, every operation rejects, no credential/network/provider composition or automatic retry |

Each lane may write only its own `docs/implementation-lanes/<suffix>.md`
record in addition to its assigned implementation files. Schema ownership
belongs exclusively to outreach-persistence. Existing migrations `0000`–`0009`
must remain byte-identical; older plans incorrectly reserve an already-used
`0009` slot. Do not import completed preparation-only modules into runtime or
replace actual missing implementation with more synthetic authority labels.

The local test-only branch `codex/outreach-worker-guard` owns only the
outreach composition scanner test. It is separate from cloud implementation
capacity and may use focused Node tests, not the held validation lane.
Its candidate `9f3295b9f30c0a38025fe1da0525658ad01fce73` adds Worker-directory
coverage and adversarial fixtures. The coordinator reviewed the one-file diff
and independently reproduced all three focused tests plus `git diff --check`.
Canonical validation remains pending; no production code changed.

## Dependency waves and remaining work

1. Review focused results from the independent first-wave lanes. Fetch exact
   commits, inspect diffs and ownership, reproduce relevant tests, and resolve
   interface conflicts before integration. Missing tests or push capability
   must be recorded, not represented as passing.
2. Build durable Package/Message operations, suppression/stop processing,
   outbox/DeliveryUnknown and manual-call logic on the accepted outreach
   schema. Keep provider adapters disabled and use synthetic local fixtures.
3. Compose governed read/command surfaces, Morning Brief and handoff history
   only after their actual authority interfaces exist. Current ContactReady
   and approved states are not Export-ready; do not fabricate the missing
   authoritative transition/history or remove reject-only downstream checks.
4. Implement archive/restore only against a reviewed capability/format
   contract. Target-specific recovery evidence, owner passphrase and actual
   restore remain distinct gates; code cannot satisfy them.
5. After explicit release of the preflight hold, run canonical integrated
   validation and independent security/UI review. Keep deployment and real
   provider/owner acceptance separate. New PRs need normal review; PR #2 is
   historical, not an open draft to update.

The existing Phase 2 private-boundary checkpoint remains unchanged: saved
owner-only policy is not yet attached to the Worker, URLs remain disabled,
and later Access/runtime/principal acceptance needs separate authority.

## Planning estimate, not a delivery commitment

With at least three effective isolated coding lanes and coordinator review,
the initial estimate is 1–3 hours for a small reviewable patch and roughly
1–3 working weeks for broader remaining offline code/integration. Confidence
is low until actual results are measured. Six dispatched lanes do not imply
sixfold throughput: schema, outbox, UI and recovery have serial dependencies.
Owner decisions, provider/account setup, preflight hold and hosted acceptance
add unknown elapsed time. No whole-system completion date is claimed.

## Dispatch lookup

| Lane | Task ID |
|---|---|
| CSV codec / successful capability probe | `6a9af186-167c-83e9-9500-353e92a21757` |
| Contacts UI | `6a9af2c7-51cc-83ea-b15e-6a33a9837220` |
| Enrichment integration | `6a9af2c8-8eb4-83ea-890e-115fb6aaf51f` |
| Weekly outcomes | `6a9af2c7-ee28-83e9-8aba-a82cc66a611d` |
| Outreach persistence | `6a9af2c9-f970-83e9-9ecb-f001b890118e` |
| Mail boundary | `6a9af2c9-3e24-83ea-9e8c-fbc551149b79` |
