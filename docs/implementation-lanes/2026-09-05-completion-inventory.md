# PROspector completion inventory

**Audited checkpoint:** `ba697b73b9b53f6c48788006b757ea464085f7dd`  
**Integration branch:** `codex/cloud-wave1-integration`  
**Purpose:** distinguish implemented local capability from missing runtime work
and non-substitutable acceptance evidence.

This inventory is not a plan summary, phase-acceptance record, provider
authorization, hosted-evidence substitute, or permission to perform an external
effect. The original private Sites project remains retired and inaccessible.
Any future hosted environment must be greenfield.

## Executive result

The repository contains a guarded local owner runtime plus substantial Phase 4,
Phase 5, Phase 6 persistence, CRM CSV, and weekly-outcome foundations. It is not
yet an operational prospecting product. The checked application composition
does not issue runner assignments, retrieve prospects, call a contact provider,
send mail, place calls, materialize an export, or create/restore an archive.

Formal plan status remains:

| Phase | Formal plan status | Current evidence boundary |
| --- | --- | --- |
| 2 | `13/14` active plans | greenfield hosted real-principal acceptance remains |
| 3 | `8/11` | hosted no-effect proof and owner lifecycle acceptance remain |
| 4 | `0/12` | strong local production-code candidate; hosted runner/retrieval/schedule proof remains |
| 5 | `0/9` | strong local domain/persistence candidate; live Contacts commands remain incomplete |
| 6 | `0/13` | substantial provider-inert persistence; runtime services and acceptance remain |
| 7 | `0/10` | CSV/weekly cores only; runtime handoff/recovery system remains |

No unchecked plan may receive a summary merely because an analogous local,
synthetic, or preparation test passes.

## Runtime reachability at the audited checkpoint

Runtime-reachable owner surfaces cover the consensus interview, Knowledge,
product discovery, profile readiness/prospect review, and a read-only Contacts
projection. The runner-ingress API route and Contacts mutation capability are
explicitly fail-closed.

The following are implemented but not composed into the application runtime:

- Phase 4 runner assignment, ingestion, qualification, schedule reconciliation,
  and material-change run intent;
- Phase 5 grant/reservation/settlement, identity resolution, contact evidence,
  eligibility, and the provider-neutral enrichment operation;
- Phase 6 Package/Message approval persistence, suppression primitives, sender
  authority, outbox enqueue, lease, pre-call receipt, inert attempt preparation,
  and preparation recovery;
- the reject-only Gmail adapter and provider-neutral mail port;
- the deterministic 22-column CRM CSV codec and weekly-outcome reducer; and
- every module under `site/preparation/`.

`site/worker/index.ts` currently composes none of the Phase 5-7 service or effect
boundaries. Static disabled UI is not runtime capability.

## Dependency-correct remaining work

### Greenfield acceptance chain

1. Complete Phase 2 Plan 02-99 for the separately authorized greenfield target:
   exact source/release, private Access, real-principal isolation, negative
   mutation/zero-delta, and secret/log review evidence.
2. Complete Phase 3 Plans 03-09 through 03-11: bounded private no-effect proof,
   owner lifecycle acceptance, and transport-capability disposition.

These checkpoints cannot be replaced by local tests. Work below may continue as
an explicitly unaccepted local candidate, but cannot earn dependency-valid plan
credit before the chain above is satisfied.

### Phase 4 candidate closure

1. Re-run the exact current Phase 4 suites and canonical local gates against one
   immutable candidate; independently review that exact revision.
2. Add separately enabled, default-off assignment issuance and runner-ingress
   composition without storing provider credentials.
3. Add default-off safe-retrieval and schedule/worker composition.
4. Perform the bounded hosted runner, retrieval, DST/overlap/misfire/watermark,
   SSRF/redirect/MIME/byte-limit, and owner lifecycle proofs.

The owner-facing profile readiness, manual run intent, review queue, deterministic
rubric, provenance policy, and persistence are already substantive local code.

### Phase 5 candidate closure

1. Finish the Contacts command boundary: server-derived create-grant,
   execute-granted-operation, and merge/split decisions; retain two-stage owner
   confirmation, fail-closed activation, safe projections, and an unconfigured
   production provider.
2. Expand integrated synthetic coverage for concurrency, caps, partial or
   uncertain settlement, identity changes, downstream freshness/suppression
   rechecks, and legacy MCP non-reachability.
3. Run current-head canonical validation and exact-revision independent review.
4. Keep real provider/account/quote/credential/spend activation separately
   authorized.

### Phase 6 candidate closure

1. Close migration `0017` with the complete outreach/importer regression run and
   an adversarial proof that only the exact pre-`0016` missing-table condition is
   tolerated.
2. Under one serialized schema/outbox owner, add provider-result terminal states,
   DeliveryUnknown evidence/reconciliation, and transactional cancellation.
3. In non-overlapping files, build runtime Package/Message services, authoritative
   suppression/originated-stop processing, and manual-call activity/DNC services.
4. Compose one worker-only mail boundary using a fake transport first. Add owner
   routes/UI only after the domain seams are integrated.
5. Keep Gmail OAuth, credentials, real messages, telephony, and real calls behind
   separate owner and provider checkpoints.

Suppression cancellation, dispatch finalization, and manual `do_not_call` share
the outbox/tombstone transaction boundary and must not be implemented by
concurrent schema writers.

### Phase 7 candidate closure

1. Correct stale plan references before execution: Phase 7 must not reuse its
   planned `0010` filename. Allocate its number only after every serialized
   predecessor migration has landed; at this checkpoint `0018` is the next
   globally available number and is not reserved for Phase 7. Every target
   reference must use the greenfield contract rather than the retired Sites
   project.
2. Add the missing Morning Brief, CRM handoff, and recovery RED contracts.
3. Add a serialized handoff/recovery schema and repository, then build Morning
   Brief and CRM materialization/manifest/delivery services.
4. Accept a maintained authenticated-encryption/KDF envelope and clean compatible
   target before archive/restore implementation or drills.
5. Add Exports & History UI, integrated validation, independent review, and a
   separately authorized controlled clean-target restore drill.

The existing CSV codec and weekly reducer are reusable cores, but neither reads a
transactional eligibility snapshot, persists a version, delivers an artifact, or
proves recovery.

Plans 07-01 through 07-03 remain dependency-blocked on Plan 06-10, and later
Phase 7 plans remain blocked on their declared predecessors and capability
checkpoints. Earlier local work is an unaccepted candidate only.

## Active implementation queue

1. `0017` recovery validation is the sole current schema/journal/outbox lane.
2. The Contacts runtime candidate may proceed in parallel because it owns only the
   Contacts handler, route, UI leaves/workspace, and focused tests.
3. After `0017` is terminal, select exactly one next schema owner. No other lane
   may allocate migration `0018` or edit the journal concurrently.
4. Integrate completed lane commits serially into
   `codex/cloud-wave1-integration`, review the combined diff, and run one final
   canonical validation/publication lane.

## External-effect boundary

Until separately authorized, do not provision or modify a hosted target, bind a
provider or credential, access real prospect/contact data, spend money, activate
a schedule, send mail, place a call, deliver an export, or perform an archive or
restore against an external system. Tests must use synthetic data and fake or
reject-only ports.
