# LOCAL_DEMO cross-phase composition track

**Status:** proposed, separate, and unaccepted. This planning amendment grants
no plan/phase, hosted, provider, data, export, credential, or effect authority.

This track permits only fictional, disposable, effect-free composition of the
existing Phase 4 prospecting, Phase 5 contact, Phase 6 outreach, and Phase 7
preview foundations under the existing `LOCAL_DEMO` guard, on loopback. Use
synthetic IDs, `.invalid` addresses, fictional `555-01xx` numbers, local clocks,
and disposable local state only. It is not a second activation path.

The original Sites project remains permanently retired and outside the
execution path. Nothing here resolves, inspects, restores, migrates, or depends
on it. The greenfield hosted target and all human/provider/control-plane gates
remain separately unaccepted.

## Dependency order

1. Phase 4 prospecting: consume only checked local profile/prospecting
   foundations and synthetic qualification/evidence; Phase 4 remains the sole
   prospecting and schedule authority.
2. Phase 5 contact: consume synthetic Phase 4-shaped projections and the
   existing reject-only provider boundary; fake ports are test injection only.
3. Phase 6 outreach: consume synthetic verified-contact/package and
   approval/suppression projections; retain provider-neutral no-invocation.
4. Phase 7 preview: consume synthetic weekly-outcome and handoff/CSV-policy
   projections without operational materialization or delivery.

Every slice remains runtime-unreachable outside a loopback request passing the
existing `LOCAL_DEMO=1`, Vite-development, local-demo identity, loopback-host,
and exact-origin checks. Hosted/production configuration must continue to reject
`LOCAL_DEMO`.

## Verification and effect-zero criteria

Focused tests, relevant static boundary scans, and applicable lint/build checks
must pass. A browser journey uses fictional disposable data, proves reads do
not seed data, and proves missing identity, wrong origin, missing/replayed CSRF,
and non-local/production configuration fail closed.

Verification must observe literal zero provider/phone calls, messages,
exports/downloads, schedule/runner activity, credentials/secrets, hosted
requests/writes, and unauthorized durable mutations. No real address, phone,
message body, provider handle, token, or private record may enter fixtures,
logs, artifacts, or bundles. Preparation-only modules remain outside runtime
composition unless a later checked plan supplies new acceptance evidence.

Passing proves only a disposable local preview. It does not prove real
prospecting, enrichment, verification, approval, sending, calling, suppression
persistence, CRM/CSV handoff, archive/restore, hosted acceptance, or plan/phase
completion. No plan summary or completion mark may be created from this track.

Stop if work requires a real principal, hosted target/database evidence,
provider/paid request, credential, external export, phone/email effect,
schedule/runner, persistent operational state, or a new owner decision.
